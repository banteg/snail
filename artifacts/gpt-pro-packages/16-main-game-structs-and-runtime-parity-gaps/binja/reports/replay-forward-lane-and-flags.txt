Fresh `update_subgoldy` evidence from the live Binary Ninja database narrows the remaining replay gap:

- `0043b2c3` loads the saved compact replay `i16` at `selected_level_record + runtime_track_index * 6 + 0x70`.
- That lane is already grounded enough to treat as the replay lateral value. The Zig port now uses it as a direct world-`x` override.
- `0043cd59` and `0043cd74` each call the same type-16 conversion helper on the second saved `i16` lane at `+0x72`.
- The downstream effects near that block include current-track-pair payload and backdrop progress helpers, which means the second lane is not safely modeled yet.
- `0043d12c` tests replay flag bit `0x1`.
- `0043d17c` tests replay flag bit `0x2`.
- Both flag branches feed movement-state or sound-side behavior through `play_movement_state_sound`.
- Replay flag bit `0x8` is already ported as the recorded-tail marker.

Questions to answer from this cluster:

1. Is the saved `forward` lane absolute track `z`, local row progress, backdrop progress, or a mixed owner payload?
2. Do replay bits `0x1` and `0x2` map to movement-state transitions, sound-only transitions, or both?
3. Is replay exit after bit `0x8` owned by gameplay, fade, or a higher frontend controller?
