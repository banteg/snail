Completion handoff controller follow-up
======================================

High-confidence corrections from the live BN pass:

- `player + 0x440` is `completion_handoff_active`
- `player + 0x444` is `completion_handoff_timer`
- `player + 0x448` is `completion_handoff_timer_step`
- `player + 0x44e` is `completion_handoff_voice_gate`

Current practical read:

- `update_subgoldy` arms this controller once the player reaches the course-end threshold at `game + 0x58`, provided no attachment-exit handoff is pending.
- On arm, it seeds:
  - `completion_handoff_active = 1`
  - `completion_handoff_timer = 0`
  - `completion_handoff_timer_step = 1/60`
  - `completion_handoff_voice_gate = 0`
- While active, it forces the presentation lane toward the fixed completion path (`track_z_offset` / `track_z_anchor` -> `320`) and advances the timer every tick.
- Once `completion_handoff_timer > 2.0`, `completion_handoff_voice_gate` gates a one-shot `play_voice_manager(..., 8, 2, -1)` dispatch.
- If `row_event_display.state == 5`, `update_subgoldy` forces `completion_handoff_timer = 5.1`.
- If `row_event_display + 0x18 == 1` and the current runtime cell has flag `0x40`, it also forces `completion_handoff_timer = 5.1`.
- Once `completion_handoff_timer > 5.0`, `update_subgoldy` begins the frontend fade, flushes `row_event_display` if active, and routes through `complete_subgame`.

Still unresolved:

- the exact gameplay name for `row_event_display + 0x18`
- the precise semantic name for the course-end scalar at `game + 0x58`
