Parcel runtime follow-up
=========================

High-confidence corrections from the live BN/IDA pass:

- `game + 0x125e480` is the real `track_parcels` array base.
- the old `game + 0x125e430` placement is rejected.
- `initialize_runtime_pools_and_path_template_bank` seeds that array with `initialize_track_parcel_runtime` (`0x408860`).
- `build_subgame_level` clears the live array through `initialize_track_parcel_slots` (`0x443160`).
- `spawn_track_parcel` (`0x443730`) is the Windows `cRSubGame::AddParcel` path:
  - allocates one slot through `allocate_track_parcel_slot` (`0x443190`)
  - writes `state = 1`
  - copies `world_position`
  - stores the parcel owner pointer
  - installs sprite `0x79`
- `update_track_parcel` (`0x4431d0`) is the parcel AI:
  - state `1` is the bobbing live pickup
  - states `4/5` home toward the mailbox anchor
  - states `6/7` run the final delivery arc
  - delivery completion calls `register_parcel_delivery(&parcel->game->row_event_display)`, kills the sprite, and clears the slot
- the earlier `update_jetpack_visual` name is rejected.
- the earlier `cRParcel::AI -> 0x43f520` guess is rejected; `0x43f520` is only a tiny helper under `update_slug_hazard_ai`.

Current high-confidence `TrackParcelRuntime` fields:

- `+0x10`: `world_position`
- `+0x38`: `state`
- `+0x3c`: `game`
- `+0x54`: `sprite`
- `+0x5c`: `bob_phase`
- `+0x60`: `bob_phase_step`
- `+0x64`: `owner`
- `+0x68`: `progress`
- `+0x6c`: `progress_step`
- `+0x70`: `target_distance`
- `+0x74`: `travel_dir_x`
- `+0x78`: `travel_dir_y`
- `+0x7c`: `travel_dir_z`
- `+0x80`: `delivery_offset_x`
- `+0x84`: `delivery_offset_y`
- `+0x88`: `delivery_offset_z`
