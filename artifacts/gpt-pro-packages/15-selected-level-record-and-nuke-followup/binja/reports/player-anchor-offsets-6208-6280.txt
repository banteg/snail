# Player anchor mapping for `+6208` (`0x1840`) and `+6280` (`0x1888`)

Recovered writer chain:

- `build_snail_hotspots` seeds a `19`-entry local hotspot bank at `player + 0x16cc`
- `update_snail_skin` transforms that bank into the live world-space hotspot bank at `player + 0x17b0`
  - slots `0..10` use the cached matrix at `player + 0x1684`
  - slots `11..18` use the cached matrix at `player + 0x1604`

That resolves the old standalone offsets:

- `+0x1840` is `snail_hotspots_world[12].x` (`CameraSkidStop`)
- `+0x1888` is `snail_hotspots_world[18].x` (`CameraIntroTalk`)

Recovered authored hotspot names from `build_snail_hotspots`:

- index `12`: `X/CameraSkidStop.tga`
- index `17`: `X/CameraSlugDeath`
- index `18`: `X/CameraIntroTalk`

Recovered cutscene consumers:

- `0x44675c` / `0x446dbe` / `0x446ac4` / `0x4467d0` / `0x4469b0` all snapshot or reuse hotspot `18`
- `0x446c29` / `0x446caa` use the `12 -> 18` lerp in the state-`5` completion blend

Interpretation:

- the old `+6208 / +6280` mystery is not a pair of isolated cutscene-only fields
- intro or completion or death shot geometry is sourced from the transformed snail-hotspot bank maintained by `update_snail_skin`
- no direct runtime consumer for hotspot `17` (`CameraSlugDeath`) was recovered in this pass
- the exact gameplay roles of the two source matrices, and the reason later cutscene legs keep reusing the authored `CameraIntroTalk` hotspot, are still not clean enough to freeze
