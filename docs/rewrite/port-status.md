# Port Status

This page is the short ledger for what the Zig runtime currently treats as verified, partial, fallback, scaffold, or debug-only.

Reference only:
- not default loop context
- use the active dossier or packet first
- update this ledger only when shipped behavior changes or a subsystem model materially changes

For the fuller subsystem-by-subsystem tracker, see [subsystem-status.md](subsystem-status.md).

Code-level convention:

- `PORT(verified)`: backed by static RE or runtime captures strongly enough to treat as ported behavior
- `PORT(partial)`: on the right path, but still missing some semantics or coverage
- `PORT(fallback)`: deliberate approximation or candidate layer, not the original runtime behavior yet
- `PORT(scaffold)`: temporary shell or structural placeholder to support forward-pass work
- `PORT(debug)`: intentionally non-shipping tooling or inspection code

Current high-signal entries:

- boot and main-menu shell: `scaffold`
  - evidence: original loading-screen assets and recovered main-menu labels are now wired, and the main menu, new-game menu, options, pause, and high-score screens now render as direct authored-space widget overlays instead of the older generic panel shell. The overall front-end flow and most handlers are still not at original parity.
  - implementation: [`main.zig`](../../zig/src/main.zig)
  - replace when: title flow, menu assets, menu actions, and front-end copy are ported
- loading-screen composition: `verified`
  - evidence: Binary Ninja decompile of `sub_418b50` and `sub_418e80`, plus the shipped `SPRITES/LOADING.TGA` and `SPRITES/LOADINGBARON.TGA` assets
  - implementation: [`loading_screen.zig`](../../zig/src/loading_screen.zig), [`main.zig`](../../zig/src/main.zig)
- loading-screen progress driver: `partial`
  - evidence: Binary Ninja decompile shows `update_loading_screen` is called from archive or file readers plus a startup initialization loop, so the original bar is activity-backed rather than time-backed
  - implementation: [`main.zig`](../../zig/src/main.zig)
  - replace when: the port moves more of the original world-init and startup loops behind the loading screen instead of only the current front-end asset, script, music, and score loads
- window bootstrap and default windowed presentation size: `verified`
  - evidence: Binary Ninja decompile of `sub_4119d0`, which falls back to a 640x480 client area in windowed mode and only exposes 4:3 fullscreen presets
  - implementation: [`main.zig`](../../zig/src/main.zig)
  - note: the current `snail` default is a deliberate development window (`1024x768`) over that verified base size
- top-level menu labels plus recovered `New Game` submenu: `partial`
  - evidence: Binary Ninja decompile of `initialize_main_menu`, `destroy_main_menu`, and `initialize_new_game_menu`, plus cross-port Android and iOS symbols for `cRMainMenu::{Init, UnInit}`
  - implementation: [`main.zig`](../../zig/src/main.zig)
  - note: the current port now positions those labels directly on the `Menubg` board area instead of reusing the older debug-style left-list/detail-panel shell. It also mirrors the recovered config-backed tutorial gate from `initialize_new_game_menu`: default `SnailMail.cfg` keeps `Postal Mode`, `Time Trial`, and `Challenge Mode` hidden until the `Tutorial` branch flips the byte at `+0xc0`.
  - replace when: deeper front-end progression and the remaining menu widget behavior are ported
- shared exit prompt: `partial`
  - evidence: Binary Ninja decompile of `initialize_exit_prompt`, plus cross-port Android and iOS symbols for `cRExit::{Init, AI}`
  - implementation: [`main.zig`](../../zig/src/main.zig)
  - note: the main-menu exit path now uses a shared `Do you really want to quit?` prompt instead of terminating immediately
  - replace when: the remaining owner-specific layouts and any post-completion uses of the shared exit prompt are ported
- front-end options menu fields: `partial`
  - evidence: Binary Ninja decompile of `initialize_options_menu`, `update_options_menu`, `destroy_options_menu`, and `apply_audio_config_volumes`, plus cross-port Android and iOS symbols for `cROptions::{Init, AI, UnInit}`
  - implementation: [`config.zig`](../../zig/src/config.zig), [`main.zig`](../../zig/src/main.zig)
  - note: the current port now renders the recovered fullscreen/music/sound/back rows directly on `Menubg` instead of the older two-panel helper UI
  - replace when: the original slider widgets, audio test feedback, and any remaining option fields are ported
- help screen flow: `partial`
  - evidence: Binary Ninja decompile of `initialize_help_screen`, `destroy_help_screen`, and `update_help_screen`, plus cross-port Android and iOS symbols for `cRHelp::{Init, AI}`
  - implementation: [`main.zig`](../../zig/src/main.zig)
  - note: the current port now returns `Back` to the recovered New Game flow instead of jumping straight to the main menu, which matches the Windows `update_help_screen -> frontend state 2` handoff better
  - replace when: the original Help-screen teardown path and any missing transition details are ported
- delivery-route map flow: `partial`
  - evidence: Binary Ninja decompile of `initialize_galaxy`, `destroy_galaxy`, `update_galaxy`, `close_galaxy_route`, `open_galaxy_route`, and `load_frontend_level_by_mode_and_index`, plus cross-port Android and iOS symbols for `cRGalaxy::{Init, AI, BoxOff, Open}` and Android `cRGalaxy::UnInit()`
  - implementation: [`config.zig`](../../zig/src/config.zig), [`main.zig`](../../zig/src/main.zig)
  - note: selected and unlocked route values are now treated as verified 1-based route numbers, so route `1` maps to `ARCADE001`, the Star Map card now follows the Windows open/close interaction model instead of staying always open, and the normal `Back` path now returns to the New Game menu instead of skipping straight to the main menu
  - replace when: the original Star Map widget layout, replay launch path, and remaining progression semantics are ported
- front-end background loading plus Distort-driven single-texture motion: `partial`
  - evidence: shipped `BACKGROUNDS/*.TXT` scripts and matching TGA assets, Binary Ninja decompile of `set_backdrop_distort`, `change_backdrop`, `initialize_backdrop`, `change_backdrop_real`, `draw_split_backdrop`, `render_backdrop`, and `update_backdrop`, plus cross-port Android and iOS symbols for `cRBackdrop::{SetDistort, Change, ChangeReal, Init, Render, AI}`
  - implementation: [`background.zig`](../../zig/src/background.zig), [`main.zig`](../../zig/src/main.zig)
  - note: the port now also runs a separate native-shaped star-field or light-streak overlay alongside the Distort backdrop, with a persistent `36`-entry controller, the recovered four-state fade machine, intro/credits/gameplay visibility gating, and textured `STARTAIL` quads driven by the native alpha and stretch lanes. The native sprite asset is now mapped: `initialize_star_field` allocates sprite id `0x20`, and `initialize_game_assets_and_world` registers that id from `SPRITES/STARTAIL.TGA`. The remaining gap is narrower: the exact renderer-side transform slot behind `draw_sprite_quad` is still inferred as a camera-plane billboard rather than traced end-to-end through the D3D wrapper.
  - replace when: the distortion grid state, the exact star-field billboard and sprite-field semantics, any real `Landscape` model path, and the final warped UV orientation plus split `_A` / `_B` crop semantics are all verified against the original renderer
- intro and credits text-screen flow: `partial`
  - evidence: shipped `INTRO/INTRO.TXT`, `INTRO/CREDITS.TXT`, and Binary Ninja decompile of `initialize_intro_screen`, `destroy_intro_screen`, and `update_intro_screen`, plus cross-port Android and iOS symbols for `cRIntro::{Init, AI}` and Android `cRIntro::UnInit()`
  - implementation: [`intro.zig`](../../zig/src/intro.zig), [`main.zig`](../../zig/src/main.zig)
  - replace when: the remaining exact image-quad sizing and any non-intro users of the shared front-end transition overlay are ported
- high-score screen table branch: `partial`
  - evidence: Binary Ninja decompile of `initialize_high_score_screen`, `destroy_high_score_screen`, `update_high_score_screen`, `initialize_high_score_tables`, `initialize_high_score_entry`, `add_arcade_high_score`, `add_survival_high_score`, `add_time_trial_high_score`, and `exit_high_score_screen`, including the recovered 11-entry postal/challenge banks, 51-entry completion bank, and scratch entry, plus cross-port Android and iOS symbols for `cRHighScore::{Init, AI}` and `cRSubHighScore::{AddArcade, AddSurvival, AddTimeTrial}`, and Android `cRHighScore::{UnInit, Exit}`
  - implementation: [`high_score.zig`](../../zig/src/high_score.zig), [`main.zig`](../../zig/src/main.zig)
  - note: browse mode now behaves like a direct banked score screen instead of a separate postal/challenge/back submenu, and post-level name entry now reuses that same screen owner with an inline editable row. The renderer now also follows the recovered Windows table geometry more closely: type-23 title/footer widgets, type-22 row widgets, 27-pixel row pitch, and the postal/challenge score-vs-replay column swap. Replay rows are still read-only.
  - replace when: the original replay row controls and remaining `cRHighScore::AI()` behavior are ported
- gameplay pause menu flow: `partial`
  - evidence: Binary Ninja decompile of `update_subgame` states `3/4`, pause-menu init `sub_440660`, pause-menu update `sub_4407a0`, shared widget helpers `sub_402790` / `sub_4027b0`, and the shared exit-prompt/options constructors
  - implementation: [`main.zig`](../../zig/src/main.zig), [`frontend.zig`](../../zig/src/frontend.zig)
  - note: the level path now has a real pause overlay with `End Game`, `Options`, and `Resume`, and the options/quit screens can be entered in pause-owned context instead of only through the main menu. The exact pause-menu widget metadata, owner-specific exit-prompt variants, and the original paused-frame parity are still in progress.
  - replace when: the remaining pause-widget metadata, pause-owned exit-prompt variants, and the exact frozen gameplay frame behavior are ported
- mutable config and score-file root: `partial`
  - evidence: Binary Ninja decompile of `initialize_default_runtime_config`, `load_high_scores_from_file`, `save_high_scores_and_config`, `load_config_file`, and `save_config_file`, which read and write `ScoreA.dat`, `ScoreB.dat`, `ScoreC.dat`, and `SnailMail.cfg`
  - implementation: [`runtime_state.zig`](../../zig/src/runtime_state.zig), [`config.zig`](../../zig/src/config.zig), [`main.zig`](../../zig/src/main.zig)
  - replace when: the remaining unknown config fields and high-score overlay serializer are ported onto that runtime root
- compact high-score overlay loading and saveback: `partial`
  - evidence: Binary Ninja decompile of `deserialize_compact_high_score_record`, `serialize_compact_high_score_record`, `load_high_scores_from_file`, and `save_high_scores_and_config`
  - implementation: [`high_score.zig`](../../zig/src/high_score.zig), [`main.zig`](../../zig/src/main.zig)
  - note: current saveback now preserves unknown raw compact-record tails for loaded entries instead of regenerating every record as a header-only stub, but the port still does not synthesize new replay payload arrays
  - replace when: the port supports writing full replay overlays for new or edited entries and wires score mutation from the actual gameplay or front-end flow
- front-end font atlas registration, width measurement, and tutorial icon tokens: `verified`
  - evidence: Binary Ninja decompile of `sub_449d20`, `sub_449e90`, and `sub_449f50`, plus the shipped `OBJECTS/FONT/FONT-MENU-HOVER.TGA` atlas and tutorial `Message=` strings using `{`, `}`, `[`, `]`, `~`, `_`, `^`, and `;`
  - implementation: [`game_font.zig`](../../zig/src/game_font.zig), [`main.zig`](../../zig/src/main.zig)
- active segment `Sample=` / `Message=` routing in the default level path: `partial`
  - evidence: authored level metadata, the recovered Windows row-event controller shape around `row_event_*`, `enqueue_tip_message`, `update_row_event_display`, and `flush_row_event_display`, plus current tutorial-level behavior and Android `cRPlayer::AI()` calling `cRTipManager::UnInitTips()` before `TipNew(...)`
  - implementation: [`level_prompt.zig`](../../zig/src/level_prompt.zig), [`main.zig`](../../zig/src/main.zig)
  - note: the port now uses authored `Duration:` values when present, clears the previous tutorial tip before showing the next one, and treats the common tutorial row-event guidance as a single-owner `cRTip`-style card with an `OK` button instead of the older debug footer/banner scaffolding or custom dialogue box. The tutorial startup prompt now also goes through the same shared type-20 prompt/widget layout path instead of a custom HUD text path. `Sample=` playback on `VOICE/TUT*.OGG` runs on a dedicated voice channel instead of the old shared preview-sound slot, and tips no longer tick down while hidden under pause/overlay, which is closer to the original hide/unhide behavior. It still does not port the original row-event actor/controller, score-flush behavior, or the full three-slot general-purpose tip manager outside the tutorial flow.
  - replace when: full row-event timing/controller behavior, segment-transition behavior, and the remaining front-end/tutorial voice ownership are ported
- level completion and post-level score flow: `partial`
  - evidence: Binary Ninja decompile of `initialize_subgoldy_death`, `update_subgoldy_resurrect`, `complete_subgame`, `initialize_completion_screen`, `destroy_completion_screen`, `initialize_exit_prompt`, `update_completion_screen`, `initialize_cutscene`, `update_cutscene`, `add_arcade_high_score`, `add_survival_high_score`, `add_time_trial_high_score`, and `exit_high_score_screen`, plus cross-port Android and iOS symbols for `cRCompletion::{Init, AI, UnInit}`, `cRExit::Init()`, `cRCutScene::{Init, AI}`, `cRHighScore::{Init, AI, Exit}`, `cRSubHighScore::{AddArcade, AddSurvival, AddTimeTrial}`, and `cRSubGoldy::{DeathInit, RessurectAI, AI}`
  - implementation: [`main.zig`](../../zig/src/main.zig), [`high_score.zig`](../../zig/src/high_score.zig)
  - note: the runtime now uses a runner-local cutscene controller before the completion screen instead of the older app-level fake cutscene phase, and `Continue` can hand off into the shared high-score screen for inline name entry instead of a separate post-level overlay. Failed runs no longer use one blanket main-menu fallback in the port: postal still lands on `New Game`, challenge now rebuilds into a literal challenge-setup owner with the recovered `Select Difficulty` / `Select Speed` / `Play` / optional `Watch Replay` / `Back` controls, and Time Trial now returns to the replay Star Map, while all three still feed the native-shaped score-table side effects from `complete_subgame(..., 1)`. Ordinary pause-menu `End Game` is also less scaffolded now: postal and challenge abandons can stage the same shared high-score entry screen directly from the exit prompt instead of silently dropping their partial score, and that post-entry exit now returns through the recovered mode-owned owner lane instead of the older preserved-launch-surface shortcut. Ordinary failed-result score entry is now less app-local too: BN plus IDA show `complete_subgame -> add_arcade_high_score` / `add_survival_high_score` arming owner `0x14`, and `update_subgoldy_resurrect` then preserves that owner in the saved-owner slot before state `0x1b`, so the port now captures the later post-entry return owner up front once postal/challenge failure score entry takes over instead of keeping the completion-screen result object live through name entry. The completion screen now also stages its recovered postal summary copy more like Windows: the bonus line and `Click to Continue` prompt start hidden and reveal in sequence instead of appearing all at once. Deeper BN plus IDA reconciliation narrowed the replay-sensitive bridge split further: `update_galaxy` and `update_challenge_setup_screen` only arm the transient `selected_level_record_active` lane, while `update_subgoldy` / `update_subgoldy_resurrect` reserve state `0x1a` for the separate persistent lane and use `0x1b` for the non-persistent selected-record return path. The high-score replay rows and menu replay-attract path no longer look like a mysterious second writer: because `initialize_subgame` runs on `data_4df904 + 0x74618`, their app-side replay-launch scratch at `+0x1066be8/+0x1066be9/+0x1066bec/+0x1066bf0` aliases the subgame-local selected-record fields directly, so postal/challenge replay launches now use the recovered persistent lane while route-map best-trial launches stay transient. The bridge now also keeps that launch family as explicit replay context instead of deriving persistent replay returns from the selected-record source alone: replay launch payloads carry `source`, `persistent`, and saved return-owner state together, which matches the recovered `+0x1066bf0` / `+0xff25d8` lane more closely. Non-random level previews now also seed the completion threshold from the start of the final `Last:` block recovered from `populate_runtime_track_cells_from_segments`, and random challenge-style previews now use the recovered `floor((difficulty * 0.65 + 0.35) * Length) - last_block_len` threshold instead of the older last-row fallback even though the preview still does not build the native generated middle strip. `update_pause_menu` plus `update_completion_screen` now pin both pause-abandon replay legs: persistent selected-record pause `End Game` goes through completion state `3`, then destroys subgame and restores `app + 0x1066bf0`, while transient route-map replay pause abandon falls through completion state `2` and the native mode-`4` rebuild path without touching state `0x1c`, so neither replay abandon path reuses the respawn-only opcode-`28` clear-replay lane now. `update_subgoldy` now also pins the remaining replay-tail branch directly: selected-record sample bit `0x8` writes `state 0x1a`, saves owner `10`, sets app byte `+0x30c = 1`, and begins the front-end fade, so the port now restarts the current replay through the explicit destroy-return lane instead of jumping straight back to the launch surface at the marker. `initialize_subgame` and `update_subgame` also now pin `level_mode == 7` as tutorial mode, so the special `0x1a -> owner 2` completion override is the same already-ported tutorial-completion return lane rather than a separate missing owner branch. `complete_subgame` also now narrows the postal replay-loss side: because it only seeds the app `+0x30d` high-score continuation flag while `selected_level_record_active == 0`, transient selected-record postal final-loss runs keep the native `0x1a -> owner 2` New Game override instead of returning through `0x1b`. BN plus IDA now also close the ordinary non-selected postal final-loss split itself: `update_subgoldy_resurrect` only keeps postal mode on `0x1b` while that same `+0x30d` high-score continuation flag is still armed, and otherwise overwrites the saved owner with `2` before forcing `0x1a`. The same outer-bridge pass now also closes the Thanks owner’s post-run destination: `update_thanks_for_playing_screen` starts the confirm fade with `sfx 8`, and `uninit_thanks_screen` then writes state `0x0e`, so final postal completion now flows into the credits crawl instead of jumping back to the main menu. The cutscene camera blend is still a conservative approximation of `cRCutScene::AI()`. Postal scoring keeps the recovered final-bonus path separate from challenge runs and applies that bonus on the last parcel event instead of waiting until the run ends, but both modes still omit the missing slug/jetpack/replay-side score events.
  - replace when: the original cutscene camera handoff, the remaining completion/final-loss frontend routing, remaining saved-replay UI paths, shared exit-prompt flow, and the remaining gameplay score events are ported
- runtime floor-height sampling and tile `0x16` floor slot: `verified`
  - evidence: [`docs/re/track-runtime.md`](../re/track-runtime.md)
  - implementation: [`track.zig`](../../zig/src/track.zig)
- common gameplay build preset `0x00f5cfff`: `partial`
  - evidence: March 8 Frida captures plus track-runtime notes
  - implementation: [`track.zig`](../../zig/src/track.zig)
  - replace when: per-mode or per-level build-flag normalization is fully ported
- ambient garbage and salt spawning: `partial`
  - evidence: trace-confirmed fallback tile families on `0x01`, `0x0f`, and `0x15`, plus Windows `update_subgame` row-scan/live-strip behavior from the spawn/collision package
  - implementation: [`track.zig`](../../zig/src/track.zig), [`gameplay.zig`](../../zig/src/gameplay.zig), [`main.zig`](../../zig/src/main.zig)
  - note: the port no longer treats garbage/salt as raw always-live tile contacts. It now seeds authored and ambient candidates into an 8-row runner-local live strip and uses the recovered garbage/salt scalar thresholds for deterministic ambient rolls. The original suppressor bits, neighbor gates, mode gates, and the separate SubLazer projectile pool (`cRSubLazerManager` @ `game + 0x356b00`, 20 slots stride 0xb0, fired by Wall2 AI via `shoot_subgoldy` for the `+0.02` damage lane) are still missing. The live strip also conflates the native Salt hazard pool (`cRSalt` @ `game + 0x3578c0`, 40 slots stride 0x98, `+0.15` damage) with the simpler runner-local scaffold.
  - replace when: the original gameplay-side spawn timing, suppressor bits, mode gates, the Salt pool owner port, and the SubLazer projectile pool (historically misnamed "Wall2 ambient pool" in these docs — the Wall2 tile is the *emitter*; the slots themselves are projectiles) inside `update_subgame` / `handle_subgoldy_collisions` are ported
- attachment builder scaffold and segment viewer: `partial`
  - evidence: Binary Ninja and IDA path-constructor work, Android `LoadPaths`/`Traverse` cross-checks, and the current public `Path=` registry in the executable
  - implementation: [`attachment_builders.zig`](../../zig/src/attachment_builders.zig), [`track_render.zig`](../../zig/src/track_render.zig), [`debug_levels.zig`](../../zig/src/debug_levels.zig)
  - note: the `Segments` view now builds and renders every public authored attachment family through the shared Zig scaffold, including the nonlinear kind-`42` family, instead of only showing labels and row hints. The nonlinear viewer path now also uses the same decompile-backed transform helper shape as live gameplay instead of a separate circle-height approximation. The scaffold also now mirrors the recovered Windows row record shape more closely: each installed row keeps two owner slots instead of one flattened winner, and those owner slots now persist across the installed attachment's full row span instead of only on rows that still show live `29/30` tiles. The remaining gaps are the exact Windows bank-root selector, row-to-slot install pairing, and some family-specific constructor/install details.
- gameplay subgame lifecycle: `partial`
  - evidence: Binary Ninja decompile of `initialize_subgame`, `build_subgame_level`, `complete_subgame`, `destroy_subgame`, `update_subgame`, `initialize_subgoldy`, `show_subgoldy_lives`, `update_subgoldy`, `begin_post_follow_carryover`, `initialize_subgoldy_death`, `update_subgoldy_resurrect`, `handle_subgoldy_collisions`, `initialize_jetpack_gauge`, `update_jetpack_gauge`, `update_damage_gauge`, `apply_damage_gauge_delta`, `advance_timer_counters`, `update_subgame_camera`, `initialize_cameraman`, and `update_cameraman`, plus cross-port Android and iOS symbols for `cRSubGame::{Init, BuildLevel, Complete, AI, CameraAI, UnInit}`, `cRSubGoldy::{Init, ShowLives, DeathInit, RessurectAI, Collision, AI}`, `cRDamageGuage::{Init, AI}`, and `cRCameraman::{Init, AI}`
  - implementation: [`gameplay.zig`](../../zig/src/gameplay.zig), [`main.zig`](../../zig/src/main.zig), [`track.zig`](../../zig/src/track.zig)
  - note: the port now carries a runner-owned cutscene camera controller instead of app-side fake intro/completion/death cameras, with the outer shared-camera selector choosing live cameraman versus override matrix plus per-source snap/FOV. The same runner also now keeps a dedicated stopwatch shaped after `advance_timer_counters` instead of deriving route time only from `tick_count` at the result screen. The active-phase vertical motion is now a literal port of `update_subgoldy` lines 352-584 at `artifacts/ida/functions/0043b120-update_subgoldy.c`: `position_y` + `velocity_y` fields with per-tick integrate, `velocity.y *= 1 - track_center_x * 0.003` damping, gravity `velocity.y += -0.01 * track_center_x^2` on the airborne branch, floor sample + snap at `floor_y + 0.49`, the 0x43bf6f swept-reentry grounded-snap lane (`[-0.163, 0.49)` band, non-open-neighbor + non-trampoline tile, velocity non-positive), tile-family velocity.y reactions for ramps 2-7 (`= 0.8` + lookback dispatch) and slides 8-13 (`= 1.2`) plus the "other-tile zero" reset, the 0x43c3ea trampoline envelope (`|position.y - cell_y| < 0.49` → `velocity.y = 1.2`, `position.y = cell_y + 0.49`, clear pending, set post-trampoline-airborne), the `y < -7` death handoff, and the `position.y < 0 && velocity.y <= 0` tail that arms `begin_post_follow_carryover`. Remaining retirement lanes 0x43bcb3 (keyed on `_pad_41c[0]`) and 0x43c06d (keyed on `tile_flags_3d` + runtime/global flag bits) still need their underlying flag writers surfaced in the port. Postal and tutorial respawns no longer happen as a hidden runner-local reset; Binary Ninja disassembly of `update_subgoldy_resurrect` now confirms the respawn branch copies the current outer owner into the bridge return slot and sets active state `0x1c`, and the port now routes respawn through that same explicit rebuild-clear-replay bridge lane before restoring score, timer, lives, and parcel-register state onto the rebuilt runner. The remaining gap is narrower: Zig still performs the rebuild through the app-side level loader rather than the literal Windows frontend state machine. Current decompile readability is also better here: Windows states `26/27/28` are now understood as bridge opcodes around `destroy_subgame`/`initialize_subgame`, while `29/30` are the separate post-run Thanks For Playing owner rather than generic completion spillover. Attachment-follow is now materially deeper than the old row-midpoint fallback: the preview derives installed owner slots from the recovered row-span installer instead of just the visible attachment tiles, every public path family builds a Zig-side template, live gameplay follows those built templates for progress and world pose, the camera consumes the same attachment/launch frame, and the nonlinear kind-`42` branch now uses the recovered transform-helper shape instead of the older circle-only approximation. The March 15 Windows `cdb` session also confirms a full shipped `HALFPIPE` lifecycle in `ARCADE007`: real begin, live follow ticks, and a real exit helper that latches post-follow state, but that helper is not the only retirement lane. Installed attachment entry now mirrors the recovered split in `update_subgoldy`: current-row tiles `29/30` go through the direct begin path, while the separate re-entry helper uses the recovered swept local-frame test from `try_enter_track_attachment_from_swept_motion` instead of the older interpolated plane-crossing approximation. That re-entry lane is now also owned by the live current-row prime path in Zig: it only probes the current installed row while `attachment_exit_pending` is armed, keys those probes from the live row's recovered owner bits (`0x40` first, then `0x80`), and does not show an immediate helper-side clear before the secondary gate check. One later retirement slice is now ported too: the native late clear at `0x43ce75` sits behind `jetpack_gauge.state == 1`, so active jetpack flight no longer keeps the Zig runner waiting on the generic attachment-exit progress timeout. Hazard-triggered falls and floor-gap losses still share one airborne runner phase in Zig, with only the hazard path arming the death cutscene override. Remaining gaps are the exact Windows installed bank installer and source-row installer lane, the later controller that retires `attachment_exit_pending` after re-entry, the post-exit gate/carryover controller, and the remaining family-specific entry/exit semantics, especially for the nonlinear branch.
  - replace when: the original installed attachment bank, nonlinear family semantics, post-follow carryover and gate semantics, special launch exits, outer subgame reload flow, the exact cutscene shot anchors, and remaining mode-specific runtime behaviors are ported
- gameplay Turbo model render: `partial`
  - evidence: shipped `TURBO-MOVE` model/animation family, native animation-load order, and the now-built attachment-aware runner world pose
  - implementation: [`main.zig`](../../zig/src/main.zig), [`xanim.zig`](../../zig/src/xanim.zig), [`x2.zig`](../../zig/src/x2.zig)
  - note: the gameplay viewport now draws a live Turbo model instead of only the track scene. The steady gameplay clip maps id 2 conservatively to `TURBO-MOVE`, matching shipped animation/load order instead of the earlier `TURBO-BOBALONG` inference, and the gameplay weapon presentation now follows the recovered three-channel `movement_flags` owner instead of one coarse tier switch. Tutorial guidance now swaps Turbo into the shipped `turbo-talk` clip through the ported anim-dispatch layer (`AnimClipId` + `AnimDispatchState` + `dispatchCutsceneAnimation`) that also routes `.damaged` / `.intoshell` / `.fall` / `.lookback_l/r` clips on the matching gameplay events (`apply_damage_gauge_delta` ouch-fallback branch, `beginFallState` cause, down-ramp tile-family reaction). The renderer now reads the active clip id off `Runner.cutscene_anim.active` through the animation catalog's family-to-first-frame lookup instead of a hard-coded model-swap. The queue, per-clip mode bit flags, and the full `update_anim_manager` progress advancement are still rough; the core dispatch contract (immediate vs queued + queue pop on clip end) matches native.
  - replace when: Turbo uses the right gameplay anchor/orientation and switches clips cleanly by gameplay/cutscene/tutorial state instead of relying on a small manual family switch
- tutorial gameplay actor presentation: `partial`
  - evidence: shipped tutorial `Message=` strings and visible hazard/pickup instruction flow, the current runtime hazard/pickup counters in Zig, and the shipped sprite assets under `SPRITES/SLUG*`, `GARBAGE*`, `HEALTH`, `JETPACK*`, `PARCEL*`, `PARTICLERING*`, `PARTICLESLOW*`, and `PARTICLEBLASTERS`
  - implementation: [`main.zig`](../../zig/src/main.zig), [`gameplay.zig`](../../zig/src/gameplay.zig)
- note: the live gameplay viewport now renders slug, garbage, salt, health, jetpack, parcel, ring/powerup, and `=` turret actors instead of leaving tutorial interactions as invisible logic or debug markers. Salt now uses the shipped `X/SALT.X2` model path through a slot-owned hazard transform instead of the older row-distance fade, while garbage now renders from the live runtime hazard slot and keeps a post-hit burst state instead of staying a static row billboard. Turbo also now shows the recovered `movement_flags`-driven weapon channels and projectile layouts instead of one generic blaster presentation: center turbo blaster, paired side blaster lanes, right-only and paired laser lanes, plus the center rocket launcher family, alongside the invincible shell overlay. Weapon mounts now step through the shipped draw/fire model families (`BLASTERTOP-DRAW/FIRE`, `BLASTERLEFT/RIGHT-DRAW`, `LASERLEFT/RIGHT-DRAW`, `ROCKETLAUNCHER-DRAW`) instead of holding on the base meshes, and the jetpack controller now drives the shipped `JETPACKTHRUST` family across the native pre-warning slice instead of staying visually absent until full shutoff. The invincible shell now cycles the shipped base frames instead of staying static. Tutorial/combat SFX are also now wired into live gameplay for fire, weapon upgrades, pickups, invincible/slow powerups, explosive rings, and basic impact feedback on separate preloaded effect sounds instead of relying only on segment `Sample=` playback. Weapon fire now uses the recovered multi-sound families (`TurboFire1/2`, `Laser1/2/3`, `Rocket1/2/3`) instead of one fixed shot sound per tier, and weapon upgrades now play the recovered shared select/change cue. Asteroid-style impacts randomize across the shipped impact pair. Garbage collisions now use the recovered hazard-to-player knockback sign, the native invincible motion guard, and a decaying track-mode lateral `velocity.x` lane, emit drifting smoke aftermath from the hazard position instead of from Turbo, and leave the garbage slot in a live burst state instead of deleting it immediately. Salt contact now uses the recovered `+0.15` damage delta; SubLazer projectiles own the separate `+0.02` Wall2-fired lane. `=` rows now use per-row fire state with visible `BLASTERTOP-FIRE` flashes instead of a single global turret metronome. Tutorial gameplay also now has a recovered-style ambient slug voice cooldown manager so nearby slugs can bark once on approach instead of only speaking on hit/death.
  - replace when: the original runtime actor ownership, animation/state switching, exact sound routing/mixing, and the remaining non-placeholder combat VFX are ported for these hazards/pickups, turret families, and Turbo attachments
- tutorial side barriers: `partial`
  - evidence: tutorial `Message=` text about blue barriers, shipped `OBJECTS/BARRIER/_OBJECT.TXT` and `OBJECTS/BARRIER/BARRIER.TGA`, Windows `initialize_game_assets_and_world` loading `Objects/Barrier`, and Android/iOS `cRBarrier::AI()` setting a fixed `y = 0.4` plus an owner-driven forward anchor
  - implementation: [`main.zig`](../../zig/src/main.zig), [`object.zig`](../../zig/src/object.zig)
  - note: the live tutorial viewport now uses the shipped barrier mesh instead of invented side quads, with a blue-biased additive treatment that reads much closer to the original thin side rails. Placement now stays world-aligned with a fixed `y = 0.4` and an owner-like forward anchor taken from live gameplay `z`, which is closer to the original `cRBarrier::AI()` behavior than the older centered row-frame approximation. Exact owner semantics, object render mode `7`, and any non-tutorial uses of `cRBarrier` are still unresolved.
  - replace when: the original barrier owner/update semantics are ported and the placement no longer relies on the current tutorial-only approximation
- damage-gauge hazard and recovery deltas: `partial`
  - evidence: Windows `update_damage_gauge`, `apply_damage_gauge_delta`, and `handle_subgoldy_collisions`, plus matching Android `cRSubGoldy::Collision()` and `cRDamageGuage::Take(float,bool)` call shapes
  - implementation: [`gameplay.zig`](../../zig/src/gameplay.zig), [`main.zig`](../../zig/src/main.zig)
  - note: Windows now splits contact damage from jetpack countdown. The current port's `damage_gauge` aligns with the collision-side controller at player `+0x3c4`, not the separate jetpack gauge at `+0x2750`. It now treats the first slug hit as the scripted death-cutscene handoff and applies the recovered `+1.0` delta only on repeated slug contact, which is closer to `handle_subgoldy_collisions`. Slug first-hit also stamps the native `(0, 0.8, -0.8)` velocity triplet (`velocity.x = 0`, `velocity.y = track_center_x * 0.2`, `velocity.z = track_center_x * -0.2`) before the fall handoff, and repeat-hit writes `velocity.z = track_center_x^2 * 0.004 * -8` per the `handle_subgoldy_collisions` repeat-branch. Hit-flash clip dispatch now routes through the voice-gated ouch-fallback branch: `dispatch_cutscene_animation(.damaged, true)` + queued `.base` fires only when the `damage_entry` voice returns zero, matching the native branch at `apply_damage_gauge_delta:25-28`. The gameplay HUD now uses the shipped `DamageGuage`, `DamageGuageFull`, `DamageGuageBright`, and `Warning` sprites plus the recovered once-per-second warning loop cue instead of the older rounded-rect placeholder, while still keeping the postal-only 9-slot life strip that Windows toggles through `show_subgoldy_lives`. Floor-gap losses now reuse the shared airborne fall controller without the hazard cutscene override. Salt contact is now `+0.15f / dist < 0.98` matching `cRSalt` (the previous `+0.02 / 0.49` conflated salt with the SubLazer projectile pool). The smaller `+0.02` damage source is still supplied by the SubLazer projectile pool (`cRSubLazerManager` @ `game + 0x356b00`, 20 slots stride 0xb0, fired by Wall2 AI via `shoot_subgoldy`), which the Zig port does not yet own. The original hazard-pool suppression flags, the exact warning owner/controller object, and attachment-carried void rows are still not ported.
  - replace when: the SubLazer projectile pool update semantics (historically misnamed "Wall2 ambient pool" in these docs — the Wall2 tile is the *emitter*; the slots are projectiles), the `bool`-gated suppression semantics, the exact warning owner/controller behavior, attachment-carried void rows, and any remaining gauge presentation gaps are ported
- score-threshold bonus life awards: `partial`
  - evidence: Windows `add_subgoldy_score`, `show_subgoldy_lives`, `initialize_subgoldy_death`, and `update_subgoldy_resurrect`, plus matching Android `cRSubGoldy::ScoreAdd(int,int)`, `cRSubGoldy::ShowLives()`, `cRSubGoldy::DeathInit()`, and `cRSubGoldy::RessurectAI()`
  - implementation: [`gameplay.zig`](../../zig/src/gameplay.zig), [`main.zig`](../../zig/src/main.zig)
  - note: Windows now confirms the visible life stock is seeded to `3` during `populate_runtime_track_cells_from_segments`, and the current port mirrors that seed plus the `50,000`-point bonus-life path capped at `9`. Postal respawns now also decrement one visible life before the explicit respawn handoff, while tutorial respawns stay non-consuming and challenge or time-trial failures still bypass the life display.
  - replace when: the full death or resurrection flow and the remaining mode-specific life behavior are ported instead of only the seed, threshold awards, and the current simplified respawn decrement
- jetpack pickup and authored-off state: `partial`
  - evidence: shipped jetpack pickup runtime tile `0x19`, authored `JetPack=Off` segment annotations, and Android `cRSubGoldy::JetPackCollect()` plus the collision branch that enters `cRSubHover::On()`
  - implementation: [`gameplay.zig`](../../zig/src/gameplay.zig), [`main.zig`](../../zig/src/main.zig)
  - note: Windows now clearly shows a separate jetpack gauge or auto-shutoff controller at player `+0x2750` through `initialize_jetpack_gauge` and `update_jetpack_gauge`, plus a separate live runtime pickup slot in `handle_subgoldy_collisions`. The current port now mirrors the `1/600` countdown, the `0.94` near-empty warning band, the route-end warning-math skip (within 5 rows of the course end, native only skips the warning-band math for that tick — state stays 1, no disarm), the `flags_b & 0x80` forced-warning snap, runtime-slot pickup collection instead of the older row-crossing shortcut, the recovered ramp-side pickup `x` bias from `spawn_track_jetpack_pickup`, and the native `set_snail_jetpack` thrust presentation cutoff: the `JETPACKTHRUST` family now appears only for the active pre-warning slice and plays its hide leg when the gauge crosses the `0.94` shutoff edge. The same controller also suppresses the new conservative floor-gap death proxy while active. The remaining gaps are narrower: the one-shot `set_snail_jetpack(0)` + `uninit_jet_particles` side effect on the 0.94 crossing (native 0x43a441, currently a TODO marker in Zig), the native state-2 hover controller (`end_jetpack_hover` @ 0x43a370) that is entirely unmodeled, the exact wobble/lift/fall interaction, the dedicated `cRSubHover::Jets` nozzle-particle owner, and the rest of `cRSubHover`.
  - replace when: the original hover movement, full fall interaction, the dedicated jet-particle/nozzle owner, and the remaining `cRSubHover` behavior are ported instead of only the runtime-slot pickup plus timer/warning/thrust slice
- debug asset browser and smoke path: `debug`
  - implementation: [`main.zig`](../../zig/src/main.zig)

Deferred plan:

- cross-port asset and content sweep
  - Windows remains the canonical behavior target
  - later, compare Android, iOS, and Wii assets against the Windows archive for higher-fidelity replacements, then evaluate whether any port-exclusive levels or content can be backported cleanly
  - defer until the core Windows gameplay and front-end loop is much closer to done, so content curation does not get mixed into behavior-port work
