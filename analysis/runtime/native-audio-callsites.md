# Native audio callsites

Whole-game native inventory for `play_sound_effect` and `play_voice_manager`, captured from Binary Ninja xrefs on 2026-03-19 against `SnailMail_unwrapped.exe.bndb`.

- `play_sound_effect` xrefs: `31`
- `play_voice_manager` xrefs: `17`
- total tracked audio callsites: `48`

Notes:

- Call addresses are the exact native xref sites from Binary Ninja.
- Caller links point at the checked-in IDA decompile export for the owning function.
- `update_click_start` needed the BN decompile for context because the current IDA export stub is empty.
- Voice rows preserve the native `mode` argument because several systems reuse the same voice id under different cooldown rules.

## `play_sound_effect`

| Call addr | Caller | Audio | Native gate / context | System guess | Port note |
| --- | --- | --- | --- | --- | --- |
| `0x402c25` | [`update_frontend_widget_interaction`](../../artifacts/ida/functions/00402820-update_frontend_widget_interaction.c) | `sfx 9` | plays when a widget first enters the hover or active selection state and the `0x40000` suppress bit is clear | frontend widget hover | frontend UI audio is present; low-priority parity |
| `0x402c90` | [`update_frontend_widget_interaction`](../../artifacts/ida/functions/00402820-update_frontend_widget_interaction.c) | `sfx 8` | activation or confirm branch for one widget-flag path | frontend widget activate | frontend UI audio is present; low-priority parity |
| `0x402cf2` | [`update_frontend_widget_interaction`](../../artifacts/ida/functions/00402820-update_frontend_widget_interaction.c) | `sfx 8` | second activation or confirm branch in the same widget interaction helper | frontend widget activate | frontend UI audio is present; low-priority parity |
| `0x404e6f` | [`update_row_event_display`](../../artifacts/ida/functions/00404cf0-update_row_event_display.c) | `sfx 49` | state `3` payout branch when the row-event controller awards the type-`5` completion bonus | row-event / parcel bonus | row-event controller exists, but this exact bonus SFX is not wired yet |
| `0x404ed2` | [`update_row_event_display`](../../artifacts/ida/functions/00404cf0-update_row_event_display.c) | `sfx 8` | accept or continue branch when the row-event controller sees flag `0x4000` and advances to state `5` | row-event / parcel UI | partial; controller exists, exact native UI cue is not matched |
| `0x405070` | [`register_parcel_delivery`](../../artifacts/ida/functions/00405040-register_parcel_delivery.c) | `sfx 45` | one-shot per parcel delivery registration | parcel delivery | missing; parcel delivery runtime is ported, but native delivery SFX ownership is still absent |
| `0x4050a4` | [`register_parcel_delivery`](../../artifacts/ida/functions/00405040-register_parcel_delivery.c) | `sfx 49` | final bonus path when delivered parcels reach the target and the bonus flag is enabled | parcel completion bonus | missing; final-delivery bonus logic exists, exact SFX does not |
| `0x409ae4` | [`update_galaxy`](../../artifacts/ida/functions/004092f0-update_galaxy.c) | `sfx 8` | route selection or confirmation branch inside galaxy navigation | frontend galaxy UI | frontend route UI exists; low-priority parity |
| `0x41b05c` | [`update_options`](../../artifacts/ida/functions/0041af60-update_options.c) | `sfx 8` | options screen plays a confirm cue when the sound-volume scalar changes | frontend options UI | likely approximate; not a gameplay target |
| `0x434129` | [`update_thanks_for_playing_screen`](../../artifacts/ida/functions/004340f0-update_thanks_for_playing_screen.c) | `sfx 8` | continue or exit confirmation after the thanks screen reaches phase `>= 2` | frontend thanks screen | likely approximate; not a gameplay target |
| `0x43b114` | [`play_movement_state_sound`](../../artifacts/ida/functions/0043afd0-play_movement_state_sound.c) | `sfx 17..24` | chooses random `17..18`, `19..21`, or `22..24` from `movement_flags`; attachment-exit path applies distance attenuation instead of direct play | movement-state audio controller | missing; the port has fire and pickup cues but no faithful movement-state sound owner |
| `0x43c3f8` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `sfx 41` | tile-type `22` trampoline contact branch when the airborne lane settles back onto the spring surface | runner trampoline landing | partial; the port exposes trampoline rows but still flattens the native bounce controller |
| `0x43c4c1` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `sfx 47` | first-entry cue for runtime tile `14` (`'|'`) before the branch's hold-and-fall timer runs | runner wall or barrier contact | partial; the port can now fire the one-shot entry cue, but still does not model the native hold-and-drop controller |
| `0x43c79d` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `sfx 0` | first completion-handoff arm when the route-end controller activates and clears the hover-end lane | completion-handoff arm | missing; the port now splits early vs late completion, but this exact arm cue is not present |
| `0x440374` | [`add_subgoldy_score`](../../artifacts/ida/functions/004402c0-add_subgoldy_score.c) | `sfx 44` | plays once when visible lives cross a `50_000` score bucket and gameplay is active | score / extra-life milestone | missing; the score controller exists, but this extra-life cue does not |
| `0x4423ed` | [`update_click_start`](../../artifacts/ida/functions/00442290-update_click_start.c) | `sfx 8` | click-start state `2 -> 3` transition, after centering both authored mouse pointers | tutorial startup handoff | partial; tutorial click-start flow is ported, exact native confirm cue is not pinned |
| `0x44500a` | [`handle_subgoldy_collisions`](../../artifacts/ida/functions/00444cf0-handle_subgoldy_collisions.c) | `sfx 39/40` | random garbage-hit impact family after score add and `+0.04` damage-gauge delta | garbage collision | partial; the port uses the shipped asteroid-impact pair but does not document this exact native owner yet |
| `0x4452fb` | [`handle_subgoldy_collisions`](../../artifacts/ida/functions/00444cf0-handle_subgoldy_collisions.c) | `sfx 27` | parcel pickup, immediately after the parcel-pickup voice and before the slot enters state `4` | parcel pickup | missing; current parcel pickup path is still silent compared with native |
| `0x44542f` | [`handle_subgoldy_collisions`](../../artifacts/ida/functions/00444cf0-handle_subgoldy_collisions.c) | `sfx 14` | health pickup inside the `delta_z < 1.0`, normalized-distance `< 0.98` branch | health pickup | ported; current gameplay health pickup cue aligns with this family |
| `0x4456f2` | [`handle_subgoldy_collisions`](../../artifacts/ida/functions/00444cf0-handle_subgoldy_collisions.c) | `sfx 43` | ring types `3` or `7`, the negative-velocity ring family | powerup / ring collision | partial; some ring audio exists, but exact native family mapping is still compressed |
| `0x445763` | [`handle_subgoldy_collisions`](../../artifacts/ida/functions/00444cf0-handle_subgoldy_collisions.c) | `sfx 1..7` | tiered sound family after ring types `4` or `5` update the player weapon tier | weapon-upgrade pickup | partial; the port has a generic weapon-change cue, not the native per-tier pickup family |
| `0x44578d` | [`handle_subgoldy_collisions`](../../artifacts/ida/functions/00444cf0-handle_subgoldy_collisions.c) | `sfx 1` | ring type `1` collect branch | powerup / ring pickup | partial; current ring cues are still collapsed |
| `0x44580d` | [`handle_subgoldy_collisions`](../../artifacts/ida/functions/00444cf0-handle_subgoldy_collisions.c) | `sfx 42` | explosive ring types `2` or `6`, immediately before `initialize_nuke` | explode-ring pickup | ported in spirit; exact native owner and timing should still be checked |
| `0x4458b4` | [`set_snail_jetpack`](../../artifacts/ida/functions/00445860-set_snail_jetpack.c) | `sfx 26` | leaving visual jetpack state `4` before swapping back to the normal weapon animation | jetpack shutoff | missing; current port mostly treats jetpack audio as a pickup cue, not a state-transition pair |
| `0x4458ec` | [`set_snail_jetpack`](../../artifacts/ida/functions/00445860-set_snail_jetpack.c) | `sfx 16` | entering visual jetpack state `4` | jetpack activation | missing or approximate; exact native transition cue is not wired |
| `0x445b62` | [`set_snail_weapon`](../../artifacts/ida/functions/00445920-set_snail_weapon.c) | `sfx 25` | shared weapon-change cue when the first two visual-state channels changed but the third did not | weapon visual-state controller | ported approximately through the generic weapon-change sound |
| `0x445b9f` | [`set_snail_weapon`](../../artifacts/ida/functions/00445920-set_snail_weapon.c) | `sfx 25` | third visual-state channel enters state `1` | weapon visual-state controller | ported approximately through the generic weapon-change sound |
| `0x445bca` | [`set_snail_weapon`](../../artifacts/ida/functions/00445920-set_snail_weapon.c) | `sfx 25` | third visual-state channel enters state `3` | weapon visual-state controller | ported approximately through the generic weapon-change sound |
| `0x445be2` | [`set_snail_weapon`](../../artifacts/ida/functions/00445920-set_snail_weapon.c) | `sfx 25` | fallback weapon visual-state change branch after channel `2` assignment | weapon visual-state controller | ported approximately through the generic weapon-change sound |
| `0x446c0a` | [`update_cutscene`](../../artifacts/ida/functions/004466d0-update_cutscene.c) | `sfx 46` | completion cutscene state `6` when it initializes the completion screen | completion cutscene | missing; the port has the early completion-screen handoff but not this native cue |
| `0x447000` | [`update_warning`](../../artifacts/ida/functions/00446f80-update_warning.c) | `sfx 50` | warning controller loops from fade-out state `2` back to state `1` once per cycle | damage-warning overlay | ported approximately; current port plays the postal warning loop once per second while active |

## `play_voice_manager`

| Call addr | Caller | Audio | Native gate / context | System guess | Port note |
| --- | --- | --- | --- | --- | --- |
| `0x420d30` | [`update_track_attachment_follow_state`](../../artifacts/ida/functions/00420cb0-update_track_attachment_follow_state.c) | `voice 4` `mode 1` | one-shot when the live sample index reaches `2 * sample_count` | attachment-follow milestone | missing; the port has no faithful voice-manager hook for attachment milestones |
| `0x421046` | [`update_track_attachment_follow_state`](../../artifacts/ida/functions/00420cb0-update_track_attachment_follow_state.c) | `voice 15` `mode 0` | kind-`31` follow completion path, immediately after the tail-launch setup | attachment-follow special exit | missing; kind-specific follow completion semantics are still incomplete |
| `0x43b84d` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `voice 13` `mode 2` `payload=row message id` | row-event or tutorial message activation when the current row carries voice payload data | row-event / tutorial speech | partial; the port plays authored `Sample=` clips, but not through the native voice-manager ownership or gating |
| `0x43b8b7` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `voice 12` `mode 0` | begins track attachment follow when the template kind is `24` | attachment-follow entry | missing; no native voice-manager dispatch on WORM entry yet |
| `0x43c874` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `voice 8` `mode 2` | completion-handoff controller once its timer passes `2.0` seconds | completion-handoff controller | missing; the port has the timer split but not the native voice event |
| `0x43cebd` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `voice 3` `mode 0` | attachment-exit gate A, once `attachment_exit_progress > 0.7` | attachment-exit controller | missing; ported gates do not yet own the native voice family |
| `0x43cf19` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `voice 1` `mode 2` | attachment-exit gate B once the player falls below `world_y < -7.0` | attachment-exit controller | missing; gate exists, native voice ownership does not |
| `0x43cf69` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `voice 7` `mode 2` | startup one-shot after the `initialize_subgoldy` timer seeded at `1/18` advances past `1.0` | runner start-of-run voice | missing until the native startup voice timer is ported |
| `0x43cff3` | [`update_subgoldy`](../../artifacts/ida/functions/0043b120-update_subgoldy.c) | `voice 6` `mode 1` | one-shot when the low-speed timer at `player + 0x435c` exceeds `1.0` while movement stays in the narrow recovered slow band | runner slowdown commentary | unresolved; current port still does not model this native low-speed timer literally |
| `0x43fbd5` | [`update_slug_hazard_ai`](../../artifacts/ida/functions/0043f930-update_slug_hazard_ai.c) | `voice 2` `mode 1` | slug engagement branch when the player is within `16` rows and the local state byte at `+196` is `1` | slug hazard AI | partial; the port has ambient slug voice cooldowns, but this exact owner and gate were not traced before |
| `0x44115e` | [`update_damage_gauge`](../../artifacts/ida/functions/00440fd0-update_damage_gauge.c) | `voice 14` `mode 0` | damage gauge state `1 -> 2` after the fill timer reaches `1.0` and the track-center lane is still `0.49` | damage-warning escalation | missing; current port only mirrors the looping warning SFX, not this voice gate |
| `0x44148e` | [`apply_damage_gauge_delta`](../../artifacts/ida/functions/004413f0-apply_damage_gauge_delta.c) | `voice 0` `mode 1` | first positive damage-gauge delta on an empty controller, before the fallback branch | damage entry voice | missing; current damage audio is still simplified |
| `0x4414a2` | [`apply_damage_gauge_delta`](../../artifacts/ida/functions/004413f0-apply_damage_gauge_delta.c) | `voice 9` `mode 0` | fallback voice when `voice 0` is rejected by the manager cooldown | damage entry voice fallback | missing; current damage audio is still simplified |
| `0x4452ef` | [`handle_subgoldy_collisions`](../../artifacts/ida/functions/00444cf0-handle_subgoldy_collisions.c) | `voice 10` `mode 1` | parcel pickup, immediately before `sfx 27` and before the parcel runtime moves to state `4` | parcel pickup voice | missing; current parcel pickup path does not route through a native-like voice family |
| `0x4457d4` | [`handle_subgoldy_collisions`](../../artifacts/ida/functions/00444cf0-handle_subgoldy_collisions.c) | `voice 5` `mode 1` | weapon-upgrade ring types `4` or `5`, only when the local weapon tier actually increases | powerup / weapon upgrade voice | missing or compressed; current port only plays a generic weapon-change SFX |
| `0x44697c` | [`update_cutscene`](../../artifacts/ida/functions/004466d0-update_cutscene.c) | `voice 3` `mode 2` | death cutscene state `10` entry, before the camera blend and weapon release | death cutscene entry | missing; the cutscene path exists, but native death voice ownership does not |
| `0x446b25` | [`update_cutscene`](../../artifacts/ida/functions/004466d0-update_cutscene.c) | `voice 11` `mode 2` | death cutscene state `12` if `initialize_subgoldy_death` did not already latch the voice gate | death cutscene fallback | missing; native death cutscene voice gating is not ported |
