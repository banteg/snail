// Authored texture-record owner retained by the Android and iOS RTexture
// objects. TextureRef remains a compatibility alias for analyzer replays and
// older Windows matching notes.
#ifndef TEXTURE_FWD_H
#define TEXTURE_FWD_H

struct cRTexture;
typedef cRTexture TextureRef;

#endif
