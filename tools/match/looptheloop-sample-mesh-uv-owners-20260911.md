# Looptheloop sample, mesh and UV recovery

`initialize_looptheloop_path_template_pair` improves from **78.55655795% to
94.44444444%** on the unchanged **2,640-byte** native span, gaining **419.4402
fuzzy-weighted bytes**. Its normalized prefix grows **44 to 159 instructions**;
all **49 references** remain clean and the native **0x54** stack allocation is
preserved. The retained body has **719/721 instructions**, versus **720/721**
before. It remains partial and receives no exact-function or encoded-body credit.

## Source and native evidence

The active shared-skeleton variant is materialized as an ordinary method in
this scratch. A fresh raw COFF function comparison proves that materialization
alone is byte-identical to the previous source. No shared header, compiler flag,
reference mapping, target range, normalizer or matcher rule changes.

The retained approach loop gives `z * 0.14285715f` a named progress value before
multiplication by the wiggle. This restores the native multiplication order.
The departure loop owns an independent sample index, shares its Z value between
lanes and advances its logical counter after storing secondary Z. The delta
pass initializes its index before the positive-count guard, as in the native
preheader. The circular sample algorithm and derived final-sample endpoint are
unchanged.

The mesh uses direct primary sample arrays, a double lateral expression with
ordinary vector scaling, and vector position addition. Its terminal endpoint
is the previous position plus `Vector3(0, 0, 1)`. That expression restores the
native terminal arithmetic, the Y-lane word copy, and the physical sample
cursor save/reload generated by the compiler. Both mesh branches preserve their
vertex indices and assignments.

The face pass initializes its winding index before computing U coordinates and
separates topology/texture dispatch from UV dispatch. The same unchanged local
index selects both parts. All fields, winding orders, and both checkerboard
texture-call paths remain present. These coupled changes recover the native
face setup and UV layout; the two checkerboard branch directions still differ.

## Encoded prefix and remaining body

A separate positional, relocation-audited proof establishes equality of the
first **159 instructions / 642 bytes**: native **0x41b0f0..0x41b372** and
candidate **[0, 642)**, with exclusive upper bounds. All **15** references in
this prefix resolve at the same instruction positions. Each instruction's raw
bytes, relocation fields and audited bytes are retained. No differing prefix
instructions remain.

The first mismatch, at offset **0x282**, is a local branch to the later curve
exit; the destination shifts because the following body is four bytes shorter.
The first different computation/address setup is the primary curve Identity
call preparation at target instruction **211**. The curve also differs in
secondary Identity and basis-right address preparation and scheduling. Mesh
vertex/width stack slots **0x20 and 0x6c** are exchanged. The face tail retains
two opposite checkerboard branch conditions and shifted branch destinations.
These are open source questions, not an established compiler limitation.

The whole native comparison covers **[0, 2630)**, with **[2630, 2640)** excluded
as recognized terminal padding and no unexplained target ranges. The full-body
reference audit is the matcher's partial alignment diagnostic; only the
separately asserted prefix uses same-index encoded comparison. Neither an
encoded-exact suffix nor a whole-body match is claimed.

## Controlled source experiments

The forward record contains **104 observations / 96 distinct full sources**;
eight duplicate-source labels are mapped explicitly. It includes all 31
nonempty combinations of five approach/departure/mesh/lateral/delta sites, all
15 combinations of four mesh operator/type sites, and all seven combinations of
three UV/index sites. Other controls cover materialization, sample indices,
orientation owners, counter placement, endpoint expressions and mesh scope.
The fixed source of each group is retained; these finite controls do not rule
out other source forms or interactions.

| Independently reversed on the retained source | Match | Instructions | Prefix |
|---|---:|---:|---:|
| Approach progress expression | 95.21165857% | 720 | 44 |
| Departure index and counter placement | 93.46314325% | 717 | 118 |
| Delta index inside guard | 94.30555556% | 719 | 159 |
| Original mesh owner block | 87.85565579% | 720 | 159 |
| Float lateral | 91.06029106% | 722 | 159 |
| Endpoint constructor | 92.75766017% | 715 | 159 |
| Joined topology and UV dispatch | 87.83877693% | 718 | 159 |
| Late face-index initialization | 82.41834607% | 718 | 159 |

The higher **95.21%** approach reversal is retained as a diagnostic in the
receipt and reverse recipe. It restores the old, different multiplication
order at instruction 44, losing the verified native approach region. Its better
aggregate score and instruction-count distance do not independently establish
more faithful source. The retained 94.44% candidate preserves that recovered
region while substantially improving the complete comparison.

The endpoint field-assignment alternative reaches 93.6111%, a copy/increment
90.0277%, and the retained vector addition 94.4444%. Final independent curve
index and physical cursor controls reach 90.7575% and 93.75%; merely naming the
Identity owners is neutral. Reversing the mesh branch order reaches 88.75%; a
named width local is neutral. Negating checkerboard tests on the preceding
92.76% source is score-neutral. These observations are limited to their recorded
baselines and do not establish byte equality of every neutral probe.

## Inspectable receipt

The [receipt](looptheloop-sample-mesh-uv-owners-20260911.json) includes full
baseline, materialized, retained and approach-reversal sources; complete native
and unlinked candidate bytes; separate addressed instruction arrays; all
reference audits and range/stack diagnostics; prefix proof; grouped forward and
reverse results; duplicate-source aliases; recipe identities; complete Windows
IDA/Binary Ninja and Android/iOS source witnesses; and proof scripts. The
canonical recipes preserve every distinct source for review and replay.

## Validation and progress

All **785** scratch builds/status checks pass. The strict experiment ledger has
**2,479 records**, zero malformed or current strict errors, and all **13** active
recipes runnable. A full forced public source/evidence refresh and a separate
saved-evidence validation pass. `git diff --check` passes.

Core fuzzy matching rises **91.80% to 91.94%**; the residual fuzzy gap falls
**19,162 to 18,743 bytes**. Exact core functions remain **602/662**. Public
normalized credit remains **714 functions / 165,216 bytes**, and encoded credit
remains **703 / 150,289 bytes**. Only Looptheloop's public ratio changes. There
are no exact gains or regressions, changed measurement identities, or changed
target spans. Linked-code credit remains zero; data and final-image identity
remain unmeasured.
