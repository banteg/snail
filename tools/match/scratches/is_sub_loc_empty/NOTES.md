# is_sub_loc_empty @ 0x439ab0

Exact `SubLoc` predicate matching Android `cRSubLoc::IsEmpty()`: both accept
tiles `0, 28, 35, 14, 29`. The authored owner name retires the former
`is_open_neighbor_tile_family` label, which described its use by track-neighbor
builders rather than its class method. The scratch remains exact at 15/15
instructions.
