{
  "schema": 1,
  "sites": [
    {
      "name": "free-slot-loop",
      "find": "    int slot_index = 0;\n    DWORD* game_words = (DWORD*)this;\n    SubHealth* scan = health_pickups;\n    while (1) {\n        if (scan->state == TRACK_PICKUP_STATE_INACTIVE)\n            break;\n        ++slot_index;\n        ++scan;\n        if (slot_index < 8)\n            continue;\n        return;\n    }",
      "replacements": [
        {
          "name": "condition-while",
          "text": "    int slot_index = 0;\n    DWORD* game_words = (DWORD*)this;\n    SubHealth* scan = health_pickups;\n    while (scan->state != TRACK_PICKUP_STATE_INACTIVE) {\n        ++slot_index;\n        ++scan;\n        if (slot_index >= 8)\n            return;\n    }"
        },
        {
          "name": "condition-while-equality-exit",
          "text": "    int slot_index = 0;\n    DWORD* game_words = (DWORD*)this;\n    SubHealth* scan = health_pickups;\n    while (scan->state != TRACK_PICKUP_STATE_INACTIVE) {\n        ++slot_index;\n        ++scan;\n        if (slot_index == 8)\n            return;\n    }"
        },
        {
          "name": "condition-for",
          "text": "    int slot_index = 0;\n    DWORD* game_words = (DWORD*)this;\n    SubHealth* scan = health_pickups;\n    for (; scan->state != TRACK_PICKUP_STATE_INACTIVE; ++scan) {\n        ++slot_index;\n        if (slot_index >= 8)\n            return;\n    }"
        },
        {
          "name": "bounded-condition-while",
          "text": "    int slot_index = 0;\n    DWORD* game_words = (DWORD*)this;\n    SubHealth* scan = health_pickups;\n    while (slot_index < 8\n        && scan->state != TRACK_PICKUP_STATE_INACTIVE) {\n        ++slot_index;\n        ++scan;\n    }\n    if (slot_index >= 8)\n        return;"
        },
        {
          "name": "bounded-condition-for",
          "text": "    int slot_index = 0;\n    DWORD* game_words = (DWORD*)this;\n    SubHealth* scan = health_pickups;\n    for (; slot_index < 8\n        && scan->state != TRACK_PICKUP_STATE_INACTIVE;\n         ++slot_index, ++scan) {\n    }\n    if (slot_index >= 8)\n        return;"
        },
        {
          "name": "guarded-do-while",
          "text": "    int slot_index = 0;\n    DWORD* game_words = (DWORD*)this;\n    SubHealth* scan = health_pickups;\n    if (scan->state != TRACK_PICKUP_STATE_INACTIVE) {\n        do {\n            ++slot_index;\n            ++scan;\n            if (slot_index >= 8)\n                return;\n        } while (scan->state != TRACK_PICKUP_STATE_INACTIVE);\n    }"
        }
      ]
    }
  ]
}
