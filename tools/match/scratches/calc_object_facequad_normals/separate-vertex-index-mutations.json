{
  "schema": 1,
  "sites": [
    {
      "name": "separate-vertex-index",
      "find": "    int vertex_offset = 0;\n    index = 0;\n    float* tally_cursor;\n    if (vertex_count > 0) {\n        tally_cursor = normal_tally;\n        do {\n            float& tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++index;\n        } while (index < vertex_count);\n    }",
      "replacements": [
        {
          "name": "inner-cursor-value",
          "text": "    int vertex_offset = 0;\n    int vertex_index = 0;\n    if (vertex_count > 0) {\n        float* tally_cursor = normal_tally;\n        do {\n            float tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++vertex_index;\n        } while (vertex_index < vertex_count);\n    }"
        },
        {
          "name": "outer-cursor-value",
          "text": "    int vertex_offset = 0;\n    int vertex_index = 0;\n    float* tally_cursor;\n    if (vertex_count > 0) {\n        tally_cursor = normal_tally;\n        do {\n            float tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++vertex_index;\n        } while (vertex_index < vertex_count);\n    }"
        },
        {
          "name": "cursor-before-index-value",
          "text": "    int vertex_offset = 0;\n    float* tally_cursor;\n    int vertex_index = 0;\n    if (vertex_count > 0) {\n        tally_cursor = normal_tally;\n        do {\n            float tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++vertex_index;\n        } while (vertex_index < vertex_count);\n    }"
        },
        {
          "name": "preinitialized-cursor-value",
          "text": "    int vertex_offset = 0;\n    int vertex_index = 0;\n    float* tally_cursor = normal_tally;\n    if (vertex_count > 0) {\n        do {\n            float tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++vertex_index;\n        } while (vertex_index < vertex_count);\n    }"
        },
        {
          "name": "inner-cursor-reference",
          "text": "    int vertex_offset = 0;\n    int vertex_index = 0;\n    if (vertex_count > 0) {\n        float* tally_cursor = normal_tally;\n        do {\n            float& tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++vertex_index;\n        } while (vertex_index < vertex_count);\n    }"
        }
      ]
    }
  ]
}
