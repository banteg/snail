{
  "schema": 1,
  "sites": [
    {
      "name": "final-tally-cursor",
      "find": "    int vertex_offset = 0;\n    index = 0;\n    if (vertex_count > 0) {\n        do {\n            float tally = normal_tally[index];\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++index;\n        } while (index < vertex_count);\n    }",
      "replacements": [
        {
          "name": "outer-cursor-assigned-after-gate",
          "text": "    int vertex_offset = 0;\n    index = 0;\n    float* tally_cursor;\n    if (vertex_count > 0) {\n        tally_cursor = normal_tally;\n        do {\n            float tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++index;\n        } while (index < vertex_count);\n    }"
        },
        {
          "name": "inner-cursor-initialized-after-gate",
          "text": "    int vertex_offset = 0;\n    index = 0;\n    if (vertex_count > 0) {\n        float* tally_cursor = normal_tally;\n        do {\n            float tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++index;\n        } while (index < vertex_count);\n    }"
        },
        {
          "name": "cursor-before-index-reset",
          "text": "    int vertex_offset = 0;\n    float* tally_cursor;\n    index = 0;\n    if (vertex_count > 0) {\n        tally_cursor = normal_tally;\n        do {\n            float tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++index;\n        } while (index < vertex_count);\n    }"
        },
        {
          "name": "cursor-reference-tally",
          "text": "    int vertex_offset = 0;\n    index = 0;\n    float* tally_cursor;\n    if (vertex_count > 0) {\n        tally_cursor = normal_tally;\n        do {\n            float& tally = *tally_cursor;\n            {\n                Vector3* normal = (Vector3*)((char*)vertex_normals + vertex_offset);\n                normal->x = normal->x / tally;\n                normal->y = normal->y / tally;\n                normal->z = normal->z / tally;\n            }\n            ((Vector3*)((char*)vertex_normals + vertex_offset))->Normalize();\n            ++tally_cursor;\n\n            Vector3* inverted = (Vector3*)((char*)vertex_normals + vertex_offset);\n            vertex_offset += sizeof(Vector3);\n            Vector3 inverted_value;\n            inverted_value.x = inverted->x * -1.0f;\n            inverted_value.y = inverted->y * -1.0f;\n            inverted_value.z = inverted->z * -1.0f;\n            *inverted = inverted_value;\n            ++index;\n        } while (index < vertex_count);\n    }"
        }
      ]
    }
  ]
}
