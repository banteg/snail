{
  "schema": 1,
  "sites": [
    {
      "name": "vector-expression-lifetime",
      "find": "    Vector3 rotated;\n    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta_source;\n            Vector3 delta;\n            delta_source.x = position.x - previous_position.x;\n            delta_source.y = position.y - previous_position.y;\n            delta_source.z = position.z - previous_position.z;\n            delta = delta_source;\n            rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta_source;\n        Vector3 delta;\n        delta_source.x = position.x - previous_position.x;\n        delta_source.y = position.y - previous_position.y;\n        delta_source.z = position.z - previous_position.z;\n        delta = delta_source;\n        rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n    }\n}\n",
      "replacements": [
        {
          "name": "delta0-scope0-angle1",
          "text": "    Vector3 rotated;\n    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta_source;\n            Vector3 delta;\n            delta_source.x = position.x - previous_position.x;\n            delta_source.y = position.y - previous_position.y;\n            delta_source.z = position.z - previous_position.z;\n            delta = delta_source;\n            rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta_source;\n        Vector3 delta;\n        delta_source.x = position.x - previous_position.x;\n        delta_source.y = position.y - previous_position.y;\n        delta_source.z = position.z - previous_position.z;\n        delta = delta_source;\n        rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n    }\n}\n"
        },
        {
          "name": "delta0-scope0-angle2",
          "text": "    Vector3 rotated;\n    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta_source;\n            Vector3 delta;\n            delta_source.x = position.x - previous_position.x;\n            delta_source.y = position.y - previous_position.y;\n            delta_source.z = position.z - previous_position.z;\n            delta = delta_source;\n            rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta_source;\n        Vector3 delta;\n        delta_source.x = position.x - previous_position.x;\n        delta_source.y = position.y - previous_position.y;\n        delta_source.z = position.z - previous_position.z;\n        delta = delta_source;\n        rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n    }\n}\n"
        },
        {
          "name": "delta0-scope1-angle0",
          "text": "    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta_source;\n            Vector3 delta;\n            delta_source.x = position.x - previous_position.x;\n            delta_source.y = position.y - previous_position.y;\n            delta_source.z = position.z - previous_position.z;\n            delta = delta_source;\n            Vector3 rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta_source;\n        Vector3 delta;\n        delta_source.x = position.x - previous_position.x;\n        delta_source.y = position.y - previous_position.y;\n        delta_source.z = position.z - previous_position.z;\n        delta = delta_source;\n        Vector3 rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n    }\n}\n"
        },
        {
          "name": "delta0-scope1-angle1",
          "text": "    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta_source;\n            Vector3 delta;\n            delta_source.x = position.x - previous_position.x;\n            delta_source.y = position.y - previous_position.y;\n            delta_source.z = position.z - previous_position.z;\n            delta = delta_source;\n            Vector3 rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta_source;\n        Vector3 delta;\n        delta_source.x = position.x - previous_position.x;\n        delta_source.y = position.y - previous_position.y;\n        delta_source.z = position.z - previous_position.z;\n        delta = delta_source;\n        Vector3 rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n    }\n}\n"
        },
        {
          "name": "delta0-scope1-angle2",
          "text": "    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta_source;\n            Vector3 delta;\n            delta_source.x = position.x - previous_position.x;\n            delta_source.y = position.y - previous_position.y;\n            delta_source.z = position.z - previous_position.z;\n            delta = delta_source;\n            Vector3 rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta_source;\n        Vector3 delta;\n        delta_source.x = position.x - previous_position.x;\n        delta_source.y = position.y - previous_position.y;\n        delta_source.z = position.z - previous_position.z;\n        delta = delta_source;\n        Vector3 rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n    }\n}\n"
        },
        {
          "name": "delta1-scope0-angle0",
          "text": "    Vector3 rotated;\n    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta;\n            delta = position - previous_position;\n            rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta;\n        delta = position - previous_position;\n        rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n    }\n}\n"
        },
        {
          "name": "delta1-scope0-angle1",
          "text": "    Vector3 rotated;\n    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta;\n            delta = position - previous_position;\n            rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta;\n        delta = position - previous_position;\n        rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n    }\n}\n"
        },
        {
          "name": "delta1-scope0-angle2",
          "text": "    Vector3 rotated;\n    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta;\n            delta = position - previous_position;\n            rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta;\n        delta = position - previous_position;\n        rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n    }\n}\n"
        },
        {
          "name": "delta1-scope1-angle0",
          "text": "    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta;\n            delta = position - previous_position;\n            Vector3 rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta;\n        delta = position - previous_position;\n        Vector3 rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n    }\n}\n"
        },
        {
          "name": "delta1-scope1-angle1",
          "text": "    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta;\n            delta = position - previous_position;\n            Vector3 rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta;\n        delta = position - previous_position;\n        Vector3 rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n    }\n}\n"
        },
        {
          "name": "delta1-scope1-angle2",
          "text": "    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 delta;\n            delta = position - previous_position;\n            Vector3 rotated = delta.Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 delta;\n        delta = position - previous_position;\n        Vector3 rotated = delta.Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n    }\n}\n"
        },
        {
          "name": "delta2-scope0-angle0",
          "text": "    Vector3 rotated;\n    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            rotated = (position - previous_position).Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        rotated = (position - previous_position).Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n    }\n}\n"
        },
        {
          "name": "delta2-scope0-angle1",
          "text": "    Vector3 rotated;\n    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            rotated = (position - previous_position).Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        rotated = (position - previous_position).Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n    }\n}\n"
        },
        {
          "name": "delta2-scope0-angle2",
          "text": "    Vector3 rotated;\n    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            rotated = (position - previous_position).Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        rotated = (position - previous_position).Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n    }\n}\n"
        },
        {
          "name": "delta2-scope1-angle0",
          "text": "    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 rotated = (position - previous_position).Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 rotated = (position - previous_position).Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 7.0685835f;\n    }\n}\n"
        },
        {
          "name": "delta2-scope1-angle1",
          "text": "    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 rotated = (position - previous_position).Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 rotated = (position - previous_position).Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + 0.7853982f + 6.2831855f;\n    }\n}\n"
        },
        {
          "name": "delta2-scope1-angle2",
          "text": "    if ((flags & SPRITE_FLAG_THROTTLE_FACING_REFRESH) != 0) {\n        if (facing_refresh_progress == 0.0f) {\n            Vector3 rotated = (position - previous_position).Rotate(*matrix);\n            float angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n            facing_refresh_progress = 0.001f;\n            facing_angle = angle;\n        }\n        facing_refresh_progress += facing_refresh_step;\n        if (facing_refresh_progress > 1.0f) {\n            facing_refresh_progress = 0.0f;\n        }\n    } else {\n        Vector3 rotated = (position - previous_position).Rotate(*matrix);\n        facing_angle = ATan(rotated.x, rotated.y) + (0.7853982f + 6.2831855f);\n    }\n}\n"
        }
      ]
    }
  ]
}
