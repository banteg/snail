# set_sprite_manager_paused

Small sprite-runtime mutator at 0x44e540. Writes the global sprite manager
pause byte and returns the byte argument.

Exact match: 100.00%, 3/3 instructions, no masked operands.
