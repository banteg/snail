{
  "schema": 1,
  "sites": [
    {
      "name": "mesh-sample-borrow",
      "find": "            PathTemplateSample* sample = &path->primary_samples[row];\n            Vector3* vertex = &vertices[column + row * (path->width_cells + 1)];\n            if (row != path->segment_count) {\n                Vector3 lateral_offset(\n                    lateral * sample->transform.basis_right.x,\n                    lateral * sample->transform.basis_right.y,\n                    lateral * sample->transform.basis_right.z);\n                Vector3 generated_position(\n                    sample->transform.position.x + lateral_offset.x,\n                    sample->transform.position.y + lateral_offset.y,\n                    sample->transform.position.z + lateral_offset.z);\n                *vertex = generated_position;\n            } else {\n                PathTemplateSample* previous = sample - 1;\n                Vector3 lateral_offset(\n                    lateral * previous->transform.basis_right.x,\n                    lateral * previous->transform.basis_right.y,\n                    lateral * previous->transform.basis_right.z);\n                Vector3 endpoint(\n                    previous->transform.position.x,\n                    previous->transform.position.y,\n                    previous->transform.position.z + 1.0f);\n                Vector3 generated_position(\n                    endpoint.x + lateral_offset.x,\n                    endpoint.y + lateral_offset.y,\n                    endpoint.z + lateral_offset.z);\n                *vertex = generated_position;\n            }",
      "replacements": [
        {
          "name": "direct-sample-arrays",
          "text": "            Vector3* vertex = &vertices[column + row * (path->width_cells + 1)];\n            if (row != path->segment_count) {\n                Vector3 lateral_offset(\n                    lateral * path->primary_samples[row].transform.basis_right.x,\n                    lateral * path->primary_samples[row].transform.basis_right.y,\n                    lateral * path->primary_samples[row].transform.basis_right.z);\n                Vector3 generated_position(\n                    path->primary_samples[row].transform.position.x + lateral_offset.x,\n                    path->primary_samples[row].transform.position.y + lateral_offset.y,\n                    path->primary_samples[row].transform.position.z + lateral_offset.z);\n                *vertex = generated_position;\n            } else {\n                Vector3 lateral_offset(\n                    lateral * path->primary_samples[row - 1].transform.basis_right.x,\n                    lateral * path->primary_samples[row - 1].transform.basis_right.y,\n                    lateral * path->primary_samples[row - 1].transform.basis_right.z);\n                Vector3 endpoint(\n                    path->primary_samples[row - 1].transform.position.x,\n                    path->primary_samples[row - 1].transform.position.y,\n                    path->primary_samples[row - 1].transform.position.z + 1.0f);\n                Vector3 generated_position(\n                    endpoint.x + lateral_offset.x,\n                    endpoint.y + lateral_offset.y,\n                    endpoint.z + lateral_offset.z);\n                *vertex = generated_position;\n            }"
        },
        {
          "name": "branch-local-pointers",
          "text": "            Vector3* vertex = &vertices[column + row * (path->width_cells + 1)];\n            if (row != path->segment_count) {\n                PathTemplateSample* sample = &path->primary_samples[row];\n                Vector3 lateral_offset(\n                    lateral * sample->transform.basis_right.x,\n                    lateral * sample->transform.basis_right.y,\n                    lateral * sample->transform.basis_right.z);\n                Vector3 generated_position(\n                    sample->transform.position.x + lateral_offset.x,\n                    sample->transform.position.y + lateral_offset.y,\n                    sample->transform.position.z + lateral_offset.z);\n                *vertex = generated_position;\n            } else {\n                PathTemplateSample* previous = &path->primary_samples[row - 1];\n                Vector3 lateral_offset(\n                    lateral * previous->transform.basis_right.x,\n                    lateral * previous->transform.basis_right.y,\n                    lateral * previous->transform.basis_right.z);\n                Vector3 endpoint(\n                    previous->transform.position.x,\n                    previous->transform.position.y,\n                    previous->transform.position.z + 1.0f);\n                Vector3 generated_position(\n                    endpoint.x + lateral_offset.x,\n                    endpoint.y + lateral_offset.y,\n                    endpoint.z + lateral_offset.z);\n                *vertex = generated_position;\n            }"
        },
        {
          "name": "branch-local-references",
          "text": "            Vector3* vertex = &vertices[column + row * (path->width_cells + 1)];\n            if (row != path->segment_count) {\n                PathTemplateSample& sample = path->primary_samples[row];\n                Vector3 lateral_offset(\n                    lateral * sample.transform.basis_right.x,\n                    lateral * sample.transform.basis_right.y,\n                    lateral * sample.transform.basis_right.z);\n                Vector3 generated_position(\n                    sample.transform.position.x + lateral_offset.x,\n                    sample.transform.position.y + lateral_offset.y,\n                    sample.transform.position.z + lateral_offset.z);\n                *vertex = generated_position;\n            } else {\n                PathTemplateSample& previous = path->primary_samples[row - 1];\n                Vector3 lateral_offset(\n                    lateral * previous.transform.basis_right.x,\n                    lateral * previous.transform.basis_right.y,\n                    lateral * previous.transform.basis_right.z);\n                Vector3 endpoint(\n                    previous.transform.position.x,\n                    previous.transform.position.y,\n                    previous.transform.position.z + 1.0f);\n                Vector3 generated_position(\n                    endpoint.x + lateral_offset.x,\n                    endpoint.y + lateral_offset.y,\n                    endpoint.z + lateral_offset.z);\n                *vertex = generated_position;\n            }"
        }
      ]
    }
  ]
}
