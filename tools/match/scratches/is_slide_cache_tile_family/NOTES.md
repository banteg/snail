# is_slide_cache_tile_family @ 0x439a40

Exact `SubLoc` predicate, despite the retained cache-oriented Windows harness
name. Android preserves the authored method as `cRSubLoc::IsFloor()` and tests
the identical tile set: `1, 20, 21, 33, 34, 27`. The scratch now defines the
helper on `SubLoc` and remains exact at 17/17 instructions.
