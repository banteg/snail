{
  "schema": 1,
  "sites": [
    {
      "name": "whole-grid-scan",
      "find": "// mark_track_warning_zones @ 0x4354f0 (thiscall, ret)\n// cRSubGame::DeSaltTrack() per Android symbols: stamps a 6-row x 2-col\n// warning footprint behind every hazard-bearing tile, suppressing both random\n// salt and garbage spawns in the approach corridor.\n\n#include \"subgame_runtime.h\"\n\nvoid cRSubGame::DeSaltTrack()\n{\n    int row = 0;\n    int saved_row = row;\n    if (runtime_row_count - 1 > 0) {\n        TrackRowCellTileByteView* row_cells = runtime_cell_tile_views();\n        do {\n            TrackRowCellTileByteView* cell = row_cells;\n            for (int col = 0; col < 8; ++col, ++cell) {\n                char t = cell->tile_id;\n                if (t == 2 || t == 3 || t == 4 || t == 5 || t == 6 || t == 7\n                    || t == 8 || t == 9 || t == 10 || t == 11 || t == 12\n                    || t == 13 || t == 14 || t == 23 || t == 25 || t == 33) {\n                    for (int back = 6; back; --back) {\n                        for (int dc = -1; dc < 1; ++dc) {\n                            if (row >= 0 && row < runtime_row_count - 1\n                                && dc + col >= 0 && dc + col < 8)\n                                runtime_cells[row][col + dc].lane_and_flags |=\n                                    SUBLOC_FLAG_RANDOM_HAZARD_BLOCKED;\n                        }\n                        --row;\n                    }\n                    row = saved_row;\n                }\n            }\n            row_cells = cell;\n            ++row;\n            saved_row = row;\n        } while (row < runtime_row_count - 1);\n    }\n}\n",
      "replacements": [
        {
          "name": "direct-grid-saved-outer-do",
          "text": "// mark_track_warning_zones @ 0x4354f0 (thiscall, ret)\n// cRSubGame::DeSaltTrack() per Android symbols: stamps a 6-row x 2-col\n// warning footprint behind every hazard-bearing tile, suppressing both random\n// salt and garbage spawns in the approach corridor.\n\n#include \"subgame_runtime.h\"\n\nvoid cRSubGame::DeSaltTrack()\n{\n    int row = 0;\n    int saved_row = row;\n    if (runtime_row_count - 1 > 0) {\n        do {\n            for (int col = 0; col < 8; ++col) {\n                char t = runtime_cells[row][col].tile_id;\n                if (t == 2 || t == 3 || t == 4 || t == 5 || t == 6 || t == 7\n                    || t == 8 || t == 9 || t == 10 || t == 11 || t == 12\n                    || t == 13 || t == 14 || t == 23 || t == 25 || t == 33) {\n                    for (int back = 6; back; --back) {\n                        for (int dc = -1; dc < 1; ++dc) {\n                            if (row >= 0 && row < runtime_row_count - 1\n                                && dc + col >= 0 && dc + col < 8)\n                                runtime_cells[row][col + dc].lane_and_flags |=\n                                    SUBLOC_FLAG_RANDOM_HAZARD_BLOCKED;\n                        }\n                        --row;\n                    }\n                    row = saved_row;\n                }\n            }\n            ++row;\n            saved_row = row;\n        } while (row < runtime_row_count - 1);\n    }\n}\n"
        },
        {
          "name": "direct-grid-saved-outer-for",
          "text": "// mark_track_warning_zones @ 0x4354f0 (thiscall, ret)\n// cRSubGame::DeSaltTrack() per Android symbols: stamps a 6-row x 2-col\n// warning footprint behind every hazard-bearing tile, suppressing both random\n// salt and garbage spawns in the approach corridor.\n\n#include \"subgame_runtime.h\"\n\nvoid cRSubGame::DeSaltTrack()\n{\n    int row = 0;\n    int saved_row = row;\n    for (; row < runtime_row_count - 1; ++row) {\n            for (int col = 0; col < 8; ++col) {\n                char t = runtime_cells[row][col].tile_id;\n                if (t == 2 || t == 3 || t == 4 || t == 5 || t == 6 || t == 7\n                    || t == 8 || t == 9 || t == 10 || t == 11 || t == 12\n                    || t == 13 || t == 14 || t == 23 || t == 25 || t == 33) {\n                    for (int back = 6; back; --back) {\n                        for (int dc = -1; dc < 1; ++dc) {\n                            if (row >= 0 && row < runtime_row_count - 1\n                                && dc + col >= 0 && dc + col < 8)\n                                runtime_cells[row][col + dc].lane_and_flags |=\n                                    SUBLOC_FLAG_RANDOM_HAZARD_BLOCKED;\n                        }\n                        --row;\n                    }\n                    row = saved_row;\n                }\n            }\n            saved_row = row;\n    }\n}\n"
        },
        {
          "name": "direct-grid-saved-row-do",
          "text": "// mark_track_warning_zones @ 0x4354f0 (thiscall, ret)\n// cRSubGame::DeSaltTrack() per Android symbols: stamps a 6-row x 2-col\n// warning footprint behind every hazard-bearing tile, suppressing both random\n// salt and garbage spawns in the approach corridor.\n\n#include \"subgame_runtime.h\"\n\nvoid cRSubGame::DeSaltTrack()\n{\n    int row = 0;\n    if (runtime_row_count - 1 > 0) {\n        do {\n            int saved_row = row;\n            for (int col = 0; col < 8; ++col) {\n                char t = runtime_cells[row][col].tile_id;\n                if (t == 2 || t == 3 || t == 4 || t == 5 || t == 6 || t == 7\n                    || t == 8 || t == 9 || t == 10 || t == 11 || t == 12\n                    || t == 13 || t == 14 || t == 23 || t == 25 || t == 33) {\n                    for (int back = 6; back; --back) {\n                        for (int dc = -1; dc < 1; ++dc) {\n                            if (row >= 0 && row < runtime_row_count - 1\n                                && dc + col >= 0 && dc + col < 8)\n                                runtime_cells[row][col + dc].lane_and_flags |=\n                                    SUBLOC_FLAG_RANDOM_HAZARD_BLOCKED;\n                        }\n                        --row;\n                    }\n                    row = saved_row;\n                }\n            }\n            ++row;\n        } while (row < runtime_row_count - 1);\n    }\n}\n"
        },
        {
          "name": "direct-grid-saved-row-for",
          "text": "// mark_track_warning_zones @ 0x4354f0 (thiscall, ret)\n// cRSubGame::DeSaltTrack() per Android symbols: stamps a 6-row x 2-col\n// warning footprint behind every hazard-bearing tile, suppressing both random\n// salt and garbage spawns in the approach corridor.\n\n#include \"subgame_runtime.h\"\n\nvoid cRSubGame::DeSaltTrack()\n{\n    int row = 0;\n    for (; row < runtime_row_count - 1; ++row) {\n            int saved_row = row;\n            for (int col = 0; col < 8; ++col) {\n                char t = runtime_cells[row][col].tile_id;\n                if (t == 2 || t == 3 || t == 4 || t == 5 || t == 6 || t == 7\n                    || t == 8 || t == 9 || t == 10 || t == 11 || t == 12\n                    || t == 13 || t == 14 || t == 23 || t == 25 || t == 33) {\n                    for (int back = 6; back; --back) {\n                        for (int dc = -1; dc < 1; ++dc) {\n                            if (row >= 0 && row < runtime_row_count - 1\n                                && dc + col >= 0 && dc + col < 8)\n                                runtime_cells[row][col + dc].lane_and_flags |=\n                                    SUBLOC_FLAG_RANDOM_HAZARD_BLOCKED;\n                        }\n                        --row;\n                    }\n                    row = saved_row;\n                }\n            }\n    }\n}\n"
        },
        {
          "name": "direct-grid-saved-hazard-do",
          "text": "// mark_track_warning_zones @ 0x4354f0 (thiscall, ret)\n// cRSubGame::DeSaltTrack() per Android symbols: stamps a 6-row x 2-col\n// warning footprint behind every hazard-bearing tile, suppressing both random\n// salt and garbage spawns in the approach corridor.\n\n#include \"subgame_runtime.h\"\n\nvoid cRSubGame::DeSaltTrack()\n{\n    int row = 0;\n    if (runtime_row_count - 1 > 0) {\n        do {\n            for (int col = 0; col < 8; ++col) {\n                char t = runtime_cells[row][col].tile_id;\n                if (t == 2 || t == 3 || t == 4 || t == 5 || t == 6 || t == 7\n                    || t == 8 || t == 9 || t == 10 || t == 11 || t == 12\n                    || t == 13 || t == 14 || t == 23 || t == 25 || t == 33) {\n                    int saved_row = row;\n                    for (int back = 6; back; --back) {\n                        for (int dc = -1; dc < 1; ++dc) {\n                            if (row >= 0 && row < runtime_row_count - 1\n                                && dc + col >= 0 && dc + col < 8)\n                                runtime_cells[row][col + dc].lane_and_flags |=\n                                    SUBLOC_FLAG_RANDOM_HAZARD_BLOCKED;\n                        }\n                        --row;\n                    }\n                    row = saved_row;\n                }\n            }\n            ++row;\n        } while (row < runtime_row_count - 1);\n    }\n}\n"
        },
        {
          "name": "direct-grid-saved-hazard-for",
          "text": "// mark_track_warning_zones @ 0x4354f0 (thiscall, ret)\n// cRSubGame::DeSaltTrack() per Android symbols: stamps a 6-row x 2-col\n// warning footprint behind every hazard-bearing tile, suppressing both random\n// salt and garbage spawns in the approach corridor.\n\n#include \"subgame_runtime.h\"\n\nvoid cRSubGame::DeSaltTrack()\n{\n    int row = 0;\n    for (; row < runtime_row_count - 1; ++row) {\n            for (int col = 0; col < 8; ++col) {\n                char t = runtime_cells[row][col].tile_id;\n                if (t == 2 || t == 3 || t == 4 || t == 5 || t == 6 || t == 7\n                    || t == 8 || t == 9 || t == 10 || t == 11 || t == 12\n                    || t == 13 || t == 14 || t == 23 || t == 25 || t == 33) {\n                    int saved_row = row;\n                    for (int back = 6; back; --back) {\n                        for (int dc = -1; dc < 1; ++dc) {\n                            if (row >= 0 && row < runtime_row_count - 1\n                                && dc + col >= 0 && dc + col < 8)\n                                runtime_cells[row][col + dc].lane_and_flags |=\n                                    SUBLOC_FLAG_RANDOM_HAZARD_BLOCKED;\n                        }\n                        --row;\n                    }\n                    row = saved_row;\n                }\n            }\n    }\n}\n"
        }
      ]
    }
  ]
}
