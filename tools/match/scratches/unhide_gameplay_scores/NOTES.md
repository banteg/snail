# unhide_gameplay_scores @ 0x445f40

Exact `cRSubGame::UnHideScores()` helper over the active `cRSubGame`.
Windows and Android both unhide the same two owned gameplay score widgets in
order. No iOS symbol was found, so the crosswalk records only the Android body
instead of inferring a second mobile mapping.
