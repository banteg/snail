{
  "schema": 1,
  "sites": [
    {
      "name": "terminal-sum-association",
      "find": "                *position =\n                    terminal[-1].transform.basis_forward * carry\n                    + *anchor\n                    + terminal[-1].transform.position;",
      "replacements": [
        {
          "name": "right-associated-base",
          "text": "                *position =\n                    terminal[-1].transform.basis_forward * carry\n                    + (*anchor + terminal[-1].transform.position);"
        },
        {
          "name": "base-first",
          "text": "                *position =\n                    (*anchor + terminal[-1].transform.position)\n                    + terminal[-1].transform.basis_forward * carry;"
        }
      ]
    },
    {
      "name": "ordinary-sample-lifetime",
      "find": "        AttachmentSample* sample = &current_template->secondary_samples[sample_index];\n        Vec3* anchor = &source_cell->position;\n        float base_x =\n            advanced * sample->delta_dir_to_next.x * lateral_scale\n            + sample->transform.position.x\n            + anchor->x;\n        float base_y =\n            advanced * sample->delta_dir_to_next.y * lateral_scale\n            + sample->transform.position.y\n            + anchor->y;\n        float base_z =\n            advanced * sample->delta_dir_to_next.z\n            + sample->transform.position.z\n            + anchor->z;",
      "replacements": [
        {
          "name": "bank-then-delayed-element",
          "text": "        AttachmentSample* ordinary_samples = current_template->secondary_samples;\n        Vec3* anchor = &source_cell->position;\n        float base_x =\n            advanced * ordinary_samples[sample_index].delta_dir_to_next.x\n                * lateral_scale\n            + ordinary_samples[sample_index].transform.position.x\n            + anchor->x;\n        float base_y =\n            advanced * ordinary_samples[sample_index].delta_dir_to_next.y\n                * lateral_scale\n            + ordinary_samples[sample_index].transform.position.y\n            + anchor->y;\n        float base_z =\n            advanced * ordinary_samples[sample_index].delta_dir_to_next.z\n            + ordinary_samples[sample_index].transform.position.z\n            + anchor->z;\n        AttachmentSample* sample = &ordinary_samples[sample_index];"
        },
        {
          "name": "bank-delta-first",
          "text": "        AttachmentSample* ordinary_samples = current_template->secondary_samples;\n        Vec3* anchor = &source_cell->position;\n        float base_x =\n            ordinary_samples[sample_index].delta_dir_to_next.x * advanced\n                * lateral_scale\n            + ordinary_samples[sample_index].transform.position.x\n            + anchor->x;\n        float base_y =\n            ordinary_samples[sample_index].delta_dir_to_next.y * advanced\n                * lateral_scale\n            + ordinary_samples[sample_index].transform.position.y\n            + anchor->y;\n        float base_z =\n            ordinary_samples[sample_index].delta_dir_to_next.z * advanced\n            + ordinary_samples[sample_index].transform.position.z\n            + anchor->z;\n        AttachmentSample* sample = &ordinary_samples[sample_index];"
        },
        {
          "name": "bank-lateral-first",
          "text": "        AttachmentSample* ordinary_samples = current_template->secondary_samples;\n        Vec3* anchor = &source_cell->position;\n        float base_x =\n            lateral_scale * ordinary_samples[sample_index].delta_dir_to_next.x\n                * advanced\n            + ordinary_samples[sample_index].transform.position.x\n            + anchor->x;\n        float base_y =\n            lateral_scale * ordinary_samples[sample_index].delta_dir_to_next.y\n                * advanced\n            + ordinary_samples[sample_index].transform.position.y\n            + anchor->y;\n        float base_z =\n            ordinary_samples[sample_index].delta_dir_to_next.z * advanced\n            + ordinary_samples[sample_index].transform.position.z\n            + anchor->z;\n        AttachmentSample* sample = &ordinary_samples[sample_index];"
        },
        {
          "name": "direct-bank-reload",
          "text": "        Vec3* anchor = &source_cell->position;\n        float base_x =\n            advanced\n                * current_template->secondary_samples[sample_index]\n                      .delta_dir_to_next.x\n                * lateral_scale\n            + current_template->secondary_samples[sample_index]\n                  .transform.position.x\n            + anchor->x;\n        float base_y =\n            advanced\n                * current_template->secondary_samples[sample_index]\n                      .delta_dir_to_next.y\n                * lateral_scale\n            + current_template->secondary_samples[sample_index]\n                  .transform.position.y\n            + anchor->y;\n        float base_z =\n            advanced\n                * current_template->secondary_samples[sample_index]\n                      .delta_dir_to_next.z\n            + current_template->secondary_samples[sample_index]\n                  .transform.position.z\n            + anchor->z;\n        AttachmentSample* sample =\n            &current_template->secondary_samples[sample_index];"
        }
      ]
    },
    {
      "name": "kind42-output-publication",
      "find": "        Vec3* output = &output_position;\n        output->x = transform.position.x;\n        output->y = transform.position.y;\n        output->z = z;",
      "replacements": [
        {
          "name": "direct-fields",
          "text": "        output_position.x = transform.position.x;\n        output_position.y = transform.position.y;\n        output_position.z = z;"
        },
        {
          "name": "constructed-vector",
          "text": "        output_position = Vec3(\n            transform.position.x,\n            transform.position.y,\n            z);"
        }
      ]
    }
  ]
}
