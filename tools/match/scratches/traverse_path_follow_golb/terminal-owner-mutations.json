{
  "schema": 1,
  "sites": [
    {
      "name": "terminal-branch-template-owner",
      "find": "                float old_x = position->x;\n                int count = terminal_template->segment_count;\n                float carry = delta + terminal_template->width_or_scale;\n                AttachmentSample* terminal =\n                    &terminal_template->secondary_samples[count];",
      "replacements": [
        {
          "name": "fresh-owner-after-old-x",
          "text": "                float old_x = position->x;\n                Path* launch_template = template_record;\n                int count = launch_template->segment_count;\n                float carry = delta + launch_template->width_or_scale;\n                AttachmentSample* terminal =\n                    &launch_template->secondary_samples[count];"
        },
        {
          "name": "fresh-owner-before-old-x",
          "text": "                Path* launch_template = template_record;\n                float old_x = position->x;\n                int count = launch_template->segment_count;\n                float carry = delta + launch_template->width_or_scale;\n                AttachmentSample* terminal =\n                    &launch_template->secondary_samples[count];"
        },
        {
          "name": "direct-state-owner",
          "text": "                float old_x = position->x;\n                int count = template_record->segment_count;\n                float carry = delta + template_record->width_or_scale;\n                AttachmentSample* terminal =\n                    &template_record->secondary_samples[count];"
        },
        {
          "name": "refresh-existing-owner",
          "text": "                float old_x = position->x;\n                terminal_template = template_record;\n                int count = terminal_template->segment_count;\n                float carry = delta + terminal_template->width_or_scale;\n                AttachmentSample* terminal =\n                    &terminal_template->secondary_samples[count];"
        }
      ]
    }
  ]
}
