{
  "schema": 1,
  "sites": [
    {
      "name": "interpolation-scalar-declarations",
      "find": "    float center_x;\n    if (sample_index == terminal_index) {\n        center_x = current_template->primary_samples[sample_index].center_x;\n    } else {\n        center_x =\n            advanced / current_template->secondary_samples[sample_index].delta_length\n                * (current_template->primary_samples[sample_index + 1].center_x\n                   - current_template->primary_samples[sample_index].center_x)\n            + current_template->primary_samples[sample_index].center_x;\n    }\n\n    float lateral_scale;\n    if (sample_index == terminal_index) {\n        lateral_scale = current_template->primary_samples[sample_index].lateral_scale;\n    } else {\n        lateral_scale =\n            advanced / current_template->secondary_samples[sample_index].delta_length\n                * (current_template->primary_samples[sample_index + 1].lateral_scale\n                   - current_template->primary_samples[sample_index].lateral_scale)\n            + current_template->primary_samples[sample_index].lateral_scale;\n    }\n\n    float special_scalar;\n    if (sample_index == terminal_index) {\n        special_scalar = current_template->primary_samples[sample_index].special_scalar;\n    } else {\n        special_scalar =\n            advanced / current_template->secondary_samples[sample_index].delta_length\n                * (current_template->primary_samples[sample_index + 1].special_scalar\n                   - current_template->primary_samples[sample_index].special_scalar)\n            + current_template->primary_samples[sample_index].special_scalar;\n    }",
      "replacements": [
        {
          "name": "special-center-lateral",
          "text": "    float special_scalar;\n    float center_x;\n    float lateral_scale;\n    if (sample_index == terminal_index) {\n        center_x = current_template->primary_samples[sample_index].center_x;\n    } else {\n        center_x =\n            advanced / current_template->secondary_samples[sample_index].delta_length\n                * (current_template->primary_samples[sample_index + 1].center_x\n                   - current_template->primary_samples[sample_index].center_x)\n            + current_template->primary_samples[sample_index].center_x;\n    }\n\n    if (sample_index == terminal_index) {\n        lateral_scale = current_template->primary_samples[sample_index].lateral_scale;\n    } else {\n        lateral_scale =\n            advanced / current_template->secondary_samples[sample_index].delta_length\n                * (current_template->primary_samples[sample_index + 1].lateral_scale\n                   - current_template->primary_samples[sample_index].lateral_scale)\n            + current_template->primary_samples[sample_index].lateral_scale;\n    }\n\n    if (sample_index == terminal_index) {\n        special_scalar = current_template->primary_samples[sample_index].special_scalar;\n    } else {\n        special_scalar =\n            advanced / current_template->secondary_samples[sample_index].delta_length\n                * (current_template->primary_samples[sample_index + 1].special_scalar\n                   - current_template->primary_samples[sample_index].special_scalar)\n            + current_template->primary_samples[sample_index].special_scalar;\n    }"
        },
        {
          "name": "special-lateral-center",
          "text": "    float special_scalar;\n    float lateral_scale;\n    float center_x;\n    if (sample_index == terminal_index) {\n        center_x = current_template->primary_samples[sample_index].center_x;\n    } else {\n        center_x =\n            advanced / current_template->secondary_samples[sample_index].delta_length\n                * (current_template->primary_samples[sample_index + 1].center_x\n                   - current_template->primary_samples[sample_index].center_x)\n            + current_template->primary_samples[sample_index].center_x;\n    }\n\n    if (sample_index == terminal_index) {\n        lateral_scale = current_template->primary_samples[sample_index].lateral_scale;\n    } else {\n        lateral_scale =\n            advanced / current_template->secondary_samples[sample_index].delta_length\n                * (current_template->primary_samples[sample_index + 1].lateral_scale\n                   - current_template->primary_samples[sample_index].lateral_scale)\n            + current_template->primary_samples[sample_index].lateral_scale;\n    }\n\n    if (sample_index == terminal_index) {\n        special_scalar = current_template->primary_samples[sample_index].special_scalar;\n    } else {\n        special_scalar =\n            advanced / current_template->secondary_samples[sample_index].delta_length\n                * (current_template->primary_samples[sample_index + 1].special_scalar\n                   - current_template->primary_samples[sample_index].special_scalar)\n            + current_template->primary_samples[sample_index].special_scalar;\n    }"
        }
      ]
    },
    {
      "name": "ordinary-result-slots",
      "find": "        Vec3 right_offset = transform.basis_right * local_x;\n        Vec3 base(base_x, base_y, base_z);\n        Vec3 result = right_offset + base;\n        *output = result;",
      "replacements": [
        {
          "name": "result-first-field-sums",
          "text": "        Vec3 result;\n        Vec3 right_offset = transform.basis_right * local_x;\n        result.x = right_offset.x + base_x;\n        result.y = right_offset.y + base_y;\n        result.z = right_offset.z + base_z;\n        *output = result;"
        },
        {
          "name": "result-first-vector-sum",
          "text": "        Vec3 result;\n        Vec3 right_offset = transform.basis_right * local_x;\n        Vec3 base(base_x, base_y, base_z);\n        result = right_offset + base;\n        *output = result;"
        },
        {
          "name": "base-then-product-add",
          "text": "        Vec3 result(base_x, base_y, base_z);\n        Vec3 right_offset = transform.basis_right * local_x;\n        result += right_offset;\n        *output = result;"
        },
        {
          "name": "product-then-result-constructor",
          "text": "        Vec3 right_offset = transform.basis_right * local_x;\n        Vec3 result(\n            right_offset.x + base_x,\n            right_offset.y + base_y,\n            right_offset.z + base_z);\n        *output = result;"
        },
        {
          "name": "direct-result-fields",
          "text": "        Vec3 result;\n        result.x = transform.basis_right.x * local_x + base_x;\n        result.y = transform.basis_right.y * local_x + base_y;\n        result.z = transform.basis_right.z * local_x + base_z;\n        *output = result;"
        },
        {
          "name": "base-first-vector-expression",
          "text": "        Vec3 base(base_x, base_y, base_z);\n        Vec3 result = base + transform.basis_right * local_x;\n        *output = result;"
        }
      ]
    }
  ]
}
