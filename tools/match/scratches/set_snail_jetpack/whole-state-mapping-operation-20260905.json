{
  "schema": 1,
  "sites": [
    {
      "name": "state-mapping-operation",
      "find": "// set_snail_jetpack @ 0x445860 (thiscall, ret 0x4)\n\n#include \"audio_system.h\"\n#include \"player.h\"\n\nvoid cRSnail::SetJetPack(int state)\n{\n    int target_state;\n\n    switch (state) {\n    case 0:\n        target_state = 0;\n        break;\n    case 1:\n        target_state = 4;\n        break;\n    default:\n        target_state = state;\n        break;\n    }\n\n    int selected_state = jetpack_channel.selected_state;\n    bool immediate = true;\n    if (selected_state != target_state) {\n        if (selected_state == 4) {\n            jetpack_channel.SetAnimation(\n                1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n            immediate = false;\n            g_sound_effect_manager.Play(26);\n        }\n\n        if (target_state != 0) {\n            if (target_state == 4) {\n                jetpack_channel.SetAnimation(1, immediate, target_state);\n                jetpack_channel.SetAnimation(\n                    0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n                g_sound_effect_manager.Play(16);\n            }\n        } else {\n            jetpack_channel.SetAnimation(\n                ANIM_MANAGER_HIDE_ANIMATION_ID,\n                0,\n                OBJECT_ANIMATION_MODE_UNCHANGED);\n        }\n\n        jetpack_channel.selected_state = target_state;\n    }\n}\n",
      "replacements": [
        {
          "name": "switch-returns",
          "text": "// set_snail_jetpack @ 0x445860 (thiscall, ret 0x4)\n\n#include \"audio_system.h\"\n#include \"player.h\"\n\nstatic inline int resolve_state(int state)\n{\n    switch (state) { case 0: return 0; case 1: return 4; default: return state; }\n}\n\nvoid cRSnail::SetJetPack(int state)\n{\n    int target_state = resolve_state(state);\n\n    int selected_state = jetpack_channel.selected_state;\n    bool immediate = true;\n    if (selected_state != target_state) {\n        if (selected_state == 4) {\n            jetpack_channel.SetAnimation(\n                1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n            immediate = false;\n            g_sound_effect_manager.Play(26);\n        }\n\n        if (target_state != 0) {\n            if (target_state == 4) {\n                jetpack_channel.SetAnimation(1, immediate, target_state);\n                jetpack_channel.SetAnimation(\n                    0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n                g_sound_effect_manager.Play(16);\n            }\n        } else {\n            jetpack_channel.SetAnimation(\n                ANIM_MANAGER_HIDE_ANIMATION_ID,\n                0,\n                OBJECT_ANIMATION_MODE_UNCHANGED);\n        }\n\n        jetpack_channel.selected_state = target_state;\n    }\n}\n"
        },
        {
          "name": "switch-fallback",
          "text": "// set_snail_jetpack @ 0x445860 (thiscall, ret 0x4)\n\n#include \"audio_system.h\"\n#include \"player.h\"\n\nstatic inline int resolve_state(int state)\n{\n    switch (state) { case 0: return 0; case 1: return 4; } return state;\n}\n\nvoid cRSnail::SetJetPack(int state)\n{\n    int target_state = resolve_state(state);\n\n    int selected_state = jetpack_channel.selected_state;\n    bool immediate = true;\n    if (selected_state != target_state) {\n        if (selected_state == 4) {\n            jetpack_channel.SetAnimation(\n                1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n            immediate = false;\n            g_sound_effect_manager.Play(26);\n        }\n\n        if (target_state != 0) {\n            if (target_state == 4) {\n                jetpack_channel.SetAnimation(1, immediate, target_state);\n                jetpack_channel.SetAnimation(\n                    0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n                g_sound_effect_manager.Play(16);\n            }\n        } else {\n            jetpack_channel.SetAnimation(\n                ANIM_MANAGER_HIDE_ANIMATION_ID,\n                0,\n                OBJECT_ANIMATION_MODE_UNCHANGED);\n        }\n\n        jetpack_channel.selected_state = target_state;\n    }\n}\n"
        },
        {
          "name": "if-returns",
          "text": "// set_snail_jetpack @ 0x445860 (thiscall, ret 0x4)\n\n#include \"audio_system.h\"\n#include \"player.h\"\n\nstatic inline int resolve_state(int state)\n{\n    if (state == 0) return 0; if (state == 1) return 4; return state;\n}\n\nvoid cRSnail::SetJetPack(int state)\n{\n    int target_state = resolve_state(state);\n\n    int selected_state = jetpack_channel.selected_state;\n    bool immediate = true;\n    if (selected_state != target_state) {\n        if (selected_state == 4) {\n            jetpack_channel.SetAnimation(\n                1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n            immediate = false;\n            g_sound_effect_manager.Play(26);\n        }\n\n        if (target_state != 0) {\n            if (target_state == 4) {\n                jetpack_channel.SetAnimation(1, immediate, target_state);\n                jetpack_channel.SetAnimation(\n                    0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n                g_sound_effect_manager.Play(16);\n            }\n        } else {\n            jetpack_channel.SetAnimation(\n                ANIM_MANAGER_HIDE_ANIMATION_ID,\n                0,\n                OBJECT_ANIMATION_MODE_UNCHANGED);\n        }\n\n        jetpack_channel.selected_state = target_state;\n    }\n}\n"
        }
      ]
    }
  ]
}
