{
  "schema": 1,
  "sites": [
    {
      "name": "mapping-control",
      "find": "    int input_state = state;\n    register int target_state;\n\n    switch (input_state) {\n    case 0:\n        target_state = 0;\n        break;\n    case 1:\n        target_state = 4;\n        break;\n    default:\n        target_state = input_state;\n        break;\n    }",
      "replacements": [
        {
          "name": "direct-switch",
          "text": "    int target_state;\n    switch (state) {\n    case 0:\n        target_state = 0;\n        break;\n    case 1:\n        target_state = 4;\n        break;\n    default:\n        target_state = state;\n        break;\n    }"
        },
        {
          "name": "nested-nonzero-first",
          "text": "    int target_state;\n    if (state != 0) {\n        if (state == 1)\n            target_state = 4;\n        else\n            target_state = state;\n    } else {\n        target_state = 0;\n    }"
        },
        {
          "name": "nested-zero-first",
          "text": "    int target_state;\n    if (state == 0)\n        target_state = 0;\n    else if (state == 1)\n        target_state = 4;\n    else\n        target_state = state;"
        }
      ]
    },
    {
      "name": "transition-control",
      "find": "    int selected_state = jetpack_channel.selected_state;\n    bool immediate = true;\n    if (selected_state != target_state) {\n        if (selected_state == 4) {\n            jetpack_channel.set_weapon_animation(\n                1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n            immediate = false;\n            g_sound_effect_manager.Play(26);\n        }\n\n        if (target_state != 0) {\n            if (target_state == 4) {\n                jetpack_channel.set_weapon_animation(1, immediate, target_state);\n                jetpack_channel.set_weapon_animation(\n                    0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n                g_sound_effect_manager.Play(16);\n            }\n        } else {\n            jetpack_channel.set_weapon_animation(\n                ANIM_MANAGER_HIDE_ANIMATION_ID,\n                0,\n                OBJECT_ANIMATION_MODE_UNCHANGED);\n        }\n\n        jetpack_channel.selected_state = target_state;\n    }",
      "replacements": [
        {
          "name": "early-equal-return",
          "text": "    int selected_state = jetpack_channel.selected_state;\n    bool immediate = true;\n    if (selected_state == target_state)\n        return;\n\n    if (selected_state == 4) {\n        jetpack_channel.set_weapon_animation(\n            1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n        immediate = false;\n        g_sound_effect_manager.Play(26);\n    }\n\n    if (target_state != 0) {\n        if (target_state == 4) {\n            jetpack_channel.set_weapon_animation(1, immediate, target_state);\n            jetpack_channel.set_weapon_animation(\n                0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n            g_sound_effect_manager.Play(16);\n        }\n    } else {\n        jetpack_channel.set_weapon_animation(\n            ANIM_MANAGER_HIDE_ANIMATION_ID,\n            0,\n            OBJECT_ANIMATION_MODE_UNCHANGED);\n    }\n\n    jetpack_channel.selected_state = target_state;"
        },
        {
          "name": "zero-path-first",
          "text": "    int selected_state = jetpack_channel.selected_state;\n    bool immediate = true;\n    if (selected_state == target_state)\n        return;\n\n    if (selected_state == 4) {\n        jetpack_channel.set_weapon_animation(\n            1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n        immediate = false;\n        g_sound_effect_manager.Play(26);\n    }\n\n    if (target_state == 0) {\n        jetpack_channel.set_weapon_animation(\n            ANIM_MANAGER_HIDE_ANIMATION_ID,\n            0,\n            OBJECT_ANIMATION_MODE_UNCHANGED);\n        jetpack_channel.selected_state = target_state;\n        return;\n    }\n    if (target_state == 4) {\n        jetpack_channel.set_weapon_animation(1, immediate, target_state);\n        jetpack_channel.set_weapon_animation(\n            0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n        g_sound_effect_manager.Play(16);\n    }\n    jetpack_channel.selected_state = target_state;"
        },
        {
          "name": "active-path-return",
          "text": "    int selected_state = jetpack_channel.selected_state;\n    bool immediate = true;\n    if (selected_state != target_state) {\n        if (selected_state == 4) {\n            jetpack_channel.set_weapon_animation(\n                1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n            immediate = false;\n            g_sound_effect_manager.Play(26);\n        }\n\n        if (target_state == 4) {\n            jetpack_channel.set_weapon_animation(1, immediate, target_state);\n            jetpack_channel.set_weapon_animation(\n                0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n            g_sound_effect_manager.Play(16);\n            jetpack_channel.selected_state = target_state;\n            return;\n        }\n        if (target_state == 0) {\n            jetpack_channel.set_weapon_animation(\n                ANIM_MANAGER_HIDE_ANIMATION_ID,\n                0,\n                OBJECT_ANIMATION_MODE_UNCHANGED);\n        }\n        jetpack_channel.selected_state = target_state;\n    }"
        },
        {
          "name": "android-structured",
          "text": "    int selected_state = jetpack_channel.selected_state;\n    if (selected_state == target_state)\n        return;\n\n    bool immediate;\n    if (selected_state == 4) {\n        jetpack_channel.set_weapon_animation(\n            1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n        immediate = false;\n        g_sound_effect_manager.Play(26);\n    } else {\n        immediate = true;\n    }\n\n    if (target_state != 0) {\n        if (target_state == 4) {\n            jetpack_channel.set_weapon_animation(1, immediate, target_state);\n            jetpack_channel.set_weapon_animation(\n                0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n            g_sound_effect_manager.Play(16);\n        }\n        jetpack_channel.selected_state = target_state;\n        return;\n    }\n    jetpack_channel.set_weapon_animation(\n        ANIM_MANAGER_HIDE_ANIMATION_ID,\n        0,\n        OBJECT_ANIMATION_MODE_UNCHANGED);\n    jetpack_channel.selected_state = target_state;"
        },
        {
          "name": "unsigned-char-immediate",
          "text": "    int selected_state = jetpack_channel.selected_state;\n    unsigned char immediate = 1;\n    if (selected_state != target_state) {\n        if (selected_state == 4) {\n            jetpack_channel.set_weapon_animation(\n                1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n            immediate = 0;\n            g_sound_effect_manager.Play(26);\n        }\n\n        if (target_state != 0) {\n            if (target_state == 4) {\n                jetpack_channel.set_weapon_animation(1, immediate, target_state);\n                jetpack_channel.set_weapon_animation(\n                    0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n                g_sound_effect_manager.Play(16);\n            }\n        } else {\n            jetpack_channel.set_weapon_animation(\n                ANIM_MANAGER_HIDE_ANIMATION_ID,\n                0,\n                OBJECT_ANIMATION_MODE_UNCHANGED);\n        }\n        jetpack_channel.selected_state = target_state;\n    }"
        },
        {
          "name": "selected-member-tests",
          "text": "    bool immediate = true;\n    if (jetpack_channel.selected_state != target_state) {\n        if (jetpack_channel.selected_state == 4) {\n            jetpack_channel.set_weapon_animation(\n                1, 1, OBJECT_ANIMATION_MODE_ONCE_REVERSE);\n            immediate = false;\n            g_sound_effect_manager.Play(26);\n        }\n\n        if (target_state != 0) {\n            if (target_state == 4) {\n                jetpack_channel.set_weapon_animation(1, immediate, target_state);\n                jetpack_channel.set_weapon_animation(\n                    0, 0, OBJECT_ANIMATION_MODE_UNCHANGED);\n                g_sound_effect_manager.Play(16);\n            }\n        } else {\n            jetpack_channel.set_weapon_animation(\n                ANIM_MANAGER_HIDE_ANIMATION_ID,\n                0,\n                OBJECT_ANIMATION_MODE_UNCHANGED);\n        }\n        jetpack_channel.selected_state = target_state;\n    }"
        }
      ]
    }
  ]
}
