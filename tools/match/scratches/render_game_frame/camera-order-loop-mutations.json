{
  "schema": 1,
  "sites": [
    {
      "name": "camera-order-loop-shape",
      "find": "    int ordered_count = 0;\n    {\n        for (int order_index = 0;\n             order_index < CAMERA_SLOT_COUNT;\n             ++order_index) {\n            if ((slots[order_index].flags & 1) != 0) {\n                if (ordered_count == 0) {\n                    camera_order[0] = order_index;\n                    ordered_count = 1;\n                } else {\n                    for (int insert = 0; insert < ordered_count; ++insert) {\n                        if (slots[order_index].sort_key >\n                            slots[camera_order[insert]].sort_key) {\n                            if (insert < CAMERA_LAST_INDEX) {\n                                int* shift = &camera_order[CAMERA_LAST_INDEX];\n                                int shift_count = CAMERA_LAST_INDEX - insert;\n                                do {\n                                    *shift = shift[-1];\n                                    --shift;\n                                    --shift_count;\n                                } while (shift_count != 0);\n                            }\n                            camera_order[insert] = order_index;\n                            ++ordered_count;\n                            break;\n                        }\n                    }\n                }\n            }\n        }\n    }",
      "replacements": [
        {
          "name": "indexed-for-mobile-exit",
          "text": "    int ordered_count = 0;\n    {\n        for (int order_index = 0;\n             order_index < CAMERA_SLOT_COUNT;\n             ++order_index) {\n            if ((slots[order_index].flags & 1) != 0) {\n                if (ordered_count == 0) {\n                    camera_order[0] = order_index;\n                    ordered_count = 1;\n                } else {\n                    for (int insert = 0; insert < ordered_count; ++insert) {\n                        if (slots[order_index].sort_key >\n                            slots[camera_order[insert]].sort_key) {\n                            if (insert < CAMERA_LAST_INDEX) {\n                                int* shift = &camera_order[CAMERA_LAST_INDEX];\n                                int shift_count = CAMERA_LAST_INDEX - insert;\n                                do {\n                                    *shift = shift[-1];\n                                    --shift;\n                                    --shift_count;\n                                } while (shift_count != 0);\n                            }\n                            camera_order[insert] = order_index;\n                            ++ordered_count;\n                            insert = ordered_count;\n                        }\n                    }\n                }\n            }\n        }\n    }"
        },
        {
          "name": "indexed-do-mobile-exit",
          "text": "    int ordered_count = 0;\n    {\n        for (int order_index = 0;\n             order_index < CAMERA_SLOT_COUNT;\n             ++order_index) {\n            if ((slots[order_index].flags & 1) != 0) {\n                if (ordered_count == 0) {\n                    camera_order[0] = order_index;\n                    ordered_count = 1;\n                } else {\n                    int insert = 0;\n                    do {\n                        if (slots[order_index].sort_key >\n                            slots[camera_order[insert]].sort_key) {\n                            if (insert < CAMERA_LAST_INDEX) {\n                                int* shift = &camera_order[CAMERA_LAST_INDEX];\n                                int shift_count = CAMERA_LAST_INDEX - insert;\n                                do {\n                                    *shift = shift[-1];\n                                    --shift;\n                                    --shift_count;\n                                } while (shift_count != 0);\n                            }\n                            camera_order[insert] = order_index;\n                            ++ordered_count;\n                            insert = ordered_count;\n                        }\n                        ++insert;\n                    } while (insert < ordered_count);\n                }\n            }\n        }\n    }"
        },
        {
          "name": "cursor-for-break",
          "text": "    int ordered_count = 0;\n    {\n        Viewport* order_slot = slots;\n        for (int order_index = 0;\n             order_index < CAMERA_SLOT_COUNT;\n             ++order_index, ++order_slot) {\n            if ((order_slot->flags & 1) != 0) {\n                if (ordered_count == 0) {\n                    camera_order[0] = order_index;\n                    ordered_count = 1;\n                } else {\n                    for (int insert = 0; insert < ordered_count; ++insert) {\n                        if (order_slot->sort_key >\n                            slots[camera_order[insert]].sort_key) {\n                            if (insert < CAMERA_LAST_INDEX) {\n                                int* shift = &camera_order[CAMERA_LAST_INDEX];\n                                int shift_count = CAMERA_LAST_INDEX - insert;\n                                do {\n                                    *shift = shift[-1];\n                                    --shift;\n                                    --shift_count;\n                                } while (shift_count != 0);\n                            }\n                            camera_order[insert] = order_index;\n                            ++ordered_count;\n                            break;\n                        }\n                    }\n                }\n            }\n        }\n    }"
        },
        {
          "name": "cursor-for-mobile-exit",
          "text": "    int ordered_count = 0;\n    {\n        Viewport* order_slot = slots;\n        for (int order_index = 0;\n             order_index < CAMERA_SLOT_COUNT;\n             ++order_index, ++order_slot) {\n            if ((order_slot->flags & 1) != 0) {\n                if (ordered_count == 0) {\n                    camera_order[0] = order_index;\n                    ordered_count = 1;\n                } else {\n                    for (int insert = 0; insert < ordered_count; ++insert) {\n                        if (order_slot->sort_key >\n                            slots[camera_order[insert]].sort_key) {\n                            if (insert < CAMERA_LAST_INDEX) {\n                                int* shift = &camera_order[CAMERA_LAST_INDEX];\n                                int shift_count = CAMERA_LAST_INDEX - insert;\n                                do {\n                                    *shift = shift[-1];\n                                    --shift;\n                                    --shift_count;\n                                } while (shift_count != 0);\n                            }\n                            camera_order[insert] = order_index;\n                            ++ordered_count;\n                            insert = ordered_count;\n                        }\n                    }\n                }\n            }\n        }\n    }"
        },
        {
          "name": "cursor-do-mobile-exit",
          "text": "    int ordered_count = 0;\n    {\n        int order_index = 0;\n        Viewport* order_slot = slots;\n        do {\n            if ((order_slot->flags & 1) != 0) {\n                if (ordered_count == 0) {\n                    camera_order[0] = order_index;\n                    ordered_count = 1;\n                } else {\n                    int insert = 0;\n                    do {\n                        if (order_slot->sort_key >\n                            slots[camera_order[insert]].sort_key) {\n                            if (insert < CAMERA_LAST_INDEX) {\n                                int* shift = &camera_order[CAMERA_LAST_INDEX];\n                                int shift_count = CAMERA_LAST_INDEX - insert;\n                                do {\n                                    *shift = shift[-1];\n                                    --shift;\n                                    --shift_count;\n                                } while (shift_count != 0);\n                            }\n                            camera_order[insert] = order_index;\n                            ++ordered_count;\n                            insert = ordered_count;\n                        }\n                        ++insert;\n                    } while (insert < ordered_count);\n                }\n            }\n            ++order_index;\n            ++order_slot;\n        } while (order_index != CAMERA_SLOT_COUNT);\n    }"
        },
        {
          "name": "cursor-do-mobile-comparison",
          "text": "    int ordered_count = 0;\n    {\n        int order_index = 0;\n        Viewport* order_slot = slots;\n        do {\n            if ((order_slot->flags & 1) != 0) {\n                if (ordered_count == 0) {\n                    camera_order[0] = order_index;\n                    ordered_count = 1;\n                } else {\n                    int insert = 0;\n                    do {\n                        if (slots[camera_order[insert]].sort_key <\n                            order_slot->sort_key) {\n                            if (insert < CAMERA_LAST_INDEX) {\n                                int* shift = &camera_order[CAMERA_LAST_INDEX];\n                                int shift_count = CAMERA_LAST_INDEX - insert;\n                                do {\n                                    *shift = shift[-1];\n                                    --shift;\n                                    --shift_count;\n                                } while (shift_count != 0);\n                            }\n                            camera_order[insert] = order_index;\n                            ++ordered_count;\n                            insert = ordered_count;\n                        }\n                        ++insert;\n                    } while (insert < ordered_count);\n                }\n            }\n            ++order_index;\n            ++order_slot;\n        } while (order_index != CAMERA_SLOT_COUNT);\n    }"
        }
      ]
    }
  ]
}
