{
  "schema": 1,
  "sites": [
    {
      "name": "depth-node-allocation",
      "find": "                        if (bucket_index >= 0) {\n                            SpriteDepthNode* head = g_sprite_depth_buckets[bucket_index];\n                            SpriteDepthNode* previous = 0;\n                            SpriteDepthNode* cursor = head;\n                            if (cursor != 0) {\n                                while (cursor != 0 && depth_key < cursor->depth_key) {\n                                    previous = cursor;\n                                    cursor = cursor->next;\n                                }\n                                if (cursor == 0) {\n                                    previous->next = next_depth_node;\n                                    next_depth_node->next = 0;\n                                } else if (previous == 0) {\n                                    next_depth_node->next = cursor;\n                                    g_sprite_depth_buckets[bucket_index] = next_depth_node;\n                                } else {\n                                    previous->next = next_depth_node;\n                                    next_depth_node->next = cursor;\n                                }\n                            } else {\n                                g_sprite_depth_buckets[bucket_index] = next_depth_node;\n                                next_depth_node->next = 0;\n                            }\n\n                            next_depth_node->position = projected;\n                            next_depth_node->depth_key = depth_key;\n                            next_depth_node->sprite = sprite;\n                            sprite->render_bucket_index = bucket_index;\n                            sprite->render_depth_key = depth_key;\n                            ++next_depth_node;\n                        }",
      "replacements": [
        {
          "name": "postincrement-before-branches",
          "text": "                        if (bucket_index >= 0) {\n                            SpriteDepthNode* head = g_sprite_depth_buckets[bucket_index];\n                            SpriteDepthNode* node = next_depth_node++;\n                            SpriteDepthNode* previous = 0;\n                            SpriteDepthNode* cursor = head;\n                            if (cursor != 0) {\n                                while (cursor != 0 && depth_key < cursor->depth_key) {\n                                    previous = cursor;\n                                    cursor = cursor->next;\n                                }\n                                if (cursor == 0) {\n                                    previous->next = node;\n                                    node->next = 0;\n                                } else if (previous == 0) {\n                                    node->next = cursor;\n                                    g_sprite_depth_buckets[bucket_index] = node;\n                                } else {\n                                    previous->next = node;\n                                    node->next = cursor;\n                                }\n                            } else {\n                                g_sprite_depth_buckets[bucket_index] = node;\n                                node->next = 0;\n                            }\n\n                            node->position = projected;\n                            node->depth_key = depth_key;\n                            node->sprite = sprite;\n                            sprite->render_bucket_index = bucket_index;\n                            sprite->render_depth_key = depth_key;\n                        }"
        },
        {
          "name": "split-increment-before-branches",
          "text": "                        if (bucket_index >= 0) {\n                            SpriteDepthNode* head = g_sprite_depth_buckets[bucket_index];\n                            SpriteDepthNode* node = next_depth_node;\n                            ++next_depth_node;\n                            SpriteDepthNode* previous = 0;\n                            SpriteDepthNode* cursor = head;\n                            if (cursor != 0) {\n                                while (cursor != 0 && depth_key < cursor->depth_key) {\n                                    previous = cursor;\n                                    cursor = cursor->next;\n                                }\n                                if (cursor == 0) {\n                                    previous->next = node;\n                                    node->next = 0;\n                                } else if (previous == 0) {\n                                    node->next = cursor;\n                                    g_sprite_depth_buckets[bucket_index] = node;\n                                } else {\n                                    previous->next = node;\n                                    node->next = cursor;\n                                }\n                            } else {\n                                g_sprite_depth_buckets[bucket_index] = node;\n                                node->next = 0;\n                            }\n\n                            node->position = projected;\n                            node->depth_key = depth_key;\n                            node->sprite = sprite;\n                            sprite->render_bucket_index = bucket_index;\n                            sprite->render_depth_key = depth_key;\n                        }"
        },
        {
          "name": "postincrement-before-head",
          "text": "                        if (bucket_index >= 0) {\n                            SpriteDepthNode* node = next_depth_node++;\n                            SpriteDepthNode* head = g_sprite_depth_buckets[bucket_index];\n                            SpriteDepthNode* previous = 0;\n                            SpriteDepthNode* cursor = head;\n                            if (cursor != 0) {\n                                while (cursor != 0 && depth_key < cursor->depth_key) {\n                                    previous = cursor;\n                                    cursor = cursor->next;\n                                }\n                                if (cursor == 0) {\n                                    previous->next = node;\n                                    node->next = 0;\n                                } else if (previous == 0) {\n                                    node->next = cursor;\n                                    g_sprite_depth_buckets[bucket_index] = node;\n                                } else {\n                                    previous->next = node;\n                                    node->next = cursor;\n                                }\n                            } else {\n                                g_sprite_depth_buckets[bucket_index] = node;\n                                node->next = 0;\n                            }\n\n                            node->position = projected;\n                            node->depth_key = depth_key;\n                            node->sprite = sprite;\n                            sprite->render_bucket_index = bucket_index;\n                            sprite->render_depth_key = depth_key;\n                        }"
        }
      ]
    }
  ]
}
