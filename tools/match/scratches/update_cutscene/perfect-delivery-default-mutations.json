{
  "schema": 1,
  "sites": [
    {
      "name": "perfect-delivery-default",
      "find": "        GameRoot* game = g_game;\n        if (game->subgame.level_mode == 0) {\n            int delivered_count = player->parcels_collected;\n            unsigned char perfect_delivery =\n                delivered_count\n                == *(int*)((char*)g_game + LEVEL_PARCEL_COUNT_FROM_GAME_ROOT);\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        } else if (game->subgame.level_mode == 1) {\n            int delivered_count = player->parcels_collected;\n            game->subgame.completion.initialize_completion_screen(delivered_count, 1);\n        }",
      "replacements": [
        {
          "name": "case-unsigned-char-default",
          "text": "        unsigned char perfect_delivery = 1;\n        GameRoot* game = g_game;\n        if (game->subgame.level_mode == 0) {\n            int delivered_count = player->parcels_collected;\n            perfect_delivery =\n                delivered_count\n                == *(int*)((char*)g_game + LEVEL_PARCEL_COUNT_FROM_GAME_ROOT);\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        } else if (game->subgame.level_mode == 1) {\n            int delivered_count = player->parcels_collected;\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        }"
        },
        {
          "name": "case-bool-default",
          "text": "        bool perfect_delivery = true;\n        GameRoot* game = g_game;\n        if (game->subgame.level_mode == 0) {\n            int delivered_count = player->parcels_collected;\n            perfect_delivery =\n                delivered_count\n                == *(int*)((char*)g_game + LEVEL_PARCEL_COUNT_FROM_GAME_ROOT);\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        } else if (game->subgame.level_mode == 1) {\n            int delivered_count = player->parcels_collected;\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        }"
        },
        {
          "name": "case-char-default",
          "text": "        char perfect_delivery = 1;\n        GameRoot* game = g_game;\n        if (game->subgame.level_mode == 0) {\n            int delivered_count = player->parcels_collected;\n            perfect_delivery =\n                delivered_count\n                == *(int*)((char*)g_game + LEVEL_PARCEL_COUNT_FROM_GAME_ROOT);\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        } else if (game->subgame.level_mode == 1) {\n            int delivered_count = player->parcels_collected;\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        }"
        },
        {
          "name": "case-int-default",
          "text": "        int perfect_delivery = 1;\n        GameRoot* game = g_game;\n        if (game->subgame.level_mode == 0) {\n            int delivered_count = player->parcels_collected;\n            perfect_delivery =\n                delivered_count\n                == *(int*)((char*)g_game + LEVEL_PARCEL_COUNT_FROM_GAME_ROOT);\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        } else if (game->subgame.level_mode == 1) {\n            int delivered_count = player->parcels_collected;\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        }"
        },
        {
          "name": "case-const-default-separate-comparison",
          "text": "        const unsigned char perfect_delivery_default = 1;\n        GameRoot* game = g_game;\n        if (game->subgame.level_mode == 0) {\n            int delivered_count = player->parcels_collected;\n            unsigned char perfect_delivery =\n                delivered_count\n                == *(int*)((char*)g_game + LEVEL_PARCEL_COUNT_FROM_GAME_ROOT);\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery);\n        } else if (game->subgame.level_mode == 1) {\n            int delivered_count = player->parcels_collected;\n            game->subgame.completion.initialize_completion_screen(\n                delivered_count,\n                perfect_delivery_default);\n        }"
        }
      ]
    }
  ]
}
