# is_sub_loc_ramp @ 0x439a70

Exact `SubLoc` predicate matching Android `cRSubLoc::IsRamp()` at the semantic
level: both accept the twelve named brace/bracket/angle ramp identities
`0x02..0x0d` in the same authored grouping. The authored owner name retires the former
`is_ramp_cache_tile_family` label. The scratch now defines the helper on
`SubLoc` and remains exact at 29/29 instructions.
