{
  "schema": 1,
  "sites": [
    {
      "name": "unlink-ownership",
      "find": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n",
      "replacements": [
        {
          "name": "snapshot-cursor-local-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "snapshot-cursor-direct-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-cursor-direct-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-cursor-borrow-direct-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-cursor-borrow-direct-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-cursor-iterator-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "snapshot-cursor-iterator-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "snapshot-index-local-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "snapshot-index-local-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "snapshot-index-direct-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-index-direct-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-index-borrow-direct-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-index-borrow-direct-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-index-iterator-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "snapshot-index-iterator-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "snapshot-reference-local-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "snapshot-reference-local-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "snapshot-reference-direct-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-reference-direct-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-reference-borrow-direct-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-reference-borrow-direct-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "snapshot-reference-iterator-macro",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "snapshot-reference-iterator-method",
          "text": "// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "direct-checks-cursor-local-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "direct-checks-cursor-local-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "direct-checks-cursor-direct-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-cursor-direct-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-cursor-borrow-direct-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-cursor-borrow-direct-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-cursor-iterator-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "direct-checks-cursor-iterator-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        char* row_record = (char*)g_game + row_index * sizeof(SubRow);\n        if ((OUTER_RUNTIME_ROW(row_record)->flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY)\n            != 0) {\n            if ((OUTER_RUNTIME_ROW(row_record)->attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &OUTER_RUNTIME_ROW(row_record)->attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "direct-checks-index-local-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "direct-checks-index-local-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "direct-checks-index-direct-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-index-direct-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-index-borrow-direct-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-index-borrow-direct-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-index-iterator-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "direct-checks-index-iterator-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        if ((g_game->subgame.runtime_rows[row_index].flags\n                & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((g_game->subgame.runtime_rows[row_index].attachment_body.list_flags\n                    & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(\n                    &g_game->subgame.runtime_rows[row_index].attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "direct-checks-reference-local-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "direct-checks-reference-local-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        Fringe* object = fringe[i];\n        if (object != 0 && (object->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(object);\n    }\n}\n"
        },
        {
          "name": "direct-checks-reference-direct-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-reference-direct-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringes[i] != 0 && (fringes[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringes[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-reference-borrow-direct-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-reference-borrow-direct-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    Fringe** fringe = fringes;\n    for (int i = 0; i < (int)(sizeof(fringes) / sizeof(fringes[0])); ++i) {\n        if (fringe[i] != 0 && (fringe[i]->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(fringe[i]);\n    }\n}\n"
        },
        {
          "name": "direct-checks-reference-iterator-macro",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\n#define REMOVE_PRECHECKED_BOD_NODE(node_expr, unlink_mask_expr)   \\\n    do {                                                          \\\n        BodNode* node = (node_expr);                              \\\n        DWORD flags = node->list_flags;                           \\\n        BodList* list = &g_game->active_bod_list;                 \\\n        if ((flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {         \\\n            report_errorf(\"List remove NEXTBOD\");                \\\n        } else {                                                  \\\n            BodNode* next = node->list_next;                      \\\n            if (next != 0)                                       \\\n                next->list_prev = node->list_prev;                \\\n            BodNode* prev = node->list_prev;                      \\\n            if (prev != 0)                                       \\\n                prev->list_next = node->list_next;                \\\n            else                                                  \\\n                list->first = node->list_next;                    \\\n            node->list_next = list->free_top;                     \\\n            list->free_top = node;                                \\\n            node->list_flags &= (unlink_mask_expr);               \\\n        }                                                         \\\n    } while (0)\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n    DWORD unlink_mask = ~BOD_FLAG_LINKED;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        REMOVE_PRECHECKED_BOD_NODE(this, unlink_mask);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        },
        {
          "name": "direct-checks-reference-iterator-method",
          "text": "// Intrusive BOD active/free list views, partial.\n#ifndef BOD_LIST_H\n#define BOD_LIST_H\n\n#include <stddef.h>\n\n#include \"contact_target.h\"\n\nint report_errorf(char* format, ...);\n\nstruct BodNode : public ContactTargetObject {\n    // Inlined cLinkedList<cRBod>::AddAfter used by the subgame group heads\n    // and their live members.\n    __forceinline void add_bod_after(BodNode* previous)\n    {\n        if ((list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADDafter\");\n        } else {\n            list_prev = previous;\n            list_next = previous->list_next;\n            previous->list_next = this;\n            if (list_next != 0)\n                list_next->list_prev = this;\n            list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    BodNode* list_prev;      // +0x08\n    BodNode* list_next;      // +0x0c\n};\ntypedef char BodNode_must_be_0x10[(sizeof(BodNode) == 0x10) ? 1 : -1];\n\n// Several pool teardown loops advance a cursor rooted at list_next rather than\n// at the containing BodNode. Keep that source shape while deriving every\n// backwards reach from the shared intrusive-node layout.\n#define BOD_NODE_FROM_NEXT_LINK(next_link) \\\n    ((BodNode*)((char*)(next_link) - (int)offsetof(BodNode, list_next)))\n#define BOD_NEXT_LINK_FLAGS(next_link) \\\n    (*(unsigned int*)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_flags))))\n#define BOD_NEXT_LINK_PREV(next_link) \\\n    (*(BodNode**)((char*)(next_link) \\\n        - ((int)offsetof(BodNode, list_next) \\\n            - (int)offsetof(BodNode, list_prev))))\n\n// Windows layout projection of the authored cLinkedList<cRBod> specialization.\n// Keep the stable BodList spelling for the exact matcher and analyzer replay.\nclass BodList {\npublic:\n    // Authored cLinkedList<cRBod>::Add(cRBod&); the reference has the same\n    // pointer ABI as this Windows projection.\n    void add_bod_to_front(BodNode* node); // @ 0x4113b0\n    // Exact tail splice inlined by cRGame::Init4 on Android/iOS. The mobile\n    // binaries preserve List ADDend but do not export a method name for it.\n    void append_bod_to_end(BodNode* node); // @ 0x411420\n    void recycle_bod_to_free_list(BodNode* node); // @ 0x447290\n\n    // Inlined cLinkedList<cRBod>::Add used by pickup allocators.\n    void add_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) != 0) {\n            report_errorf(\"List ADD\");\n        } else {\n            if (first == 0) {\n                first = node;\n                node->list_prev = 0;\n                first->list_next = 0;\n            } else {\n                first->list_prev = node;\n                first->list_prev->list_next = first;\n                first = first->list_prev;\n                first->list_prev = 0;\n            }\n            node->list_flags |= BOD_FLAG_LINKED;\n        }\n    }\n\n    // Inlined cLinkedList<cRBod>::Remove; 0x447290 is its exact emitted copy.\n    void remove_bod(BodNode* node)\n    {\n        if ((node->list_flags & BOD_FLAG_LINKED) == 0) {\n            report_errorf(\"List remove\");\n            return;\n        }\n        if ((node->list_flags & BOD_FLAG_NEXT_UPDATE_GUARD) != 0) {\n            report_errorf(\"List remove NEXTBOD\");\n            return;\n        }\n\n        BodNode* next = node->list_next;\n        if (next != 0)\n            next->list_prev = node->list_prev;\n\n        BodNode* prev = node->list_prev;\n        if (prev != 0)\n            prev->list_next = node->list_next;\n        else\n            first = node->list_next;\n\n        node->list_next = free_top;\n        free_top = node;\n        node->list_flags &= ~BOD_FLAG_LINKED;\n    }\n\n    int unknown_00;\n    BodNode* first;    // +0x04\n    BodNode* free_top; // +0x08\n};\ntypedef char BodList_must_be_0x0c[(sizeof(BodList) == 0x0c) ? 1 : -1];\n\n#endif\n\n// remove_sub_loc @ 0x439bc0 (thiscall, ret) \u2014 cRSubLoc::Remove()\n\n#include <stddef.h>\n\n#include \"fringe_object.h\"\n#include \"game_root.h\"\n#include \"track_attachment_types.h\"\n\ntypedef unsigned int DWORD;\n\n\nint report_errorf(char* format, ...);\n\nenum {\n    ROOT_RUNTIME_ROWS_BASE =\n        offsetof(GameRoot, subgame) + offsetof(cRSubGame, runtime_rows),\n};\n\n#define OUTER_RUNTIME_ROW(cursor_expr) \\\n    ((SubRow*)((cursor_expr) + ROOT_RUNTIME_ROWS_BASE))\n\nvoid cRSubLoc::Remove()\n{\n    int row_index = Yi();\n    unsigned char tile = tile_id;\n\n    if (tile == SUBLOC_TILE_PATH_ENTRY_LOWERCASE\n        || tile == SUBLOC_TILE_PATH_ENTRY_UPPERCASE) {\n        SubRow& row = g_game->subgame.runtime_rows[row_index];\n        if ((row.flags & SUBROW_FLAG_PATH_OR_MODEL_VELOCITY) != 0) {\n            if ((row.attachment_body.list_flags & BOD_FLAG_LINKED) != 0)\n                g_game->active_bod_list.remove_bod(&row.attachment_body);\n        }\n    }\n\n    if ((list_flags & BOD_FLAG_LINKED) != 0)\n        g_game->active_bod_list.remove_bod(this);\n\n    for (Fringe** fringe = fringes; fringe != fringes + sizeof(fringes) / sizeof(fringes[0]); ++fringe) {\n        if (*fringe != 0 && ((*fringe)->list_flags & BOD_FLAG_LINKED) != 0)\n            g_game->active_bod_list.remove_bod(*fringe);\n    }\n}\n"
        }
      ]
    }
  ]
}
