{
  "schema": 1,
  "sites": [
    {
      "name": "slide-scan-owner",
      "find": "                    int run_length = 0;\n                    cRSubLoc* cursor = cell;\n                    int lane_cursor = lane;\n                    while (lane_cursor\n                               < (int)(sizeof(runtime_cells[0])\n                                   / sizeof(runtime_cells[0][0]))\n                           && cursor->IsSlide() != 0\n                           && (cursor->lane_and_flags & SUBLOC_FLAG_CORNER_OBJECT) == 0\n                           && (cursor->lane_and_flags & SUBLOC_FLAG_UNCACHED_BODY) != 0\n                           && (cursor->lane_and_flags\n                                   & (SUBLOC_FLAG_WARNING_CACHE_FAMILY\n                                       | SUBLOC_FLAG_CACHE_FAMILY_SWAPPED))\n                               == 0) {\n                        ++run_length;\n                        ++cursor;\n                        ++lane_cursor;\n                    }\n\n                    if (run_length > 1) {\n                        ((BodBase*)CELL_FROM_LANE_FLAGS(cell_lane_flags))\n                            ->SetObject(\n                                g_game->root_bod_catalog.slide_slices\n                                    .storage[run_length - 1]\n                                    .object);\n                        CLEAR_MERGED_CONTINUATIONS(this, row_index, lane, run_length);\n                    }",
      "replacements": [
        {
          "name": "lane-flags-cursor",
          "text": "                    int run_length = 0;\n                    unsigned int* cursor_lane_flags = cell_lane_flags;\n                    int lane_cursor = lane;\n                    while (lane_cursor\n                               < (int)(sizeof(runtime_cells[0])\n                                   / sizeof(runtime_cells[0][0]))\n                           && CELL_FROM_LANE_FLAGS(cursor_lane_flags)->IsSlide() != 0\n                           && (*cursor_lane_flags & SUBLOC_FLAG_CORNER_OBJECT) == 0\n                           && (*cursor_lane_flags & SUBLOC_FLAG_UNCACHED_BODY) != 0\n                           && (*cursor_lane_flags\n                                   & (SUBLOC_FLAG_WARNING_CACHE_FAMILY\n                                       | SUBLOC_FLAG_CACHE_FAMILY_SWAPPED))\n                               == 0) {\n                        ++run_length;\n                        cursor_lane_flags +=\n                            sizeof(cRSubLoc) / sizeof(unsigned int);\n                        ++lane_cursor;\n                    }\n\n                    if (run_length > 1) {\n                        ((BodBase*)CELL_FROM_LANE_FLAGS(cell_lane_flags))\n                            ->SetObject(\n                                g_game->root_bod_catalog.slide_slices\n                                    .storage[run_length - 1]\n                                    .object);\n                        CLEAR_MERGED_CONTINUATIONS(this, row_index, lane, run_length);\n                    }"
        },
        {
          "name": "lane-flags-cursor-shared-lane",
          "text": "                    int run_length = 0;\n                    int first_lane = lane;\n                    unsigned int* cursor_lane_flags = cell_lane_flags;\n                    while (lane\n                               < (int)(sizeof(runtime_cells[0])\n                                   / sizeof(runtime_cells[0][0]))\n                           && CELL_FROM_LANE_FLAGS(cursor_lane_flags)->IsSlide() != 0\n                           && (*cursor_lane_flags & SUBLOC_FLAG_CORNER_OBJECT) == 0\n                           && (*cursor_lane_flags & SUBLOC_FLAG_UNCACHED_BODY) != 0\n                           && (*cursor_lane_flags\n                                   & (SUBLOC_FLAG_WARNING_CACHE_FAMILY\n                                       | SUBLOC_FLAG_CACHE_FAMILY_SWAPPED))\n                               == 0) {\n                        ++run_length;\n                        cursor_lane_flags +=\n                            sizeof(cRSubLoc) / sizeof(unsigned int);\n                        ++lane;\n                    }\n\n                    if (run_length > 1) {\n                        ((BodBase*)CELL_FROM_LANE_FLAGS(cell_lane_flags))\n                            ->SetObject(\n                                g_game->root_bod_catalog.slide_slices\n                                    .storage[run_length - 1]\n                                    .object);\n                        CLEAR_MERGED_CONTINUATIONS(\n                            this, row_index, first_lane, run_length);\n                    }\n                    lane = first_lane;"
        },
        {
          "name": "lane-flags-cursor-saved-lane-after-scan",
          "text": "                    int run_length = 0;\n                    int saved_lane = lane;\n                    unsigned int* cursor_lane_flags = cell_lane_flags;\n                    do {\n                        if (lane\n                                >= (int)(sizeof(runtime_cells[0])\n                                    / sizeof(runtime_cells[0][0]))) {\n                            break;\n                        }\n                        if (CELL_FROM_LANE_FLAGS(cursor_lane_flags)->IsSlide() == 0\n                            || (*cursor_lane_flags & SUBLOC_FLAG_CORNER_OBJECT) != 0\n                            || (*cursor_lane_flags & SUBLOC_FLAG_UNCACHED_BODY) == 0\n                            || (*cursor_lane_flags\n                                    & (SUBLOC_FLAG_WARNING_CACHE_FAMILY\n                                        | SUBLOC_FLAG_CACHE_FAMILY_SWAPPED))\n                                != 0) {\n                            break;\n                        }\n                        ++run_length;\n                        cursor_lane_flags +=\n                            sizeof(cRSubLoc) / sizeof(unsigned int);\n                        ++lane;\n                    } while (lane\n                        < (int)(sizeof(runtime_cells[0])\n                            / sizeof(runtime_cells[0][0])));\n\n                    if (run_length > 1) {\n                        ((BodBase*)CELL_FROM_LANE_FLAGS(cell_lane_flags))\n                            ->SetObject(\n                                g_game->root_bod_catalog.slide_slices\n                                    .storage[run_length - 1]\n                                    .object);\n                        CLEAR_MERGED_CONTINUATIONS(\n                            this, row_index, saved_lane, run_length);\n                    }\n                    lane = saved_lane;"
        },
        {
          "name": "lane-flags-cursor-direct-limit",
          "text": "                    int run_length = 0;\n                    unsigned int* cursor_lane_flags = cell_lane_flags;\n                    int lane_cursor = lane;\n                    while (lane_cursor < 8\n                           && CELL_FROM_LANE_FLAGS(cursor_lane_flags)->IsSlide() != 0\n                           && (*cursor_lane_flags & SUBLOC_FLAG_CORNER_OBJECT) == 0\n                           && (*cursor_lane_flags & SUBLOC_FLAG_UNCACHED_BODY) != 0\n                           && (*cursor_lane_flags\n                                   & (SUBLOC_FLAG_WARNING_CACHE_FAMILY\n                                       | SUBLOC_FLAG_CACHE_FAMILY_SWAPPED))\n                               == 0) {\n                        ++run_length;\n                        cursor_lane_flags += 21;\n                        ++lane_cursor;\n                    }\n\n                    if (run_length > 1) {\n                        ((BodBase*)CELL_FROM_LANE_FLAGS(cell_lane_flags))\n                            ->SetObject(\n                                g_game->root_bod_catalog.slide_slices\n                                    .storage[run_length - 1]\n                                    .object);\n                        CLEAR_MERGED_CONTINUATIONS(this, row_index, lane, run_length);\n                    }"
        },
        {
          "name": "cell-cursor-shared-lane",
          "text": "                    int run_length = 0;\n                    int first_lane = lane;\n                    cRSubLoc* cursor = cell;\n                    while (lane\n                               < (int)(sizeof(runtime_cells[0])\n                                   / sizeof(runtime_cells[0][0]))\n                           && cursor->IsSlide() != 0\n                           && (cursor->lane_and_flags & SUBLOC_FLAG_CORNER_OBJECT) == 0\n                           && (cursor->lane_and_flags & SUBLOC_FLAG_UNCACHED_BODY) != 0\n                           && (cursor->lane_and_flags\n                                   & (SUBLOC_FLAG_WARNING_CACHE_FAMILY\n                                       | SUBLOC_FLAG_CACHE_FAMILY_SWAPPED))\n                               == 0) {\n                        ++run_length;\n                        ++cursor;\n                        ++lane;\n                    }\n\n                    if (run_length > 1) {\n                        ((BodBase*)CELL_FROM_LANE_FLAGS(cell_lane_flags))\n                            ->SetObject(\n                                g_game->root_bod_catalog.slide_slices\n                                    .storage[run_length - 1]\n                                    .object);\n                        CLEAR_MERGED_CONTINUATIONS(\n                            this, row_index, first_lane, run_length);\n                    }\n                    lane = first_lane;"
        }
      ]
    }
  ]
}
