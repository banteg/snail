{
  "schema": 1,
  "sites": [
    {
      "name": "file-lookup",
      "find": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n",
      "replacements": [
        {
          "name": "inline-local-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-local-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-local-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-return-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-return-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-return-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-return-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-return-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-return-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-return-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-return-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-ref-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-ref-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-ref-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-ref-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-ptr-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-ptr-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-ptr-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-char-ptr-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-value-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-value-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-value-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-value-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-select-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-select-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-select-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "inline-int-select-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0) {\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n                }\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-local-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-local-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-local-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-local-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-return-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-return-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-return-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-return-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-return-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-return-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-return-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-return-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-ref-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-ref-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-ref-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-ref-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-ptr-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-ptr-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-ptr-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-char-ptr-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-value-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-value-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-value-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-value-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-select-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-select-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-select-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "do-int-select-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            do {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            } while (entry_index < count);\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-local-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-local-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-local-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-local-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-return-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-return-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-return-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-return-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-return-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-return-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-return-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-return-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-ref-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-ref-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-ref-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-ref-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-ptr-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-ptr-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-ptr-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-char-ptr-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-value-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-value-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-value-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-value-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-select-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-select-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-select-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "for-int-select-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            for (; entry_index < count; ++entry_index, entry_path += 3) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-local-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-local-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-local-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-local-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    if (request_char >= 'a' && request_char <= 'z') {\n                        request_char = request_char - 32;\n                    }\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-return-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-return-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-return-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-return-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline char fold_archive_request(char value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-return-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-return-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-return-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-return-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline int fold_archive_request(int value) { if (value >= 'a' && value <= 'z') return value - 32; return value; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    request_char = (char)fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-ref-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-ref-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-ref-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-ref-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char& value) { if (value >= 'a' && value <= 'z') value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-ptr-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-ptr-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-ptr-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-char-ptr-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nstatic inline void fold_archive_request(char* value) { if (*value >= 'a' && *value <= 'z') *value -= 32; }\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    fold_archive_request(&request_char);\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-value-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-value-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-value-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-value-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char;\n                    if (folded >= 'a' && folded <= 'z') folded -= 32;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-select-error0",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-select-error1",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char current_directory[512];\n        getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, current_directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-select-error2",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        char* directory = getcwd(current_directory, 512);\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, directory);\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        },
        {
          "name": "while-int-select-error3",
          "text": "// load_file_bytes_fixed_size_from_archive_or_fs @ 0x431030 (cdecl)\n\n#include <direct.h>\n\n#include \"archive_index.h\"\n#include \"loading_bar.h\"\n\nint get_stream_length_preserve_position(FILE* file);\nvoid* allocate_tracked_memory(int size, char* name);\nvoid xor_archive_bytes_in_place(int start_offset, char* bytes, int count);\nint report_messagef(char* format, ...);\n\nchar* __cdecl load_file_bytes_fixed_size_from_archive_or_fs(\n    char* file_name, char* buffer, int byte_count)\n{\n    char current_directory[512];\n    ArchiveIndex* archive_index;\n    int count;\n    int entry_index;\n    char** entry_path;\n    FILE* file;\n    char* result;\n\n    g_loading_bar.AI();\n\n    archive_index = g_archive_index_records;\n    if (archive_index != 0) {\n        count = archive_index->count;\n        entry_index = 0;\n        if (count > 0) {\n            entry_path = &archive_index->entries[0].path;\n            while (entry_index < count) {\n                char* archive_cursor = *entry_path;\n                char* request_cursor = file_name;\n\n                while (*archive_cursor != 0) {\n                    char archive_char = *archive_cursor;\n                    char request_char = *request_cursor;\n                    if (request_char == 0) {\n                        break;\n                    }\n\n                    int folded = request_char >= 'a' && request_char <= 'z' ? request_char - 32 : request_char;\n                    request_char = (char)folded;\n\n                    if (archive_char != request_char) {\n                        break;\n                    }\n\n                    ++request_cursor;\n                    ++archive_cursor;\n                }\n\n                if (*archive_cursor == 0 && *request_cursor == 0)\n                    goto found_archive_entry;\n\n                ++entry_index;\n                entry_path += 3;\n            }\n        }\n    }\n\n    goto filesystem_entry;\n\nfound_archive_entry:\n                    if (buffer == (char*)-1) {\n                        return (char*)g_archive_index_records->entries[entry_index].data_offset;\n                    }\n\n                    if (buffer == 0) {\n                        char* allocated = (char*)allocate_tracked_memory(byte_count, file_name);\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(allocated, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            allocated,\n                            byte_count);\n                        return allocated;\n                    } else {\n                        int current_offset = ftell(g_archive_file);\n                        fseek(g_archive_file,\n                            g_archive_index_records->entries[entry_index].data_offset\n                                - current_offset,\n                            SEEK_CUR);\n                        fread(buffer, 1, byte_count, g_archive_file);\n                        xor_archive_bytes_in_place(\n                            g_archive_index_records->entries[entry_index].data_offset,\n                            buffer,\n                            byte_count);\n                        return buffer;\n                    }\n\nfilesystem_entry:\n    file = fopen(file_name, \"rb\");\n    if (file == 0) {\n        report_messagef(\"WARNING:Cannot find file : %s (from %s)\\n\",\n            file_name, getcwd(current_directory, 512));\n        return 0;\n    }\n\n    get_stream_length_preserve_position(file);\n    result = buffer;\n    if (result == 0 || result == (char*)-1) {\n        result = (char*)allocate_tracked_memory(byte_count, file_name);\n    }\n\n    fread(result, 1, byte_count, file);\n    fclose(file);\n    return result;\n}\n"
        }
      ]
    }
  ]
}
