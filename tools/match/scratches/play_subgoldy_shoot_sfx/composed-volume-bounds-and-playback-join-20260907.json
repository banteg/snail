{
  "schema": 1,
  "sites": [
    {
      "name": "volume-bounds",
      "find": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (attachment_exit_pending) {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            if (volume < 0.0f) {\n                volume = 0.0f;\n            } else if (volume > 1.0f) {\n                volume = 1.0f;\n            }\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        } else {\n            g_sound_effect_manager.Play(sound_id);\n        }\n    }\n}\n",
      "replacements": [
        {
          "name": "expression-min-max-value-current",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(float value, float low) { return value < low ? low : value; }\nstatic inline float maximum_volume(float value, float high) { return value > high ? high : value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (attachment_exit_pending) {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = maximum_volume(minimum_volume(volume, 0.0f), 1.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        } else {\n            g_sound_effect_manager.Play(sound_id);\n        }\n    }\n}\n"
        },
        {
          "name": "expression-min-max-value-scoped-direct",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(float value, float low) { return value < low ? low : value; }\nstatic inline float maximum_volume(float value, float high) { return value > high ? high : value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (!attachment_exit_pending) goto direct_playback;\n        {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = maximum_volume(minimum_volume(volume, 0.0f), 1.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        }\n        return;\ndirect_playback:\n        g_sound_effect_manager.Play(sound_id);\n    }\n}\n"
        },
        {
          "name": "expression-min-max-reference-current",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(const float& value, float low) { return value < low ? low : value; }\nstatic inline float maximum_volume(const float& value, float high) { return value > high ? high : value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (attachment_exit_pending) {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = maximum_volume(minimum_volume(volume, 0.0f), 1.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        } else {\n            g_sound_effect_manager.Play(sound_id);\n        }\n    }\n}\n"
        },
        {
          "name": "expression-min-max-reference-scoped-direct",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(const float& value, float low) { return value < low ? low : value; }\nstatic inline float maximum_volume(const float& value, float high) { return value > high ? high : value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (!attachment_exit_pending) goto direct_playback;\n        {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = maximum_volume(minimum_volume(volume, 0.0f), 1.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        }\n        return;\ndirect_playback:\n        g_sound_effect_manager.Play(sound_id);\n    }\n}\n"
        },
        {
          "name": "expression-max-min-value-current",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(float value, float low) { return value < low ? low : value; }\nstatic inline float maximum_volume(float value, float high) { return value > high ? high : value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (attachment_exit_pending) {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = minimum_volume(maximum_volume(volume, 1.0f), 0.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        } else {\n            g_sound_effect_manager.Play(sound_id);\n        }\n    }\n}\n"
        },
        {
          "name": "expression-max-min-value-scoped-direct",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(float value, float low) { return value < low ? low : value; }\nstatic inline float maximum_volume(float value, float high) { return value > high ? high : value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (!attachment_exit_pending) goto direct_playback;\n        {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = minimum_volume(maximum_volume(volume, 1.0f), 0.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        }\n        return;\ndirect_playback:\n        g_sound_effect_manager.Play(sound_id);\n    }\n}\n"
        },
        {
          "name": "expression-max-min-reference-current",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(const float& value, float low) { return value < low ? low : value; }\nstatic inline float maximum_volume(const float& value, float high) { return value > high ? high : value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (attachment_exit_pending) {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = minimum_volume(maximum_volume(volume, 1.0f), 0.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        } else {\n            g_sound_effect_manager.Play(sound_id);\n        }\n    }\n}\n"
        },
        {
          "name": "expression-max-min-reference-scoped-direct",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(const float& value, float low) { return value < low ? low : value; }\nstatic inline float maximum_volume(const float& value, float high) { return value > high ? high : value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (!attachment_exit_pending) goto direct_playback;\n        {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = minimum_volume(maximum_volume(volume, 1.0f), 0.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        }\n        return;\ndirect_playback:\n        g_sound_effect_manager.Play(sound_id);\n    }\n}\n"
        },
        {
          "name": "branches-min-max-value-current",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(float value, float low) { if (value < low) return low; return value; }\nstatic inline float maximum_volume(float value, float high) { if (value > high) return high; return value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (attachment_exit_pending) {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = maximum_volume(minimum_volume(volume, 0.0f), 1.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        } else {\n            g_sound_effect_manager.Play(sound_id);\n        }\n    }\n}\n"
        },
        {
          "name": "branches-min-max-value-scoped-direct",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(float value, float low) { if (value < low) return low; return value; }\nstatic inline float maximum_volume(float value, float high) { if (value > high) return high; return value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (!attachment_exit_pending) goto direct_playback;\n        {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = maximum_volume(minimum_volume(volume, 0.0f), 1.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        }\n        return;\ndirect_playback:\n        g_sound_effect_manager.Play(sound_id);\n    }\n}\n"
        },
        {
          "name": "branches-min-max-reference-current",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(const float& value, float low) { if (value < low) return low; return value; }\nstatic inline float maximum_volume(const float& value, float high) { if (value > high) return high; return value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (attachment_exit_pending) {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = maximum_volume(minimum_volume(volume, 0.0f), 1.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        } else {\n            g_sound_effect_manager.Play(sound_id);\n        }\n    }\n}\n"
        },
        {
          "name": "branches-min-max-reference-scoped-direct",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(const float& value, float low) { if (value < low) return low; return value; }\nstatic inline float maximum_volume(const float& value, float high) { if (value > high) return high; return value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (!attachment_exit_pending) goto direct_playback;\n        {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = maximum_volume(minimum_volume(volume, 0.0f), 1.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        }\n        return;\ndirect_playback:\n        g_sound_effect_manager.Play(sound_id);\n    }\n}\n"
        },
        {
          "name": "branches-max-min-value-current",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(float value, float low) { if (value < low) return low; return value; }\nstatic inline float maximum_volume(float value, float high) { if (value > high) return high; return value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (attachment_exit_pending) {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = minimum_volume(maximum_volume(volume, 1.0f), 0.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        } else {\n            g_sound_effect_manager.Play(sound_id);\n        }\n    }\n}\n"
        },
        {
          "name": "branches-max-min-value-scoped-direct",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(float value, float low) { if (value < low) return low; return value; }\nstatic inline float maximum_volume(float value, float high) { if (value > high) return high; return value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (!attachment_exit_pending) goto direct_playback;\n        {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = minimum_volume(maximum_volume(volume, 1.0f), 0.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        }\n        return;\ndirect_playback:\n        g_sound_effect_manager.Play(sound_id);\n    }\n}\n"
        },
        {
          "name": "branches-max-min-reference-current",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(const float& value, float low) { if (value < low) return low; return value; }\nstatic inline float maximum_volume(const float& value, float high) { if (value > high) return high; return value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (attachment_exit_pending) {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = minimum_volume(maximum_volume(volume, 1.0f), 0.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        } else {\n            g_sound_effect_manager.Play(sound_id);\n        }\n    }\n}\n"
        },
        {
          "name": "branches-max-min-reference-scoped-direct",
          "text": "// play_subgoldy_shoot_sfx @ 0x43afd0, authored cRSubGoldy::PlayShootSfx()\n\n#include \"audio_system.h\"\n#include \"game_root.h\"\n#include \"player.h\"\n\n\nint gRMathRand2();\n\n\nstatic inline float minimum_volume(const float& value, float low) { if (value < low) return low; return value; }\nstatic inline float maximum_volume(const float& value, float high) { if (value > high) return high; return value; }\n\nvoid cRSubGoldy::PlayShootSfx()\n{\n    unsigned int flags = shoot_flags;\n    int sound_id;\n\n    if ((flags & 7) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 2.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 17;\n    } else if ((flags & 0x18) != 0) {\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 19;\n    } else {\n        if ((flags & 0x60) == 0)\n            return;\n        int variant = (int)(\n            (float)gRMathRand2() * 0.0000305175781f * 3.0f);\n        shoot_sfx_variant_sample = variant;\n        sound_id = variant + 22;\n    }\n\n    if (sound_id != -1) {\n        if (!attachment_exit_pending) goto direct_playback;\n        {\n            Vector3 vector;\n            Vector3 normalized_vector;\n\n            vector =\n                g_game->players[0].transform.position - transform.position;\n\n            normalized_vector = vector;\n            float distance = normalized_vector.Normalize();\n            float volume = distance / -60.0f + 1.0f;\n            volume = minimum_volume(maximum_volume(volume, 1.0f), 0.0f);\n            g_sound_effect_manager.PlayVolume(sound_id, volume);\n        }\n        return;\ndirect_playback:\n        g_sound_effect_manager.Play(sound_id);\n    }\n}\n"
        }
      ]
    }
  ]
}
