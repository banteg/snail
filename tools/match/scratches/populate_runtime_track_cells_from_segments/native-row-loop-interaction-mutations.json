{
  "schema": 1,
  "sites": [
    {
      "name": "native-cursor-initialization",
      "find": "    int segment_cursor = 0;\n    mode = level_mode;",
      "replacements": [
        {
          "name": "initialize-in-consuming-modes",
          "text": "    int segment_cursor;\n    mode = level_mode;"
        }
      ]
    },
    {
      "name": "native-inner-row-entry",
      "find": "        int segment_row = 0;\n        while (build_row < runtime_row_count) {\n            if (segment_row >= ((SubSegment*)active_segment)->row_count)\n                break;",
      "replacements": [
        {
          "name": "guarded-do-entry",
          "text": "        int segment_row = 0;\n        if (build_row < runtime_row_count) {\n            do {\n                if (segment_row >= ((SubSegment*)active_segment)->row_count)\n                    break;"
        }
      ]
    },
    {
      "name": "native-inner-row-exit",
      "find": "            ++segment_row;\n            ++build_row;\n        }\n        if (level_mode != 3 || first_or_last_row == 0) {",
      "replacements": [
        {
          "name": "guarded-do-exit",
          "text": "                ++segment_row;\n                ++build_row;\n            } while (build_row < runtime_row_count);\n        }\n        if (level_mode != 3 || first_or_last_row == 0) {"
        }
      ]
    },
    {
      "name": "native-parcel-set-source",
      "find": "                runtime_rows[build_row].parcel_set_id =\n                    *(int*)(\n                        authored_row_owner + SEGMENT_AUTHORED_ROWS_BASE\n                        + AUTHORED_ROW_PARCEL_SET_ID);",
      "replacements": [
        {
          "name": "indexed-active-segment-source",
          "text": "                runtime_rows[build_row].parcel_set_id =\n                    *(int*)(\n                        active_segment\n                        + sizeof(AuthoredSegmentRow) * segment_row\n                        + SEGMENT_AUTHORED_ROWS_BASE\n                        + AUTHORED_ROW_PARCEL_SET_ID);"
        }
      ]
    }
  ]
}
