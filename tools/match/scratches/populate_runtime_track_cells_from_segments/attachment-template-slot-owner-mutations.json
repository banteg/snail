{
  "schema": 1,
  "sites": [
    {
      "name": "attachment-template-slot-declaration",
      "find": "                case 'p': {\n                    cRSubLoc* runtime_cell = (cRSubLoc*)(cell + CELL_BOD_BASE);",
      "replacements": [
        {
          "name": "declare-raw-slot-owner",
          "text": "                case 'p': {\n                    cRSubLoc* runtime_cell = (cRSubLoc*)(cell + CELL_BOD_BASE);\n                    Path** attachment_template_slot = (Path**)(\n                        cell + CELL_BOD_BASE\n                        + offsetof(cRSubLoc, attachment_template_record));"
        }
      ]
    },
    {
      "name": "attachment-template-slot-store",
      "find": "                    if (base[TRACK_MIRROR_FLAG_OFFSET])\n                        runtime_cell->attachment_template_record = (Path*)(\n                            base + PATH_PAIRS_BASE + PATH_PAIR_SECONDARY_DELTA\n                            + template_index * sizeof(PathPair));\n                    else\n                        runtime_cell->attachment_template_record = (Path*)(\n                            base + PATH_PAIRS_BASE + template_index * sizeof(PathPair));",
      "replacements": [
        {
          "name": "store-through-raw-slot",
          "text": "                    if (base[TRACK_MIRROR_FLAG_OFFSET])\n                        *attachment_template_slot = (Path*)(\n                            base + PATH_PAIRS_BASE + PATH_PAIR_SECONDARY_DELTA\n                            + template_index * sizeof(PathPair));\n                    else\n                        *attachment_template_slot = (Path*)(\n                            base + PATH_PAIRS_BASE + template_index * sizeof(PathPair));"
        }
      ]
    },
    {
      "name": "attachment-template-slot-loads",
      "find": "                    if (attachment_entry_installed == 0) {\n                        attachment_entry_installed = 1;\n                        ((BodBase*)(cell + CELL_BOD_BASE))->SetObject(\n                            runtime_cell->attachment_template_record->object);\n                        *glyph_list_flags |= 0x20;\n                        runtime_rows[build_row].attachment_body.SetObject(\n                            runtime_cell->attachment_template_record\n                                ->fringe_mesh_bod.object);\n                        *(int*)(\n                            (char*)&runtime_rows[build_row]\n                            + ROW_ATTACHMENT_LIST_FLAGS) |= 0x20;\n                        runtime_rows[build_row].installed_heading_delta =\n                            *(float*)(active_segment + SEGMENT_ANGLE_RADIANS);\n\n                        SubRow* stamped_row = &runtime_rows[build_row];\n                        int span_index = 0;\n                        if (runtime_cell->attachment_template_record->row_span_count > 0) {\n                            do {\n                                int stamped_flags = stamped_row->flags;\n                                if ((stamped_flags & SUBROW_FLAG_PRIMARY_ATTACHMENT) != 0) {\n                                    stamped_row->flags =\n                                        stamped_flags | SUBROW_FLAG_SECONDARY_ATTACHMENT;\n                                    stamped_row->secondary_attachment_cell = runtime_cell;\n                                } else {\n                                    stamped_row->flags =\n                                        stamped_flags | SUBROW_FLAG_PRIMARY_ATTACHMENT;\n                                    stamped_row->primary_attachment_cell = runtime_cell;\n                                }\n                                ++span_index;\n                                ++stamped_row;\n                            } while (\n                                span_index\n                                < runtime_cell->attachment_template_record->row_span_count);\n                        }\n                    }",
      "replacements": [
        {
          "name": "load-through-raw-slot",
          "text": "                    if (attachment_entry_installed == 0) {\n                        attachment_entry_installed = 1;\n                        ((BodBase*)(cell + CELL_BOD_BASE))->SetObject(\n                            (*attachment_template_slot)->object);\n                        *glyph_list_flags |= 0x20;\n                        runtime_rows[build_row].attachment_body.SetObject(\n                            (*attachment_template_slot)->fringe_mesh_bod.object);\n                        *(int*)(\n                            (char*)&runtime_rows[build_row]\n                            + ROW_ATTACHMENT_LIST_FLAGS) |= 0x20;\n                        runtime_rows[build_row].installed_heading_delta =\n                            *(float*)(active_segment + SEGMENT_ANGLE_RADIANS);\n\n                        SubRow* stamped_row = &runtime_rows[build_row];\n                        int span_index = 0;\n                        if ((*attachment_template_slot)->row_span_count > 0) {\n                            do {\n                                int stamped_flags = stamped_row->flags;\n                                if ((stamped_flags & SUBROW_FLAG_PRIMARY_ATTACHMENT) != 0) {\n                                    stamped_row->flags =\n                                        stamped_flags | SUBROW_FLAG_SECONDARY_ATTACHMENT;\n                                    stamped_row->secondary_attachment_cell = runtime_cell;\n                                } else {\n                                    stamped_row->flags =\n                                        stamped_flags | SUBROW_FLAG_PRIMARY_ATTACHMENT;\n                                    stamped_row->primary_attachment_cell = runtime_cell;\n                                }\n                                ++span_index;\n                                ++stamped_row;\n                            } while (\n                                span_index\n                                < (*attachment_template_slot)->row_span_count);\n                        }\n                    }"
        }
      ]
    }
  ]
}
