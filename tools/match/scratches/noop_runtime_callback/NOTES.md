# noop_runtime_callback

Shared no-op runtime callback at 0x40a290. Both BN and IDA decompiles show it
ignoring the single argument and returning immediately.

Exact match: 100.00%, 1/1 instruction, no masked operands.
