{
  "schema": 1,
  "sites": [
    {
      "name": "mesh-lateral-owner",
      "find": "            TransformMatrix* transform = &primary_samples[0].transform;\n            if (row != segment_count) {\n                transform = (TransformMatrix*)((char*)transform + mesh_cursor);\n                Vector3 lateral_offset = transform->basis_right\n                    * ((float)column - (float)width_cells * 0.5f);\n                Vector3 generated_position(\n                    transform->position.x + lateral_offset.x,\n                    transform->position.y + lateral_offset.y,\n                    transform->position.z + lateral_offset.z);\n                Vector3* vertex =\n                    &vertices[column + row * (width_cells + 1)];\n                *vertex = generated_position;\n            } else {\n                transform = (TransformMatrix*)((char*)transform + mesh_cursor);\n                TransformMatrix* previous =\n                    (TransformMatrix*)((char*)transform - sizeof(AttachmentSample));\n                Vector3 lateral_offset = previous->basis_right\n                    * ((float)column - (float)width_cells * 0.5f);\n                Vector3 endpoint(\n                    previous->position.x,\n                    previous->position.y,\n                    previous->position.z + 1.0f);\n                Vector3 generated_position(\n                    endpoint.x + lateral_offset.x,\n                    endpoint.y + lateral_offset.y,\n                    endpoint.z + lateral_offset.z);\n                Vector3* vertex =\n                    &vertices[column + row * (width_cells + 1)];\n                *vertex = generated_position;\n            }",
      "replacements": [
        {
          "name": "shared-float",
          "text": "            float lateral =\n                (float)column - (float)width_cells * 0.5f;\n            TransformMatrix* transform = &primary_samples[0].transform;\n            if (row != segment_count) {\n                transform = (TransformMatrix*)((char*)transform + mesh_cursor);\n                Vector3 lateral_offset = transform->basis_right * lateral;\n                Vector3 generated_position(\n                    transform->position.x + lateral_offset.x,\n                    transform->position.y + lateral_offset.y,\n                    transform->position.z + lateral_offset.z);\n                Vector3* vertex =\n                    &vertices[column + row * (width_cells + 1)];\n                *vertex = generated_position;\n            } else {\n                transform = (TransformMatrix*)((char*)transform + mesh_cursor);\n                TransformMatrix* previous =\n                    (TransformMatrix*)((char*)transform - sizeof(AttachmentSample));\n                Vector3 lateral_offset = previous->basis_right * lateral;\n                Vector3 endpoint(\n                    previous->position.x,\n                    previous->position.y,\n                    previous->position.z + 1.0f);\n                Vector3 generated_position(\n                    endpoint.x + lateral_offset.x,\n                    endpoint.y + lateral_offset.y,\n                    endpoint.z + lateral_offset.z);\n                Vector3* vertex =\n                    &vertices[column + row * (width_cells + 1)];\n                *vertex = generated_position;\n            }"
        },
        {
          "name": "shared-double",
          "text": "            double lateral =\n                (double)column - (double)width_cells * 0.5;\n            TransformMatrix* transform = &primary_samples[0].transform;\n            if (row != segment_count) {\n                transform = (TransformMatrix*)((char*)transform + mesh_cursor);\n                Vector3 lateral_offset = transform->basis_right * (float)lateral;\n                Vector3 generated_position(\n                    transform->position.x + lateral_offset.x,\n                    transform->position.y + lateral_offset.y,\n                    transform->position.z + lateral_offset.z);\n                Vector3* vertex =\n                    &vertices[column + row * (width_cells + 1)];\n                *vertex = generated_position;\n            } else {\n                transform = (TransformMatrix*)((char*)transform + mesh_cursor);\n                TransformMatrix* previous =\n                    (TransformMatrix*)((char*)transform - sizeof(AttachmentSample));\n                Vector3 lateral_offset = previous->basis_right * (float)lateral;\n                Vector3 endpoint(\n                    previous->position.x,\n                    previous->position.y,\n                    previous->position.z + 1.0f);\n                Vector3 generated_position(\n                    endpoint.x + lateral_offset.x,\n                    endpoint.y + lateral_offset.y,\n                    endpoint.z + lateral_offset.z);\n                Vector3* vertex =\n                    &vertices[column + row * (width_cells + 1)];\n                *vertex = generated_position;\n            }"
        },
        {
          "name": "shared-float-no-explicit-casts",
          "text": "            float lateral = column - width_cells * 0.5f;\n            TransformMatrix* transform = &primary_samples[0].transform;\n            if (row != segment_count) {\n                transform = (TransformMatrix*)((char*)transform + mesh_cursor);\n                Vector3 lateral_offset = transform->basis_right * lateral;\n                Vector3 generated_position(\n                    transform->position.x + lateral_offset.x,\n                    transform->position.y + lateral_offset.y,\n                    transform->position.z + lateral_offset.z);\n                Vector3* vertex =\n                    &vertices[column + row * (width_cells + 1)];\n                *vertex = generated_position;\n            } else {\n                transform = (TransformMatrix*)((char*)transform + mesh_cursor);\n                TransformMatrix* previous =\n                    (TransformMatrix*)((char*)transform - sizeof(AttachmentSample));\n                Vector3 lateral_offset = previous->basis_right * lateral;\n                Vector3 endpoint(\n                    previous->position.x,\n                    previous->position.y,\n                    previous->position.z + 1.0f);\n                Vector3 generated_position(\n                    endpoint.x + lateral_offset.x,\n                    endpoint.y + lateral_offset.y,\n                    endpoint.z + lateral_offset.z);\n                Vector3* vertex =\n                    &vertices[column + row * (width_cells + 1)];\n                *vertex = generated_position;\n            }"
        }
      ]
    }
  ]
}
