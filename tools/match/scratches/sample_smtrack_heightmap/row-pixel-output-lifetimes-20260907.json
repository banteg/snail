{
  "schema": 1,
  "sites": [
    {
      "name": "heightmap-owners",
      "find": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float red = (float)pixel[2];\n                float green = (float)pixel[1];\n                float blue = (float)pixel[0];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n",
      "replacements": [
        {
          "name": "outside-before-float-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float red = (float)pixel[20];\n                float green = (float)pixel[19];\n                float blue = (float)pixel[18];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-before-red-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[2] + (float)pixel[1] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-before-red-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[20] + (float)pixel[19] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-before-green-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[1] + (float)pixel[2] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-before-green-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[19] + (float)pixel[20] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-before-byte-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                unsigned char green = pixel[1];\n                unsigned char red = pixel[2];\n                unsigned char blue = pixel[0];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-before-byte-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                unsigned char green = pixel[19];\n                unsigned char red = pixel[20];\n                unsigned char blue = pixel[18];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-after-float-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float red = (float)pixel[2];\n                float green = (float)pixel[1];\n                float blue = (float)pixel[0];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-after-float-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float red = (float)pixel[20];\n                float green = (float)pixel[19];\n                float blue = (float)pixel[18];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-after-red-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[2] + (float)pixel[1] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-after-red-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[20] + (float)pixel[19] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-after-green-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[1] + (float)pixel[2] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-after-green-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[19] + (float)pixel[20] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-after-byte-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                unsigned char green = pixel[1];\n                unsigned char red = pixel[2];\n                unsigned char blue = pixel[0];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-after-byte-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                unsigned char green = pixel[19];\n                unsigned char red = pixel[20];\n                unsigned char blue = pixel[18];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-post-float-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float red = (float)pixel[2];\n                float green = (float)pixel[1];\n                float blue = (float)pixel[0];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-post-float-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float red = (float)pixel[20];\n                float green = (float)pixel[19];\n                float blue = (float)pixel[18];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-post-red-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[2] + (float)pixel[1] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-post-red-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[20] + (float)pixel[19] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-post-green-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[1] + (float)pixel[2] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-post-green-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[19] + (float)pixel[20] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-post-byte-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                unsigned char green = pixel[1];\n                unsigned char red = pixel[2];\n                unsigned char blue = pixel[0];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-post-byte-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                unsigned char green = pixel[19];\n                unsigned char red = pixel[20];\n                unsigned char blue = pixel[18];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-named-float-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float red = (float)pixel[2];\n                float green = (float)pixel[1];\n                float blue = (float)pixel[0];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-named-float-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float red = (float)pixel[20];\n                float green = (float)pixel[19];\n                float blue = (float)pixel[18];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-named-red-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[2] + (float)pixel[1] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-named-red-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[20] + (float)pixel[19] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-named-green-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[1] + (float)pixel[2] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-named-green-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[19] + (float)pixel[20] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-named-byte-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                unsigned char green = pixel[1];\n                unsigned char red = pixel[2];\n                unsigned char blue = pixel[0];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "outside-named-byte-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            int row_base = (image->height - y - 1) * image->width;\n            do {\n                int x = (int)(column * x_step);\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                unsigned char green = pixel[19];\n                unsigned char red = pixel[20];\n                unsigned char blue = pixel[18];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-before-float-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float red = (float)pixel[2];\n                float green = (float)pixel[1];\n                float blue = (float)pixel[0];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-before-float-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float red = (float)pixel[20];\n                float green = (float)pixel[19];\n                float blue = (float)pixel[18];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-before-red-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[2] + (float)pixel[1] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-before-red-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[20] + (float)pixel[19] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-before-green-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[1] + (float)pixel[2] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-before-green-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[19] + (float)pixel[20] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-before-byte-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                unsigned char green = pixel[1];\n                unsigned char red = pixel[2];\n                unsigned char blue = pixel[0];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-before-byte-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                unsigned char green = pixel[19];\n                unsigned char red = pixel[20];\n                unsigned char blue = pixel[18];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                ++sample;\n                sample[-1].y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-after-float-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float red = (float)pixel[2];\n                float green = (float)pixel[1];\n                float blue = (float)pixel[0];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-after-float-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float red = (float)pixel[20];\n                float green = (float)pixel[19];\n                float blue = (float)pixel[18];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-after-red-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[2] + (float)pixel[1] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-after-red-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[20] + (float)pixel[19] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-after-green-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[1] + (float)pixel[2] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-after-green-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[19] + (float)pixel[20] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-after-byte-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                unsigned char green = pixel[1];\n                unsigned char red = pixel[2];\n                unsigned char blue = pixel[0];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-after-byte-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                unsigned char green = pixel[19];\n                unsigned char red = pixel[20];\n                unsigned char blue = pixel[18];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                sample->y = value * scale + base;\n                ++sample;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-post-float-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float red = (float)pixel[2];\n                float green = (float)pixel[1];\n                float blue = (float)pixel[0];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-post-float-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float red = (float)pixel[20];\n                float green = (float)pixel[19];\n                float blue = (float)pixel[18];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-post-red-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[2] + (float)pixel[1] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-post-red-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[20] + (float)pixel[19] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-post-green-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[1] + (float)pixel[2] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-post-green-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[19] + (float)pixel[20] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-post-byte-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                unsigned char green = pixel[1];\n                unsigned char red = pixel[2];\n                unsigned char blue = pixel[0];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-post-byte-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                unsigned char green = pixel[19];\n                unsigned char red = pixel[20];\n                unsigned char blue = pixel[18];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                (sample++)->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-named-float-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float red = (float)pixel[2];\n                float green = (float)pixel[1];\n                float blue = (float)pixel[0];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-named-float-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float red = (float)pixel[20];\n                float green = (float)pixel[19];\n                float blue = (float)pixel[18];\n                float value = red;\n                value += green;\n                value += blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-named-red-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[2] + (float)pixel[1] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-named-red-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[20] + (float)pixel[19] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-named-green-first-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                float value = (float)pixel[1] + (float)pixel[2] + (float)pixel[0];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-named-green-first-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                float value = (float)pixel[19] + (float)pixel[20] + (float)pixel[18];\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-named-byte-locals-payload",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = image->pixels + pixel_index;\n                unsigned char green = pixel[1];\n                unsigned char red = pixel[2];\n                unsigned char blue = pixel[0];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        },
        {
          "name": "inside-named-byte-locals-image-base",
          "text": "// ObjectProcLandScapeUpdate @ 0x41a360 (cdecl)\n\n#include \"smtracks.h\"\n#include \"tga_image_view.h\"\n\nvoid __cdecl ObjectProcLandScapeUpdate(\n    Object* source,\n    float base,\n    float scale,\n    cRTexture* replacement,\n    bool cubic)\n{\n    TgaImageView* image = (TgaImageView*)replacement->texture_ref;\n    float sample_count_float = (float)source->heightmap_sample_count;\n    int row_count =\n        (int)(sample_count_float * source->heightmap_sample_scale /\n            source->heightmap_sample_divisor);\n    float row_count_float = (float)row_count;\n    float x_step = (float)image->width / (sample_count_float + 1.0f);\n    float y_step = (float)image->height / (row_count_float + 1.0f);\n    Vector3* sample = source->vertices;\n\n    for (float row = 0.0f; row <= row_count_float; row += 1.0f) {\n        float column = 0.0f;\n        if (column <= sample_count_float) {\n            int y = (int)(row * y_step);\n            do {\n                int x = (int)(column * x_step);\n                int row_base = (image->height - y - 1) * image->width;\n                int pixel_index =\n                    (row_base + x)\n                    * (image->bits_per_pixel >> 3);\n                unsigned char* pixel = (unsigned char*)image + pixel_index;\n                unsigned char green = pixel[19];\n                unsigned char red = pixel[20];\n                unsigned char blue = pixel[18];\n                float value = (float)red + (float)green + (float)blue;\n                value *= 0.00392156886f;\n                value *= 0.333333343f;\n\n                if (cubic)\n                    value = value * value * value;\n\n                Vector3* output = sample++;\n                output->y = value * scale + base;\n                column += 1.0f;\n            } while (column <= sample_count_float);\n        }\n    }\n}\n"
        }
      ]
    }
  ]
}
