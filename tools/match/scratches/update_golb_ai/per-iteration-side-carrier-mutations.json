{
  "schema": 1,
  "sites": [
    {
      "name": "direct-garbage-per-iteration-side-carrier",
      "find": "            SubGarbage* garbage = game->garbage_hazards.active_head;\n            while (garbage) {\n                if (garbage->state == SUB_GARBAGE_STATE_ACTIVE) {\n                    Vec3 collision_delta;\n                    collision_delta.x =\n                        garbage->transform.position.x - new_output->x;\n                    collision_delta.y =\n                        garbage->transform.position.y - new_output->y;\n                    float dz =\n                        garbage->transform.position.z - new_output->z;\n                    collision_delta.z = dz;\n                    probe = collision_delta;\n                    if (dz < 0.0f)\n                        dz = -dz;\n                    if (dz < 3.0f\n                        && normalize_vector(&probe) < garbage->radius + 0.49000001f) {\n                        garbage->state = SUB_GARBAGE_STATE_BURST_PENDING;\n                        if (probe.x < 0.0f)\n                            garbage->collision_side =\n                                SUB_GARBAGE_COLLISION_SIDE_LEFT;\n                        else\n                            garbage->collision_side =\n                                SUB_GARBAGE_COLLISION_SIDE_RIGHT;\n                        player->ScoreAdd(SUBGOLDY_SCORE_GARBAGE, 0);\n                        if (kind != 1)\n                            goto garbage_hit;\n                    }\n                }\n                garbage = garbage->next_active;\n            }",
      "replacements": [
        {
          "name": "reset-at-loop-top-literal-kind",
          "text": "            SubGarbage* garbage = game->garbage_hazards.active_head;\n            SubGarbageCollisionSide direct_collision_side;\n            while (garbage) {\n                direct_collision_side = SUB_GARBAGE_COLLISION_SIDE_LEFT;\n                if (garbage->state == SUB_GARBAGE_STATE_ACTIVE) {\n                    Vec3 collision_delta;\n                    collision_delta.x =\n                        garbage->transform.position.x - new_output->x;\n                    collision_delta.y =\n                        garbage->transform.position.y - new_output->y;\n                    float dz =\n                        garbage->transform.position.z - new_output->z;\n                    collision_delta.z = dz;\n                    probe = collision_delta;\n                    if (dz < 0.0f)\n                        dz = -dz;\n                    if (dz < 3.0f\n                        && normalize_vector(&probe) < garbage->radius + 0.49000001f) {\n                        garbage->state = SUB_GARBAGE_STATE_BURST_PENDING;\n                        if (probe.x < 0.0f) {\n                            garbage->collision_side = direct_collision_side;\n                            direct_collision_side =\n                                SUB_GARBAGE_COLLISION_SIDE_RIGHT;\n                        } else {\n                            direct_collision_side =\n                                SUB_GARBAGE_COLLISION_SIDE_RIGHT;\n                            garbage->collision_side = direct_collision_side;\n                        }\n                        player->ScoreAdd(SUBGOLDY_SCORE_GARBAGE, 0);\n                        if (kind != 1)\n                            goto garbage_hit;\n                    }\n                }\n                garbage = garbage->next_active;\n            }"
        },
        {
          "name": "reset-at-loop-top-carried-kind",
          "text": "            SubGarbage* garbage = game->garbage_hazards.active_head;\n            SubGarbageCollisionSide direct_collision_side;\n            while (garbage) {\n                direct_collision_side = SUB_GARBAGE_COLLISION_SIDE_LEFT;\n                if (garbage->state == SUB_GARBAGE_STATE_ACTIVE) {\n                    Vec3 collision_delta;\n                    collision_delta.x =\n                        garbage->transform.position.x - new_output->x;\n                    collision_delta.y =\n                        garbage->transform.position.y - new_output->y;\n                    float dz =\n                        garbage->transform.position.z - new_output->z;\n                    collision_delta.z = dz;\n                    probe = collision_delta;\n                    if (dz < 0.0f)\n                        dz = -dz;\n                    if (dz < 3.0f\n                        && normalize_vector(&probe) < garbage->radius + 0.49000001f) {\n                        garbage->state = SUB_GARBAGE_STATE_BURST_PENDING;\n                        if (probe.x < 0.0f) {\n                            garbage->collision_side = direct_collision_side;\n                            direct_collision_side =\n                                SUB_GARBAGE_COLLISION_SIDE_RIGHT;\n                        } else {\n                            direct_collision_side =\n                                SUB_GARBAGE_COLLISION_SIDE_RIGHT;\n                            garbage->collision_side = direct_collision_side;\n                        }\n                        player->ScoreAdd(SUBGOLDY_SCORE_GARBAGE, 0);\n                        if (kind != (int)direct_collision_side)\n                            goto garbage_hit;\n                    }\n                }\n                garbage = garbage->next_active;\n            }"
        },
        {
          "name": "reset-inside-active-gate-carried-kind",
          "text": "            SubGarbage* garbage = game->garbage_hazards.active_head;\n            SubGarbageCollisionSide direct_collision_side;\n            while (garbage) {\n                if (garbage->state == SUB_GARBAGE_STATE_ACTIVE) {\n                    direct_collision_side = SUB_GARBAGE_COLLISION_SIDE_LEFT;\n                    Vec3 collision_delta;\n                    collision_delta.x =\n                        garbage->transform.position.x - new_output->x;\n                    collision_delta.y =\n                        garbage->transform.position.y - new_output->y;\n                    float dz =\n                        garbage->transform.position.z - new_output->z;\n                    collision_delta.z = dz;\n                    probe = collision_delta;\n                    if (dz < 0.0f)\n                        dz = -dz;\n                    if (dz < 3.0f\n                        && normalize_vector(&probe) < garbage->radius + 0.49000001f) {\n                        garbage->state = SUB_GARBAGE_STATE_BURST_PENDING;\n                        if (probe.x < 0.0f) {\n                            garbage->collision_side = direct_collision_side;\n                            direct_collision_side =\n                                SUB_GARBAGE_COLLISION_SIDE_RIGHT;\n                        } else {\n                            direct_collision_side =\n                                SUB_GARBAGE_COLLISION_SIDE_RIGHT;\n                            garbage->collision_side = direct_collision_side;\n                        }\n                        player->ScoreAdd(SUBGOLDY_SCORE_GARBAGE, 0);\n                        if (kind != (int)direct_collision_side)\n                            goto garbage_hit;\n                    }\n                }\n                garbage = garbage->next_active;\n            }"
        }
      ]
    }
  ]
}
