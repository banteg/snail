# `update_slug_hazard_ai` match result

Exact focused scratch for `Slug::update_slug_hazard_ai @ 0x43f930`.

Final focused run:

```text
match: 100.00%
target: 464 insns, candidate: 464 insns
prefix: 464/464 target insns
masked operands: 71 ok, 0 unresolved, 0 mismatch
```

The exact source recovers the active-only contact registration, the shared
active/lateral presentation tail, the three native inlined active-list removal
sites, the state-2-to-state-3 teardown fallthrough, and the by-value `Vector3`
velocity construction. Direction correction reuses the independently exact
`SubGarbage` hazard idiom, including the unknown-direction skip and explicit
zero classification. No dummy locals, padding, volatile accesses, or other
fakematching are used.
