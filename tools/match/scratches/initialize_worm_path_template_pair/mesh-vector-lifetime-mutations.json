{
  "schema": 1,
  "sites": [
    {
      "name": "ordinary-mesh-vertex-lifetime",
      "find": "                        float radius = primary_samples[row].lateral_scale;\n                        radius *= WORM_RADIUS;\n\n                        Vector3 up_radius =\n                            radius * primary_samples[row].transform.basis_up;\n                        float column_as_float = (float)column;\n                        Vector3 up_component =\n                            cosine(column_as_float / (float)width_cells * WORM_TAU) *\n                                up_radius;\n                        Vector3 base_plus_right =\n                            primary_samples[row].transform.position +\n                            sine(column_as_float / (float)width_cells * WORM_TAU) *\n                                (radius * primary_samples[row].transform.basis_right);\n                        Vector3 vertex = base_plus_right + up_component;\n                        vertices[column + row * width_cells] = vertex;",
      "replacements": [
        {
          "name": "explicit-right-temporaries",
          "text": "                        float radius = primary_samples[row].lateral_scale;\n                        radius *= WORM_RADIUS;\n\n                        Vector3 up_radius =\n                            radius * primary_samples[row].transform.basis_up;\n                        float column_as_float = (float)column;\n                        Vector3 up_component =\n                            cosine(column_as_float / (float)width_cells * WORM_TAU) *\n                                up_radius;\n                        Vector3 right_radius =\n                            radius * primary_samples[row].transform.basis_right;\n                        Vector3 right_component =\n                            sine(column_as_float / (float)width_cells * WORM_TAU) *\n                                right_radius;\n                        Vector3 base_plus_right =\n                            primary_samples[row].transform.position + right_component;\n                        Vector3 vertex = base_plus_right + up_component;\n                        vertices[column + row * width_cells] = vertex;"
        },
        {
          "name": "in-place-vertex-additions",
          "text": "                        float radius = primary_samples[row].lateral_scale;\n                        radius *= WORM_RADIUS;\n\n                        Vector3 up_radius =\n                            radius * primary_samples[row].transform.basis_up;\n                        float column_as_float = (float)column;\n                        Vector3 up_component =\n                            cosine(column_as_float / (float)width_cells * WORM_TAU) *\n                                up_radius;\n                        Vector3 right_component =\n                            sine(column_as_float / (float)width_cells * WORM_TAU) *\n                                (radius * primary_samples[row].transform.basis_right);\n                        Vector3 vertex = primary_samples[row].transform.position;\n                        vertex += right_component;\n                        vertex += up_component;\n                        vertices[column + row * width_cells] = vertex;"
        },
        {
          "name": "in-place-base-addition",
          "text": "                        float radius = primary_samples[row].lateral_scale;\n                        radius *= WORM_RADIUS;\n\n                        Vector3 up_radius =\n                            radius * primary_samples[row].transform.basis_up;\n                        float column_as_float = (float)column;\n                        Vector3 up_component =\n                            cosine(column_as_float / (float)width_cells * WORM_TAU) *\n                                up_radius;\n                        Vector3 base_plus_right =\n                            primary_samples[row].transform.position +\n                            sine(column_as_float / (float)width_cells * WORM_TAU) *\n                                (radius * primary_samples[row].transform.basis_right);\n                        base_plus_right += up_component;\n                        vertices[column + row * width_cells] = base_plus_right;"
        },
        {
          "name": "component-final-vector",
          "text": "                        float radius = primary_samples[row].lateral_scale;\n                        radius *= WORM_RADIUS;\n\n                        Vector3 up_radius =\n                            radius * primary_samples[row].transform.basis_up;\n                        float column_as_float = (float)column;\n                        Vector3 up_component =\n                            cosine(column_as_float / (float)width_cells * WORM_TAU) *\n                                up_radius;\n                        Vector3 base_plus_right =\n                            primary_samples[row].transform.position +\n                            sine(column_as_float / (float)width_cells * WORM_TAU) *\n                                (radius * primary_samples[row].transform.basis_right);\n                        Vector3 vertex;\n                        vertex.x = base_plus_right.x + up_component.x;\n                        vertex.y = base_plus_right.y + up_component.y;\n                        vertex.z = base_plus_right.z + up_component.z;\n                        vertices[column + row * width_cells] = vertex;"
        },
        {
          "name": "direct-destination-components",
          "text": "                        float radius = primary_samples[row].lateral_scale;\n                        radius *= WORM_RADIUS;\n\n                        Vector3 up_radius =\n                            radius * primary_samples[row].transform.basis_up;\n                        float column_as_float = (float)column;\n                        Vector3 up_component =\n                            cosine(column_as_float / (float)width_cells * WORM_TAU) *\n                                up_radius;\n                        Vector3 base_plus_right =\n                            primary_samples[row].transform.position +\n                            sine(column_as_float / (float)width_cells * WORM_TAU) *\n                                (radius * primary_samples[row].transform.basis_right);\n                        Vector3* vertex =\n                            &vertices[column + row * width_cells];\n                        vertex->x = base_plus_right.x + up_component.x;\n                        vertex->y = base_plus_right.y + up_component.y;\n                        vertex->z = base_plus_right.z + up_component.z;"
        },
        {
          "name": "copied-position",
          "text": "                        float radius = primary_samples[row].lateral_scale;\n                        radius *= WORM_RADIUS;\n\n                        Vector3 up_radius =\n                            radius * primary_samples[row].transform.basis_up;\n                        float column_as_float = (float)column;\n                        Vector3 up_component =\n                            cosine(column_as_float / (float)width_cells * WORM_TAU) *\n                                up_radius;\n                        Vector3 position =\n                            primary_samples[row].transform.position;\n                        Vector3 base_plus_right =\n                            position +\n                            sine(column_as_float / (float)width_cells * WORM_TAU) *\n                                (radius * primary_samples[row].transform.basis_right);\n                        Vector3 vertex = base_plus_right + up_component;\n                        vertices[column + row * width_cells] = vertex;"
        },
        {
          "name": "shared-column-angle",
          "text": "                        float radius = primary_samples[row].lateral_scale;\n                        radius *= WORM_RADIUS;\n\n                        Vector3 up_radius =\n                            radius * primary_samples[row].transform.basis_up;\n                        float column_as_float = (float)column;\n                        float column_angle =\n                            column_as_float / (float)width_cells * WORM_TAU;\n                        Vector3 up_component = cosine(column_angle) * up_radius;\n                        Vector3 base_plus_right =\n                            primary_samples[row].transform.position +\n                            sine(column_angle) *\n                                (radius * primary_samples[row].transform.basis_right);\n                        Vector3 vertex = base_plus_right + up_component;\n                        vertices[column + row * width_cells] = vertex;"
        }
      ]
    }
  ]
}
