# start_squidge_y @ 0x444980

Exact authored `cRSquidge::StartY(float)` member. Live Windows callsites pass the
inline `cRSubGoldy + 0x4344` owner from one track-attachment helper and three
branches of `cRSubGoldy::AI()`. Android and iOS independently expose the same
class-qualified symbol and both preserve a method body.

The scratch exports `?StartY@cRSquidge@@QAEXM@Z` and remains an exact 5/5
instruction match with one clean masked relocation.
