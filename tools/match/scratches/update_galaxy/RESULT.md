# `update_galaxy` exact match

`cRGalaxy::AI()` at **0x4092f0** matches all **566/566 instructions** and
**2051 native bytes**, with a **566-instruction prefix** and **55 clean
references** (zero unresolved, mismatched, or unaudited operands).

Verified with the canonical `msvc6.5 /O2 /G5 /W3` profile on 2026-09-05.
The complete native frame is `0x3c`. No compiler flags, shared arithmetic
implementations, or reference-audit rules were changed.

The recovery separates the panel-highlight and hit-test counters, retains a
zero-z mouse vector and one reusable normalization probe, and indexes route
records through their owned arrays. The hit-test counter begins immediately
before its loop. Click decisions use a game borrow established after probing,
fresh input-pointer reads, and the live hover-state field.

Those coupled lifetimes recover the rendering and input stack slots, loop
schedules, mask tests, and final exits. Earlier negative isolated owner and
stack probes did not establish an irreducible compiler residual. See NOTES.md
and the append-only experiment ledger for the measured progression.
