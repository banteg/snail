{
  "schema": 1,
  "sites": [
    {
      "name": "delta-loop-control",
      "find": "    int delta_offset = 0;\n    if (segment_count - 1 > 0) {\n        for (i = 0; i < segment_count - 1;\n             ++i, delta_offset += (int)sizeof(PathAttachmentSample)) {\n            ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                ->delta_dir_to_next =\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset) + 1)\n                    ->transform.position -\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                    ->transform.position;\n            ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                ->delta_length =\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                    ->delta_dir_to_next.Normalize();\n\n            ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                ->delta_dir_to_next =\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset) + 1)\n                    ->transform.position -\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                    ->transform.position;\n            ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                ->delta_length =\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                    ->delta_dir_to_next.Normalize();\n        }\n    }",
      "replacements": [
        {
          "name": "physical-before-shared-do",
          "text": "    int delta_offset = 0;\n    if (segment_count - 1 > 0) {\n        i = 0;\n        do {\n            ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                ->delta_dir_to_next =\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset) + 1)\n                    ->transform.position -\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                    ->transform.position;\n            ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                ->delta_length =\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                    ->delta_dir_to_next.Normalize();\n\n            ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                ->delta_dir_to_next =\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset) + 1)\n                    ->transform.position -\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                    ->transform.position;\n            ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                ->delta_length =\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                    ->delta_dir_to_next.Normalize();\n\n            ++i;\n            delta_offset += (int)sizeof(PathAttachmentSample);\n        } while (i < segment_count - 1);\n    }"
        },
        {
          "name": "both-inside-shared-do",
          "text": "    if (segment_count - 1 > 0) {\n        i = 0;\n        int delta_offset = 0;\n        do {\n            ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                ->delta_dir_to_next =\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset) + 1)\n                    ->transform.position -\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                    ->transform.position;\n            ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                ->delta_length =\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                    ->delta_dir_to_next.Normalize();\n\n            ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                ->delta_dir_to_next =\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset) + 1)\n                    ->transform.position -\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                    ->transform.position;\n            ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                ->delta_length =\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                    ->delta_dir_to_next.Normalize();\n\n            ++i;\n            delta_offset += (int)sizeof(PathAttachmentSample);\n        } while (i < segment_count - 1);\n    }"
        },
        {
          "name": "logical-before-shared-do",
          "text": "    i = 0;\n    if (segment_count - 1 > 0) {\n        int delta_offset = 0;\n        do {\n            ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                ->delta_dir_to_next =\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset) + 1)\n                    ->transform.position -\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                    ->transform.position;\n            ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                ->delta_length =\n                ((PathAttachmentSample*)((char*)primary_samples + delta_offset))\n                    ->delta_dir_to_next.Normalize();\n\n            ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                ->delta_dir_to_next =\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset) + 1)\n                    ->transform.position -\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                    ->transform.position;\n            ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                ->delta_length =\n                ((PathAttachmentSample*)((char*)secondary_samples + delta_offset))\n                    ->delta_dir_to_next.Normalize();\n\n            ++i;\n            delta_offset += (int)sizeof(PathAttachmentSample);\n        } while (i < segment_count - 1);\n    }"
        }
      ]
    }
  ]
}
