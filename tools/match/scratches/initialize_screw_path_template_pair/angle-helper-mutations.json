{
  "schema": 1,
  "sites": [
    {
      "name": "screw-angle-helper",
      "find": "static inline void orient_previous_with_fixed_up(\n    PathAttachmentSample* previous,\n    PathAttachmentSample* next,\n    float up_x,\n    float up_y,\n    float up_z,\n    float roll)\n{\n    previous->transform.basis_up = Vector3(up_x, up_y, up_z);\n    previous->transform.basis_forward = Vector3(\n        next->transform.position.x - previous->transform.position.x,\n        next->transform.position.y - previous->transform.position.y,\n        next->transform.position.z - previous->transform.position.z);\n    previous->transform.basis_forward.Normalize();\n    previous->transform.basis_right.cross_vectors(\n        &previous->transform.basis_up,\n        &previous->transform.basis_forward);\n    if (roll != 0.0f)\n        previous->transform.RotLocalZ(roll);\n}",
      "replacements": [
        {
          "name": "add-local-angle-helper",
          "text": "static inline void orient_previous_with_fixed_up(\n    PathAttachmentSample* previous,\n    PathAttachmentSample* next,\n    float up_x,\n    float up_y,\n    float up_z,\n    float roll)\n{\n    previous->transform.basis_up = Vector3(up_x, up_y, up_z);\n    previous->transform.basis_forward = Vector3(\n        next->transform.position.x - previous->transform.position.x,\n        next->transform.position.y - previous->transform.position.y,\n        next->transform.position.z - previous->transform.position.z);\n    previous->transform.basis_forward.Normalize();\n    previous->transform.basis_right.cross_vectors(\n        &previous->transform.basis_up,\n        &previous->transform.basis_forward);\n    if (roll != 0.0f)\n        previous->transform.RotLocalZ(roll);\n}\n\nstatic inline void orient_previous_from_screw_angle(\n    PathAttachmentSample* previous,\n    PathAttachmentSample* next,\n    float angle)\n{\n    float up_y = cosine(angle);\n    float up_x = -sine(angle);\n    previous->transform.basis_up = Vector3(up_x, up_y, 0.0f);\n    previous->transform.basis_forward = Vector3(\n        next->transform.position.x - previous->transform.position.x,\n        next->transform.position.y - previous->transform.position.y,\n        next->transform.position.z - previous->transform.position.z);\n    previous->transform.basis_forward.Normalize();\n    previous->transform.basis_right.cross_vectors(\n        &previous->transform.basis_up,\n        &previous->transform.basis_forward);\n}"
        },
        {
          "name": "add-direct-angle-helper",
          "text": "static inline void orient_previous_with_fixed_up(\n    PathAttachmentSample* previous,\n    PathAttachmentSample* next,\n    float up_x,\n    float up_y,\n    float up_z,\n    float roll)\n{\n    previous->transform.basis_up = Vector3(up_x, up_y, up_z);\n    previous->transform.basis_forward = Vector3(\n        next->transform.position.x - previous->transform.position.x,\n        next->transform.position.y - previous->transform.position.y,\n        next->transform.position.z - previous->transform.position.z);\n    previous->transform.basis_forward.Normalize();\n    previous->transform.basis_right.cross_vectors(\n        &previous->transform.basis_up,\n        &previous->transform.basis_forward);\n    if (roll != 0.0f)\n        previous->transform.RotLocalZ(roll);\n}\n\nstatic __forceinline void orient_previous_from_screw_angle(\n    PathAttachmentSample* previous,\n    PathAttachmentSample* next,\n    float angle)\n{\n    previous->transform.basis_up =\n        Vector3(-sine(angle), cosine(angle), 0.0f);\n    previous->transform.basis_forward = Vector3(\n        next->transform.position.x - previous->transform.position.x,\n        next->transform.position.y - previous->transform.position.y,\n        next->transform.position.z - previous->transform.position.z);\n    previous->transform.basis_forward.Normalize();\n    previous->transform.basis_right.cross_vectors(\n        &previous->transform.basis_up,\n        &previous->transform.basis_forward);\n}"
        }
      ]
    },
    {
      "name": "screw-angle-helper-calls",
      "find": "            } else {\n                float up_y = cosine(angle);\n                float up_x = -sine(angle);\n                orient_previous_with_fixed_up(\n                    &primary_samples[sample_index - 1],\n                    &primary_samples[sample_index],\n                    up_x,\n                    up_y,\n                    0.0f,\n                    0.0f);\n                orient_previous_with_fixed_up(\n                    &secondary_samples[sample_index - 1],\n                    &secondary_samples[sample_index],\n                    up_x,\n                    up_y,\n                    0.0f,\n                    0.0f);\n            }",
      "replacements": [
        {
          "name": "call-angle-helper",
          "text": "            } else {\n                orient_previous_from_screw_angle(\n                    &primary_samples[sample_index - 1],\n                    &primary_samples[sample_index],\n                    angle);\n                orient_previous_from_screw_angle(\n                    &secondary_samples[sample_index - 1],\n                    &secondary_samples[sample_index],\n                    angle);\n            }"
        },
        {
          "name": "call-angle-helper-through-pointers",
          "text": "            } else {\n                PathAttachmentSample* previous_primary =\n                    &primary_samples[sample_index - 1];\n                PathAttachmentSample* next_primary =\n                    &primary_samples[sample_index];\n                orient_previous_from_screw_angle(\n                    previous_primary, next_primary, angle);\n                PathAttachmentSample* previous_secondary =\n                    &secondary_samples[sample_index - 1];\n                PathAttachmentSample* next_secondary =\n                    &secondary_samples[sample_index];\n                orient_previous_from_screw_angle(\n                    previous_secondary, next_secondary, angle);\n            }"
        }
      ]
    }
  ]
}
