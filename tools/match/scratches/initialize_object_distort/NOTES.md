# cRDistort::Init

`0x41aa30` is the authored `cRDistort::Init()` method from `ObjectProc.o`.
Android and iOS preserve the owner and method; Windows embeds the five-float
subobject at `cRObject +0x80`.

- VC6 symbol: `?Init@cRDistort@@QAEXXZ`
- exact Windows match: 7/7 instructions
- live caller: `cRSubGoldy::Init`
