{
  "schema": 1,
  "sites": [
    {
      "name": "setter-parameter-lifetimes",
      "find": "void Weapon::set_weapon_animation(\n    int animation_id,\n    bool immediate,\n    int mode_flags)",
      "replacements": [
        {
          "name": "register-animation-id",
          "text": "void Weapon::set_weapon_animation(\n    register int animation_id,\n    bool immediate,\n    int mode_flags)"
        },
        {
          "name": "const-animation-id",
          "text": "void Weapon::set_weapon_animation(\n    const int animation_id,\n    bool immediate,\n    int mode_flags)"
        },
        {
          "name": "signed-animation-id",
          "text": "void Weapon::set_weapon_animation(\n    signed int animation_id,\n    bool immediate,\n    int mode_flags)"
        },
        {
          "name": "register-immediate",
          "text": "void Weapon::set_weapon_animation(\n    int animation_id,\n    register bool immediate,\n    int mode_flags)"
        },
        {
          "name": "register-mode-flags",
          "text": "void Weapon::set_weapon_animation(\n    int animation_id,\n    bool immediate,\n    register int mode_flags)"
        },
        {
          "name": "register-all-parameters",
          "text": "void Weapon::set_weapon_animation(\n    register int animation_id,\n    register bool immediate,\n    register int mode_flags)"
        }
      ]
    }
  ]
}
