{
  "schema": 1,
  "sites": [
    {
      "name": "terminal-lateral-scale",
      "find": "                        Vector3 lateral_offset(\n                            lateral * sample[-1].transform.basis_right.x,\n                            lateral * sample[-1].transform.basis_right.y,\n                            lateral * sample[-1].transform.basis_right.z);",
      "replacements": [
        {
          "name": "operator-scale",
          "text": "                        Vector3 lateral_offset =\n                            sample[-1].transform.basis_right * lateral;"
        }
      ]
    },
    {
      "name": "terminal-position-add",
      "find": "                        Vector3 point(\n                            lateral_offset.x + endpoint.x,\n                            lateral_offset.y + endpoint.y,\n                            lateral_offset.z + endpoint.z);",
      "replacements": [
        {
          "name": "position-first",
          "text": "                        Vector3 point =\n                            endpoint + lateral_offset;"
        },
        {
          "name": "offset-first",
          "text": "                        Vector3 point =\n                            lateral_offset + endpoint;"
        }
      ]
    },
    {
      "name": "ordinary-lateral-scale",
      "find": "                        Vector3 lateral_offset(\n                            lateral * sample->transform.basis_right.x,\n                            lateral * sample->transform.basis_right.y,\n                            lateral * sample->transform.basis_right.z);",
      "replacements": [
        {
          "name": "operator-scale",
          "text": "                        Vector3 lateral_offset =\n                            sample->transform.basis_right * lateral;"
        }
      ]
    },
    {
      "name": "ordinary-position-add",
      "find": "                        Vector3 point(\n                            lateral_offset.x + sample->transform.position.x,\n                            lateral_offset.y + sample->transform.position.y,\n                            lateral_offset.z + sample->transform.position.z);",
      "replacements": [
        {
          "name": "position-first",
          "text": "                        Vector3 point =\n                            sample->transform.position + lateral_offset;"
        },
        {
          "name": "offset-first",
          "text": "                        Vector3 point =\n                            lateral_offset + sample->transform.position;"
        }
      ]
    }
  ]
}
