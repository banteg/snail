{
  "schema": 1,
  "sites": [
    {
      "name": "authored-ring-argument-lifetimes",
      "find": "                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_NORMAL_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_POWER_UP_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_EXPLODE_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_SLOW_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);",
      "replacements": [
        {
          "name": "branch-local-player-pointers",
          "text": "                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    Player* authored_ring_player = &player;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_NORMAL_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    Player* authored_ring_player = &player;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_POWER_UP_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    Player* authored_ring_player = &player;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_EXPLODE_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    Player* authored_ring_player = &player;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_SLOW_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);"
        },
        {
          "name": "branch-local-speed-values",
          "text": "                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_NORMAL_AUTHORED, &player,\n                                        authored_ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_POWER_UP_AUTHORED, &player,\n                                        authored_ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_EXPLODE_AUTHORED, &player,\n                                        authored_ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_SLOW_AUTHORED, &player,\n                                        authored_ring_speed);"
        },
        {
          "name": "branch-local-player-then-speed",
          "text": "                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    Player* authored_ring_player = &player;\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_NORMAL_AUTHORED, authored_ring_player,\n                                        authored_ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    Player* authored_ring_player = &player;\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_POWER_UP_AUTHORED, authored_ring_player,\n                                        authored_ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    Player* authored_ring_player = &player;\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_EXPLODE_AUTHORED, authored_ring_player,\n                                        authored_ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    Player* authored_ring_player = &player;\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_SLOW_AUTHORED, authored_ring_player,\n                                        authored_ring_speed);"
        },
        {
          "name": "branch-local-speed-then-player",
          "text": "                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    Player* authored_ring_player = &player;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_NORMAL_AUTHORED, authored_ring_player,\n                                        authored_ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    Player* authored_ring_player = &player;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_POWER_UP_AUTHORED, authored_ring_player,\n                                        authored_ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    Player* authored_ring_player = &player;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_EXPLODE_AUTHORED, authored_ring_player,\n                                        authored_ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    float authored_ring_speed = runtime_rows[cell_index].ring_speed;\n                                    Player* authored_ring_player = &player;\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_SLOW_AUTHORED, authored_ring_player,\n                                        authored_ring_speed);"
        },
        {
          "name": "common-player-pointer",
          "text": "                                Player* authored_ring_player = &player;\n                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_NORMAL_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_POWER_UP_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_EXPLODE_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_SLOW_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);"
        }
      ]
    }
  ]
}
