{
  "schema": 1,
  "sites": [
    {
      "name": "authored-ring-cell-lifetime",
      "find": "                            if (cell_slot->cell.tile_id == SUBLOC_TILE_RING_MARKER) {\n                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_NORMAL_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_POWER_UP_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_EXPLODE_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    AddRing(\n                                        &cell_slot->cell, SUB_RING_KIND_SLOW_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else {\n                                    goto after_authored_ring;\n                                }\n                                player.last_ring_spawn_z =\n                                    cell_slot->cell.position.z;",
      "replacements": [
        {
          "name": "common-cell-pointer",
          "text": "                            if (cell_slot->cell.tile_id == SUBLOC_TILE_RING_MARKER) {\n                                cRSubLoc* authored_ring_cell = &cell_slot->cell;\n                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_NORMAL_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_POWER_UP_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_EXPLODE_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_SLOW_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else {\n                                    goto after_authored_ring;\n                                }\n                                player.last_ring_spawn_z =\n                                    authored_ring_cell->position.z;"
        },
        {
          "name": "common-cell-reference",
          "text": "                            if (cell_slot->cell.tile_id == SUBLOC_TILE_RING_MARKER) {\n                                cRSubLoc& authored_ring_cell = cell_slot->cell;\n                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    AddRing(\n                                        &authored_ring_cell, SUB_RING_KIND_NORMAL_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    AddRing(\n                                        &authored_ring_cell, SUB_RING_KIND_POWER_UP_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    AddRing(\n                                        &authored_ring_cell, SUB_RING_KIND_EXPLODE_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    AddRing(\n                                        &authored_ring_cell, SUB_RING_KIND_SLOW_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else {\n                                    goto after_authored_ring;\n                                }\n                                player.last_ring_spawn_z =\n                                    authored_ring_cell.position.z;"
        },
        {
          "name": "common-call-cell-pointer",
          "text": "                            if (cell_slot->cell.tile_id == SUBLOC_TILE_RING_MARKER) {\n                                cRSubLoc* authored_ring_cell = &cell_slot->cell;\n                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_NORMAL_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_POWER_UP_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_EXPLODE_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_SLOW_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else {\n                                    goto after_authored_ring;\n                                }\n                                player.last_ring_spawn_z =\n                                    cell_slot->cell.position.z;"
        },
        {
          "name": "branch-local-cell-pointers",
          "text": "                            if (cell_slot->cell.tile_id == SUBLOC_TILE_RING_MARKER) {\n                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    cRSubLoc* authored_ring_cell = &cell_slot->cell;\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_NORMAL_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    cRSubLoc* authored_ring_cell = &cell_slot->cell;\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_POWER_UP_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    cRSubLoc* authored_ring_cell = &cell_slot->cell;\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_EXPLODE_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    cRSubLoc* authored_ring_cell = &cell_slot->cell;\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_SLOW_AUTHORED, &player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else {\n                                    goto after_authored_ring;\n                                }\n                                player.last_ring_spawn_z =\n                                    cell_slot->cell.position.z;"
        },
        {
          "name": "common-cell-and-player-pointers",
          "text": "                            if (cell_slot->cell.tile_id == SUBLOC_TILE_RING_MARKER) {\n                                cRSubLoc* authored_ring_cell = &cell_slot->cell;\n                                Player* authored_ring_player = &player;\n                                if ((ring_flags & SUBROW_FLAG_RING_NORMAL) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_NORMAL_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_POWER_UP) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_POWER_UP_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_EXPLODE) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_EXPLODE_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else if ((ring_flags & SUBROW_FLAG_RING_SLOW) != 0) {\n                                    AddRing(\n                                        authored_ring_cell, SUB_RING_KIND_SLOW_AUTHORED, authored_ring_player,\n                                        runtime_rows[cell_index].ring_speed);\n                                } else {\n                                    goto after_authored_ring;\n                                }\n                                authored_ring_player->last_ring_spawn_z =\n                                    authored_ring_cell->position.z;"
        }
      ]
    }
  ]
}
