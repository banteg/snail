{
  "schema": 1,
  "sites": [
    {
      "name": "salt-source-vector",
      "find": "                    delta =\n                        *(Vec3*)(slot + SALT_POSITION_FROM_SUBGAME)\n                        - cached_camera_target_world;\n                    probe_salt = delta;\n                    if (delta.z < 1.0f && normalize_vector(&probe_salt) < 0.98000002f) {",
      "replacements": [
        {
          "name": "branch-local",
          "text": "                    delta =\n                        *(Vec3*)(slot + SALT_POSITION_FROM_SUBGAME)\n                        - cached_camera_target_world;\n                    Vec3 salt_probe = delta;\n                    if (delta.z < 1.0f\n                        && normalize_vector(&salt_probe) < 0.98000002f) {"
        }
      ]
    },
    {
      "name": "firework-source-vector",
      "find": "                                burst_offset = probe_b * half;\n                                probe_salt.x = burst_offset.x + cached_camera_target_world.x;\n                                burst_position.x = probe_salt.x;\n                                int slot_id = player_slot;\n                                probe_salt.y = burst_offset.y + cached_camera_target_world.y;\n                                burst_position.y = probe_salt.y;\n                                probe_salt.z = burst_offset.z + cached_camera_target_world.z;\n                                burst_position.z = probe_salt.z;",
      "replacements": [
        {
          "name": "branch-local",
          "text": "                                burst_offset = probe_b * half;\n                                Vec3 burst_sum;\n                                burst_sum.x = burst_offset.x + cached_camera_target_world.x;\n                                burst_position.x = burst_sum.x;\n                                int slot_id = player_slot;\n                                burst_sum.y = burst_offset.y + cached_camera_target_world.y;\n                                burst_position.y = burst_sum.y;\n                                burst_sum.z = burst_offset.z + cached_camera_target_world.z;\n                                burst_position.z = burst_sum.z;"
        }
      ]
    },
    {
      "name": "parcel-source-vector",
      "find": "                probe_salt.x =\n                    game->parcel_manager.slots[n].position.x\n                    - cached_camera_target_world.x;\n                probe_salt.y =\n                    game->parcel_manager.slots[n].position.y\n                    - cached_camera_target_world.y;\n                probe_salt.z =\n                    game->parcel_manager.slots[n].position.z\n                    - cached_camera_target_world.z;\n                probe_rings = probe_salt;\n                if (probe_salt.z < 1.0f && normalize_vector(&probe_rings) < 1.24f) {",
      "replacements": [
        {
          "name": "branch-local",
          "text": "                Vec3 parcel_delta;\n                parcel_delta.x =\n                    game->parcel_manager.slots[n].position.x\n                    - cached_camera_target_world.x;\n                parcel_delta.y =\n                    game->parcel_manager.slots[n].position.y\n                    - cached_camera_target_world.y;\n                parcel_delta.z =\n                    game->parcel_manager.slots[n].position.z\n                    - cached_camera_target_world.z;\n                probe_rings = parcel_delta;\n                if (parcel_delta.z < 1.0f\n                    && normalize_vector(&probe_rings) < 1.24f) {"
        }
      ]
    },
    {
      "name": "ring-source-vector",
      "find": "            probe_salt.x =\n                game->ring_effects.slots[jj].transform.position.x\n                - cached_camera_target_world.x;\n            probe_salt.y =\n                game->ring_effects.slots[jj].transform.position.y\n                - cached_camera_target_world.y;\n            probe_salt.z =\n                game->ring_effects.slots[jj].transform.position.z\n                - cached_camera_target_world.z;\n            probe_fx = probe_salt;\n            if (probe_salt.z < 1.0f) {",
      "replacements": [
        {
          "name": "branch-local",
          "text": "            Vec3 ring_delta;\n            ring_delta.x =\n                game->ring_effects.slots[jj].transform.position.x\n                - cached_camera_target_world.x;\n            ring_delta.y =\n                game->ring_effects.slots[jj].transform.position.y\n                - cached_camera_target_world.y;\n            ring_delta.z =\n                game->ring_effects.slots[jj].transform.position.z\n                - cached_camera_target_world.z;\n            probe_fx = ring_delta;\n            if (ring_delta.z < 1.0f) {"
        }
      ]
    }
  ]
}
