{
  "schema": 1,
  "sites": [
    {
      "name": "texel-clamp-operations",
      "find": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            if (x < 0) {\n                x = 0;\n            } else if (x > width - 1) {\n                x = width - 1;\n            }\n\n            if (y < 0) {\n                y = 0;\n            } else if (y > height - 1) {\n                y = height - 1;\n            }\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n",
      "replacements": [
        {
          "name": "early-value-1",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline int clamp_texel(int value, int size) { if (value < 0) return 0; if (value > size - 1) return size - 1; return value; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            x = clamp_texel(x, width);\n\n            if (y < 0) {\n                y = 0;\n            } else if (y > height - 1) {\n                y = height - 1;\n            }\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "early-value-2",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline int clamp_texel(int value, int size) { if (value < 0) return 0; if (value > size - 1) return size - 1; return value; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            if (x < 0) {\n                x = 0;\n            } else if (x > width - 1) {\n                x = width - 1;\n            }\n\n            y = clamp_texel(y, height);\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "early-value-3",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline int clamp_texel(int value, int size) { if (value < 0) return 0; if (value > size - 1) return size - 1; return value; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            x = clamp_texel(x, width);\n\n            y = clamp_texel(y, height);\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "nested-value-1",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline int clamp_texel(int value, int size) { if (value < 0) value = 0; else if (value > size - 1) value = size - 1; return value; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            x = clamp_texel(x, width);\n\n            if (y < 0) {\n                y = 0;\n            } else if (y > height - 1) {\n                y = height - 1;\n            }\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "nested-value-2",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline int clamp_texel(int value, int size) { if (value < 0) value = 0; else if (value > size - 1) value = size - 1; return value; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            if (x < 0) {\n                x = 0;\n            } else if (x > width - 1) {\n                x = width - 1;\n            }\n\n            y = clamp_texel(y, height);\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "nested-value-3",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline int clamp_texel(int value, int size) { if (value < 0) value = 0; else if (value > size - 1) value = size - 1; return value; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            x = clamp_texel(x, width);\n\n            y = clamp_texel(y, height);\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "reference-1",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline void clamp_texel(int& value, int size) { if (value < 0) value = 0; else if (value > size - 1) value = size - 1; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            clamp_texel(x, width);\n\n            if (y < 0) {\n                y = 0;\n            } else if (y > height - 1) {\n                y = height - 1;\n            }\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "reference-2",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline void clamp_texel(int& value, int size) { if (value < 0) value = 0; else if (value > size - 1) value = size - 1; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            if (x < 0) {\n                x = 0;\n            } else if (x > width - 1) {\n                x = width - 1;\n            }\n\n            clamp_texel(y, height);\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "reference-3",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline void clamp_texel(int& value, int size) { if (value < 0) value = 0; else if (value > size - 1) value = size - 1; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            clamp_texel(x, width);\n\n            clamp_texel(y, height);\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "pointer-1",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline void clamp_texel(int* value, int size) { if (*value < 0) *value = 0; else if (*value > size - 1) *value = size - 1; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            clamp_texel(&x, width);\n\n            if (y < 0) {\n                y = 0;\n            } else if (y > height - 1) {\n                y = height - 1;\n            }\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "pointer-2",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline void clamp_texel(int* value, int size) { if (*value < 0) *value = 0; else if (*value > size - 1) *value = size - 1; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            if (x < 0) {\n                x = 0;\n            } else if (x > width - 1) {\n                x = width - 1;\n            }\n\n            clamp_texel(&y, height);\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        },
        {
          "name": "pointer-3",
          "text": "// cRBorder::MouseTest() / border_mouse_test @ 0x404580\n\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"tga_image_view.h\"\n\nextern cRSpriteManager g_sprite_manager; // data_790f30\n\nstatic inline void clamp_texel(int* value, int size) { if (*value < 0) *value = 0; else if (*value > size - 1) *value = size - 1; }\n\nunsigned char cRBorder::MouseTest()\n{\n    cRMouse* mouse = &g_game->players[0].mouse_cursor;\n\n    if (texture_hit_test_enabled) {\n        float texture_right = texture_hit_width;\n        texture_right += texture_hit_x;\n        float texture_bottom = texture_hit_height;\n        texture_bottom += texture_hit_y;\n        if (mouse->saved_x >= texture_hit_x\n            && texture_right > mouse->saved_x\n            && mouse->saved_y >= texture_hit_y\n            && texture_bottom > mouse->saved_y) {\n            float u = texture_hit_x;\n            u = (mouse->saved_x - u) / texture_hit_width;\n            float v = texture_hit_y;\n            v = (mouse->saved_y - v) / texture_hit_height;\n            TgaImageView* mask =\n                g_sprite_manager.GetTga(texture_hit_test_sprite);\n\n            int width = mask->width;\n            int x = (int)((float)width * u);\n            int height = mask->height;\n            int y = (int)((float)height * v);\n\n            clamp_texel(&x, width);\n\n            clamp_texel(&y, height);\n\n            int row = width;\n            row *= y;\n            int pixel_index = row + x + 6;\n\n            if (*((unsigned char*)mask + pixel_index * 3) == 0)\n                return 1;\n        }\n    } else if (layout_x - target_padding < mouse->saved_x\n        && layout_width + target_padding + layout_x > mouse->saved_x\n        && layout_y - target_padding < mouse->saved_y\n        && layout_height + target_padding + layout_y > mouse->saved_y) {\n        return 1;\n    }\n\n    return 0;\n}\n"
        }
      ]
    }
  ]
}
