{
  "schema": 1,
  "sites": [
    {
      "name": "insertion-terminal-home-owners",
      "find": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* slot = &text_buffer[input_cursor];\n            while (next != 0) {\n                *slot = carry;\n                ++slot;\n                carry = next;\n                next = *slot;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char* slot = &text_buffer[input_cursor];\n            while (*slot != 0) {\n                char next = *slot;\n                *slot = carry;\n                ++slot;\n                carry = next;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n",
      "replacements": [
        {
          "name": "combined-finish",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n\n    if (((input_flags & 0x0c) != 0 &&\n         (input_ok_state()->AI(),\n          (input_ok_state()->ok_widget->widget_flags &\n           FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED) != 0)) ||\n        key == 5 ||\n        (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* slot = &text_buffer[input_cursor];\n            while (next != 0) {\n                *slot = carry;\n                ++slot;\n                carry = next;\n                next = *slot;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char* slot = &text_buffer[input_cursor];\n            while (*slot != 0) {\n                char next = *slot;\n                *slot = carry;\n                ++slot;\n                carry = next;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "initial-flags-owner",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n\n    unsigned int initial_flags = input_flags;\n    if ((initial_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* slot = &text_buffer[input_cursor];\n            while (next != 0) {\n                *slot = carry;\n                ++slot;\n                carry = next;\n                next = *slot;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char* slot = &text_buffer[input_cursor];\n            while (*slot != 0) {\n                char next = *slot;\n                *slot = carry;\n                ++slot;\n                carry = next;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "home-for",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* slot = &text_buffer[input_cursor];\n            while (next != 0) {\n                *slot = carry;\n                ++slot;\n                carry = next;\n                next = *slot;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        for (; pos > 0; --pos) {\n            if (text_buffer[pos] == '>')\n                break;\n        }\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char* slot = &text_buffer[input_cursor];\n            while (*slot != 0) {\n                char next = *slot;\n                *slot = carry;\n                ++slot;\n                carry = next;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "key-switch",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    switch (key) {\n    case 6: {\n        if ((input_flags & 1) != 0)\n            goto input_character;\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* slot = &text_buffer[input_cursor];\n            while (next != 0) {\n                *slot = carry;\n                ++slot;\n                carry = next;\n                next = *slot;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n\n        break;\n    }\n    case 3: {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n\n        break;\n    }\n    case 4: {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n\n        break;\n    }\n    case 7: {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n\n        break;\n    }\n    case 8: {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n\n        break;\n    }\n    case 9: {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n\n        break;\n    }\n    case 10: {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n\n        break;\n    }\n    case 1: {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n\n        break;\n    }\n    case 2: {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n\n        break;\n    }\n    case 0:\n        break;\n    default:\ninput_character: {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char* slot = &text_buffer[input_cursor];\n            while (*slot != 0) {\n                char next = *slot;\n                *slot = carry;\n                ++slot;\n                carry = next;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n\n        break;\n    }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "key-switch-combined",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n\n    if (((input_flags & 0x0c) != 0 &&\n         (input_ok_state()->AI(),\n          (input_ok_state()->ok_widget->widget_flags &\n           FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED) != 0)) ||\n        key == 5 ||\n        (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    switch (key) {\n    case 6: {\n        if ((input_flags & 1) != 0)\n            goto input_character;\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* slot = &text_buffer[input_cursor];\n            while (next != 0) {\n                *slot = carry;\n                ++slot;\n                carry = next;\n                next = *slot;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n\n        break;\n    }\n    case 3: {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n\n        break;\n    }\n    case 4: {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n\n        break;\n    }\n    case 7: {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n\n        break;\n    }\n    case 8: {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n\n        break;\n    }\n    case 9: {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n\n        break;\n    }\n    case 10: {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n\n        break;\n    }\n    case 1: {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n\n        break;\n    }\n    case 2: {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n\n        break;\n    }\n    case 0:\n        break;\n    default:\ninput_character: {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char* slot = &text_buffer[input_cursor];\n            while (*slot != 0) {\n                char next = *slot;\n                *slot = carry;\n                ++slot;\n                carry = next;\n            }\n            *slot = carry;\n            slot[1] = 0;\n            ++input_length;\n        }\n\n        break;\n    }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-insertion-chain",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-insertion-switch",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    switch (key) {\n    case 6: {\n        if ((input_flags & 1) != 0)\n            goto input_character;\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n\n        break;\n    }\n    case 3: {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n\n        break;\n    }\n    case 4: {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n\n        break;\n    }\n    case 7: {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n\n        break;\n    }\n    case 8: {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n\n        break;\n    }\n    case 9: {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n\n        break;\n    }\n    case 10: {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n\n        break;\n    }\n    case 1: {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n\n        break;\n    }\n    case 2: {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n\n        break;\n    }\n    case 0:\n        break;\n    default:\ninput_character: {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n\n        break;\n    }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "home-shared-late-final",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "home-local-publication",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* insertion_end = &text_buffer[input_cursor];\n            char final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* insertion_end = &text_buffer[input_cursor];\n            char final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "home-local-late-final",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* insertion_end = &text_buffer[input_cursor];\n            char final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* insertion_end = &text_buffer[input_cursor];\n            char final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-flags-owner",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n    unsigned int flags = input_flags;\n\n    if ((flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    flags = widget_flags;\n    unsigned int cleared_flags =\n        flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "complete-direct-mobile-publications",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    widget_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    --input_length;\n    widget_flags =\n        (original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE)\n        | FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "complete-cleared-store-before-length-local",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    widget_flags = cleared_flags;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "complete-cleared-store-before-closing-local",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    widget_flags = cleared_flags;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    int remaining_length = input_length - 1;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "complete-length-store-between-publications",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    widget_flags = cleared_flags;\n    int remaining_length = input_length - 1;\n    input_length = remaining_length;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "complete-direct-cleared-named-closing",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    widget_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "complete-named-cleared-direct-closing",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    widget_flags = cleared_flags;\n    --input_length;\n    widget_flags =\n        (original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE)\n        | FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "finish-late-compound-corrected",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if (!((((input_flags & 0x0c) != 0 &&\n         (input_ok_state()->AI(),\n          (input_ok_state()->ok_widget->widget_flags &\n           FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED) != 0)) ||\n        key == 5 ||\n        (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0))) {\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    } else {\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n    }\n}\n"
        },
        {
          "name": "direct-completion",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    widget_flags &= ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    --input_length;\n    widget_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "rotation-old-pending-first",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                char displaced = next;\n                *insertion_end++ = carry;\n                next = *insertion_end;\n                carry = displaced;\n                final_character = displaced;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                char displaced = next;\n                *insertion_end++ = carry;\n                next = *insertion_end;\n                carry = displaced;\n                final_character = displaced;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "rotation-next-address-first",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = insertion_end[0];\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = insertion_end[0];\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "rotation-lookahead-before-advance",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end = carry;\n                final_character = next;\n                next = insertion_end[1];\n                ++insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end = carry;\n                final_character = next;\n                next = insertion_end[1];\n                ++insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "rotation-carry-after-advance",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end = carry;\n                final_character = next;\n                next = *++insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end = carry;\n                final_character = next;\n                next = *++insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "finish-early-structured",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    bool finish = false;\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        finish = (input_ok_state()->ok_widget->widget_flags &\n                  FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED) != 0;\n    }\n    if (key == 5 ||\n        (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        finish = true;\n    if (finish) {\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n    } else {\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    }\n}\n"
        },
        {
          "name": "finish-late-structured",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    bool finish = false;\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        finish = (input_ok_state()->ok_widget->widget_flags &\n                  FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED) != 0;\n    }\n    if (key == 5 ||\n        (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        finish = true;\n    if (!finish) {\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    } else {\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n    }\n}\n"
        },
        {
          "name": "finish-bool-goto",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    bool finish = false;\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        finish = (input_ok_state()->ok_widget->widget_flags &\n                  FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED) != 0;\n    }\n    if (key == 5 ||\n        (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        finish = true;\n    if (finish)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "finish-early-compound",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if (((input_flags & 0x0c) != 0 &&\n         (input_ok_state()->AI(),\n          (input_ok_state()->ok_widget->widget_flags &\n           FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED) != 0)) ||\n        key == 5 ||\n        (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0) {\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n    } else {\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    }\n}\n"
        },
        {
          "name": "finish-publish-before-store",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                final_character = next;\n                *insertion_end++ = carry;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                final_character = next;\n                *insertion_end++ = carry;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "finish-publish-after-store",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "finish-split-cursor-advance",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end = carry;\n                final_character = next;\n                ++insertion_end;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end = carry;\n                final_character = next;\n                ++insertion_end;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "finish-for-pending-carry",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = next;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = next;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-after-key",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n    char* insertion_end;\n    char final_character;\n\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-before-dispatch",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    char* insertion_end;\n    char final_character;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-char-first",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char final_character;\n    char* insertion_end;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-next-owner",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char next;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-carry-owner",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char carry;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-consumed-key",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            key = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = key;\n            while (next != 0) {\n                *insertion_end++ = key;\n                final_character = next;\n                next = *insertion_end;\n                key = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            key = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = key;\n            while (next != 0) {\n                *insertion_end++ = key;\n                final_character = next;\n                next = *insertion_end;\n                key = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-guarded",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            if (next != 0) {\n                do {\n                    *insertion_end++ = carry;\n                    final_character = next;\n                    next = *insertion_end;\n                    carry = final_character;\n                } while (*insertion_end != 0);\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            if (next != 0) {\n                do {\n                    *insertion_end++ = carry;\n                    final_character = next;\n                    next = *insertion_end;\n                    carry = final_character;\n                } while (*insertion_end != 0);\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-memory-test",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (*insertion_end != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (*insertion_end != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-early-next",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char next = text_buffer[input_cursor];\n            char carry = ' ';\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char next = text_buffer[input_cursor];\n            char carry = ' ';\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-late-final",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "home-current-reload",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        int old_cursor = input_cursor;\n        char* remove = &text_buffer[old_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "home-guarded",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        if (text_buffer[input_cursor] != 0) {\n            do {\n                *remove = remove[1];\n                ++remove;\n            } while (*remove != 0);\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[pos];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "retained",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                char displaced = next;\n                *insertion_end++ = carry;\n                next = *insertion_end;\n                carry = displaced;\n                final_character = displaced;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                char displaced = next;\n                *insertion_end++ = carry;\n                next = *insertion_end;\n                carry = displaced;\n                final_character = displaced;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-structured",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-structured-next-first",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n                carry = final_character;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-structured-local-end",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            char* insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                final_character = carry;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-structured-local-final",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            char final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                char final_character = carry;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            char final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = next;\n                next = *insertion_end;\n                char final_character = carry;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-final-before-next",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-final-and-carry-assignment",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = final_character = next;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = final_character = next;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "shared-loop-final-condition",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            for (; next != 0; carry = final_character) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            for (; next != 0; carry = final_character) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n            }\n            goto finish_insertion;\n        }\n    }\n\n    goto input_postprocess;\nfinish_insertion:\n    *insertion_end = final_character;\n    insertion_end[1] = 0;\n    ++input_length;\ninput_postprocess:\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "structured-final-before-next",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                final_character = next;\n                carry = final_character;\n                next = *insertion_end;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "structured-final-and-carry-assignment",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = final_character = next;\n                next = *insertion_end;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            while (next != 0) {\n                *insertion_end++ = carry;\n                carry = final_character = next;\n                next = *insertion_end;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        },
        {
          "name": "structured-loop-final-condition",
          "text": "// border_input_text @ 0x4035b0 (thiscall)\n\n#include \"border_manager.h\"\n#include \"frontend_widget.h\"\n#include \"game_root.h\"\n#include \"input_ok_state.h\"\n\n\nchar read_repeating_text_input_key_code();\n\nvoid cRBorder::InputText()\n{\n    char* insertion_end;\n    char final_character;\n    char key = read_repeating_text_input_key_code();\n\n    if ((input_flags & 0x0c) != 0) {\n        input_ok_state()->AI();\n        if ((input_ok_state()->ok_widget->widget_flags\n                & FRONTEND_WIDGET_FLAG_PRIMARY_ACTION_TRIGGERED)\n            != 0)\n            goto finish_input;\n    }\n\n    if (key == 5\n        || (widget_flags & FRONTEND_WIDGET_FLAG_TEXT_INPUT_SUBMIT_REQUESTED) != 0)\n        goto finish_input;\n\n    if (key == 6 && (input_flags & 1) == 0) {\n        if ((input_flags & 2) == 0) {\n            text_buffer[input_cursor] = '>';\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            for (; next != 0; carry = final_character) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    } else if (key == 3) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char previous = current[-1];\n            *current = previous;\n            current[-1] = old;\n            int updated_cursor = input_cursor - 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 4) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* current = &text_buffer[cursor];\n            char old = *current;\n            char next = current[1];\n            *current = next;\n            current[1] = old;\n            int updated_cursor = input_cursor + 1;\n            input_cursor = updated_cursor;\n        }\n    } else if (key == 7) {\n        int pos = input_cursor;\n        int column = -1;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>')\n            --pos;\n\n        if (pos != 0) {\n            int next_column = 0;\n            while (pos > 0) {\n                if (text_buffer[pos] == '>')\n                    break;\n                ++next_column;\n                --pos;\n            }\n            if (text_buffer[pos] == '>')\n                ++pos;\n            if (column > next_column)\n                column = next_column;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = pos + column;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 8) {\n        int pos = input_cursor;\n        int column = 0;\n        while (pos > 0) {\n            if (text_buffer[pos] == '>')\n                break;\n            ++column;\n            --pos;\n        }\n        if (text_buffer[pos] == '>') {\n            ++pos;\n            --column;\n        }\n\n        int separator = pos;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n        if (text_buffer[separator] == '>') {\n            ++separator;\n            int next_length = 0;\n            while (text_buffer[separator + next_length] != 0\n                && text_buffer[separator + next_length] != '>') {\n                ++next_length;\n            }\n            if (column > next_length)\n                column = next_length;\n\n            char* remove = &text_buffer[input_cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n\n            input_cursor = separator + column - 1;\n            char* insert = &text_buffer[input_cursor];\n            char carry = *insert;\n            do {\n                char next = insert[1];\n                insert[1] = carry;\n                ++insert;\n                carry = next;\n            } while (*insert != 0);\n        }\n    } else if (key == 9) {\n        int separator = input_cursor;\n        while (text_buffer[separator] != 0 && text_buffer[separator] != '>')\n            ++separator;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = separator - 1;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 10) {\n        int pos = input_cursor;\n        while (pos > 0 && text_buffer[pos] != '>')\n            --pos;\n        if (text_buffer[pos] == '>')\n            ++pos;\n\n        char* remove = &text_buffer[input_cursor];\n        while (*remove != 0) {\n            *remove = remove[1];\n            ++remove;\n        }\n\n        input_cursor = pos;\n        char* insert = &text_buffer[input_cursor];\n        char carry = *insert;\n        do {\n            char next = insert[1];\n            insert[1] = carry;\n            ++insert;\n            carry = next;\n        } while (*insert != 0);\n    } else if (key == 1) {\n        int cursor = input_cursor;\n        if (cursor > 0) {\n            --cursor;\n            input_cursor = cursor;\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key == 2) {\n        int cursor = input_cursor;\n        if (cursor < input_length) {\n            char* remove = &text_buffer[cursor];\n            while (*remove != 0) {\n                *remove = remove[1];\n                ++remove;\n            }\n            --input_length;\n        }\n    } else if (key != 0) {\n        if (input_length < input_capacity\n            && (key != ' ' || (input_flags & 1) == 0)\n            && ((input_flags & 2) == 0 || (key != ',' && key != '.'))) {\n            text_buffer[input_cursor] = key;\n            ++input_cursor;\n\n            char carry = ' ';\n            char next = text_buffer[input_cursor];\n            insertion_end = &text_buffer[input_cursor];\n            final_character = carry;\n            for (; next != 0; carry = final_character) {\n                *insertion_end++ = carry;\n                final_character = next;\n                next = *insertion_end;\n            }\n            *insertion_end = final_character;\n            insertion_end[1] = 0;\n            ++input_length;\n        }\n    }\n\n    if ((input_flags & 2) != 0) {\n        int index = 0;\n        char ch = text_buffer[0];\n        while (ch != 0) {\n            if (text_buffer[index] != ' ' && index != input_cursor)\n                break;\n            if (index != input_cursor) {\n                char* remove = &text_buffer[index];\n                while (*remove != 0) {\n                    *remove = remove[1];\n                    ++remove;\n                }\n                if (index <= input_cursor)\n                    --input_cursor;\n                --input_length;\n            }\n            ch = text_buffer[index + 1];\n            ++index;\n        }\n    }\n\n    input_cursor_blink_progress += input_cursor_blink_step;\n    if (input_cursor_blink_progress > 1.0f) {\n        input_cursor_blink_progress = 0.0f;\n        input_cursor_visible = input_cursor_visible == 0;\n    }\n\n    if (input_cursor_visible == 1)\n        text_buffer[input_cursor] = '|';\n    else\n        text_buffer[input_cursor] = ' ';\n    RePosition();\n    return;\n\nfinish_input:\n    char* remove = &text_buffer[input_cursor];\n    while (*remove != 0) {\n        *remove = remove[1];\n        ++remove;\n    }\n    unsigned int original_flags = widget_flags;\n    unsigned int cleared_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    int remaining_length = input_length - 1;\n    unsigned int closing_flags =\n        original_flags & ~FRONTEND_WIDGET_FLAG_TEXT_INPUT_ACTIVE;\n    closing_flags |= FRONTEND_WIDGET_FLAG_TEXT_INPUT_COMPLETE;\n    widget_flags = cleared_flags;\n    input_length = remaining_length;\n    widget_flags = closing_flags;\n    RePosition();\n    if ((input_flags & 0x0c) != 0)\n        g_game->border_manager\n            .kill_border(input_ok_state()->ok_widget);\n}\n"
        }
      ]
    }
  ]
}
