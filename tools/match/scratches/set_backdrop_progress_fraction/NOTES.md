# set_backdrop_progress_fraction @ 0x410c30

Exact match: 100.00%, 3/3 instructions.

The stable Windows harness name describes its only known Windows input: a
normalized route-progress fraction stored at backdrop offset `+0x6c8`.
`update_subgoldy` drives the setter from the active track-row payload ratio.

The symbol-preserving iOS builds name the corresponding authored member
`cRBackdrop::SetZoom(float)`. The shared Windows source therefore exposes the
member as `Backdrop::set_backdrop_zoom` and the owned field as
`Backdrop::zoom`; the scratch directory and `FUNCTION` selector retain the
stable Windows harness identifier.
