# set_backdrop_progress_fraction @ 0x410c30

Exact match: 100.00%, 3/3 instructions.

Stores the normalized route-progress fraction at backdrop offset `+0x6c8`.
`update_subgoldy` drives this setter from the active track-row payload ratio,
so the matched helper pins that lane as a plain float field update.
